---
phase: 14-baseline-feedback-loop-and-review-gate
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/ood/eval_cli.py
  - tests/test_eval_cli.py
autonomous: true
requirements: [EVAL-05, EVAL-11]
must_haves:
  truths:
    - "User can deliberately create a baseline from an existing eval report via the existing `ood eval` namespace."
    - "User can generate a review artifact from a report that contains failed or errored cases and machine-readable proposed fix recommendations via the existing `ood eval` namespace."
    - "Generated baseline and review files live under ignored `data/evaluation/...` defaults unless the user provides explicit output paths."
  artifacts:
    - path: "src/ood/eval_cli.py"
      provides: "`ood eval baseline` and `ood eval review` commands using Phase 14 artifact helpers while preserving proposed fix metadata"
      contains: ["@eval_app.command(\"baseline\")", "@eval_app.command(\"review\")"]
    - path: "tests/test_eval_cli.py"
      provides: "CLI tests for baseline and review creation, JSON stdout, default paths, German messages, and import safety"
  key_links:
    - from: "src/ood/eval_cli.py"
      to: "src/ood/eval_baseline.py"
      via: "imports save_observational_baseline and save_review_artifact"
      pattern: "from ood.eval_baseline import"
    - from: "ood eval baseline"
      to: "data/evaluation/baselines/current.json"
      via: "default --out path"
      pattern: "data/evaluation/baselines/current.json"
    - from: "ood eval review"
      to: "data/evaluation/reviews/<dataset>-<hash>.review.json"
      via: "default review path derived from report meta"
      pattern: "data/evaluation/reviews"
---

<objective>
Expose the Phase 14 baseline and review artifact creation workflow through the existing `ood eval` Typer namespace.

Purpose: EVAL-05 and EVAL-11 become usable from the CLI without adding top-level commands, hidden auto-promotion, or threshold gates.
Output: New `ood eval baseline` and `ood eval review` commands plus CLI tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-baseline-feedback-loop-and-review-gate/14-CONTEXT.md
@.planning/phases/14-baseline-feedback-loop-and-review-gate/14-RESEARCH.md
@.planning/phases/14-baseline-feedback-loop-and-review-gate/14-01-SUMMARY.md
@.planning/phases/13-evaluation-service-and-cli-reporting/13-03-SUMMARY.md
@src/ood/eval_cli.py
@tests/test_eval_cli.py

<interfaces>
Plan 01 exports to use:
```python
from ood.eval_baseline import (
    load_json_file,
    save_observational_baseline,
    save_review_artifact,
    write_json_file,
)
```

Existing import-safety invariant in `src/ood/eval_cli.py`:
```python
eval_app = typer.Typer(name="eval", help="Lokale Evaluation des OOD Agents über RagEngine.query().", no_args_is_help=True)
from ood.cli import (...)  # must stay after eval_app definition
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `ood eval baseline` command</name>
  <files>src/ood/eval_cli.py, tests/test_eval_cli.py</files>
  <read_first>
    - src/ood/eval_cli.py
    - tests/test_eval_cli.py
    - src/ood/eval_baseline.py
    - .planning/phases/14-baseline-feedback-loop-and-review-gate/14-CONTEXT.md
    - .planning/phases/13-evaluation-service-and-cli-reporting/13-03-SUMMARY.md
  </read_first>
  <behavior>
    - `ood eval baseline --report report.json --out baseline.json` writes an observational baseline JSON file with `artifact_type == "ood_eval_baseline"`, `baseline_kind == "observational"`, `thresholds == null`, and embedded `report.schema_version == 1`.
    - If `--out` is omitted, the command writes to `data/evaluation/baselines/current.json`.
    - Without `--json` and without `--quiet`, stdout contains the German string `Baseline gespeichert:` and the output path.
    - With `--json`, stdout is compact JSON containing `artifact_type`, `baseline_path`, `source_report_hash`, `baseline_kind`, `gate_mode`, and `thresholds`.
    - Invalid report files exit 1 and stderr/stdout contains `Baseline konnte nicht erstellt werden`.
  </behavior>
  <action>Modify `src/ood/eval_cli.py` without moving the existing `eval_app = typer.Typer(...)` line or the `from ood.cli import (...)` block. Add `ReportOption = Annotated[Path, typer.Option("--report", help="Pfad zu einem schema_version=1 Eval-Report.")]` and `BaselineOutOption = Annotated[Path | None, typer.Option("--out", help="Zielpfad für die Baseline-Datei (Default: data/evaluation/baselines/current.json).") ]`. Import `save_observational_baseline` from `ood.eval_baseline` after the existing `ood.cli` import block. Add `@eval_app.command("baseline") def baseline(report: ReportOption, out: BaselineOutOption = None, json_output: JsonOption = False, quiet: QuietOption = False) -> None:`. Resolve `baseline_path = out if out is not None else Path("data/evaluation/baselines/current.json")`. Call `payload = save_observational_baseline(report, baseline_path)`. On `FileNotFoundError`, `ValueError`, or `json.JSONDecodeError`, echo `Baseline konnte nicht erstellt werden: {error}` to stderr and exit 1. For `--json`, echo `json.dumps({"artifact_type": payload["artifact_type"], "baseline_path": str(baseline_path), "source_report_hash": payload["source_report_hash"], "baseline_kind": payload["baseline_kind"], "gate_mode": payload["gate_mode"], "thresholds": payload["thresholds"]}, ensure_ascii=False)` and return. For non-quiet human output, echo `Baseline gespeichert: {baseline_path}` and `Baseline-Typ: observational (keine Schwellwerte)`. This implements D-01, D-02, D-05, and D-06; do not call `EvalRunner.run`, do not auto-promote `ood eval run --out`, and do not add thresholds.</action>
  <verify>
    <automated>uv run pytest tests/test_eval_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '@eval_app.command("baseline")' src/ood/eval_cli.py` succeeds.
    - `grep -q 'data/evaluation/baselines/current.json' src/ood/eval_cli.py` succeeds.
    - `grep -q 'Baseline gespeichert:' src/ood/eval_cli.py` succeeds.
    - `grep -q 'Baseline konnte nicht erstellt werden' src/ood/eval_cli.py` succeeds.
    - `uv run pytest tests/test_eval_cli.py -q` exits 0.
    - `uv run python -c "from ood.eval_cli import eval_app; print('ok')"` prints `ok`.
  </acceptance_criteria>
  <done>`ood eval baseline` creates an explicit observational baseline from a report file and preserves the existing eval import-safety contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `ood eval review` command</name>
  <files>src/ood/eval_cli.py, tests/test_eval_cli.py</files>
  <read_first>
    - src/ood/eval_cli.py
    - tests/test_eval_cli.py
    - src/ood/eval_baseline.py
    - .planning/phases/14-baseline-feedback-loop-and-review-gate/14-CONTEXT.md
    - .planning/phases/14-baseline-feedback-loop-and-review-gate/14-RESEARCH.md
  </read_first>
  <behavior>
    - `ood eval review --report report.json --out review.json` writes a review JSON file with `artifact_type == "ood_eval_review"`, top-level `decision == "deferred"`, and `cases` containing failed and errored cases only.
    - Each generated review case includes `proposed_fix_type == "investigate"` and `proposed_fix_notes is None` by default, while allowing later edits to use `corpus_fix`, `retrieval_fix`, `query_fix`, `dataset_fix`, `baseline_update`, or `investigate`.
    - If `--out` is omitted and the report meta contains `dataset: "mock-v1"` and `dataset_hash: "abcdef..."`, the default path starts with `data/evaluation/reviews/mock-v1-abcdef12` and ends with `.review.json`.
    - Without `--json` and without `--quiet`, stdout contains `Review gespeichert:` and `Review-Fälle:`.
    - With `--json`, stdout is compact JSON containing `artifact_type`, `review_path`, `decision`, `baseline_update_status`, `case_count`, and `proposed_fix_types` with the sorted allowed values `baseline_update`, `corpus_fix`, `dataset_fix`, `investigate`, `query_fix`, `retrieval_fix`.
    - Invalid report files exit 1 and stderr/stdout contains `Review konnte nicht erstellt werden`.
  </behavior>
  <action>Modify `src/ood/eval_cli.py` to import `PROPOSED_FIX_TYPES`, `load_json_file`, and `save_review_artifact` from `ood.eval_baseline`. Add `ReviewOutOption = Annotated[Path | None, typer.Option("--out", help="Zielpfad für die Review-Datei (Default: data/evaluation/reviews/<dataset>-<hash>.review.json).") ]`. Add private helper `_default_review_path(report_path: Path) -> Path` that loads the report with `load_json_file(report_path)`, reads `dataset = str(report.get("meta", {}).get("dataset") or "unknown-dataset")`, reads `dataset_hash = str(report.get("meta", {}).get("dataset_hash") or "unknownhash")[:8]`, replaces `/` and spaces in dataset with `-`, and returns `Path("data/evaluation/reviews") / f"{dataset}-{dataset_hash}.review.json"`. Add `@eval_app.command("review") def review(report: ReportOption, out: ReviewOutOption = None, json_output: JsonOption = False, quiet: QuietOption = False) -> None:`. Resolve `review_path = out if out is not None else _default_review_path(report)`. Call `payload = save_review_artifact(report, review_path)` and rely on Plan 01 helpers to create `proposed_fix_type` and `proposed_fix_notes` for every failed/errored case. On `FileNotFoundError`, `ValueError`, or `json.JSONDecodeError`, echo `Review konnte nicht erstellt werden: {error}` to stderr and exit 1. For `--json`, echo compact JSON with `artifact_type`, `review_path`, `decision`, `baseline_update_status`, `case_count: len(payload["cases"])`, and `proposed_fix_types: sorted(PROPOSED_FIX_TYPES)`. For human output, echo `Review gespeichert: {review_path}`, `Review-Fälle: {len(payload["cases"])} (failed/errored)`, and `Vorgeschlagene Fix-Typen: baseline_update, corpus_fix, dataset_fix, investigate, query_fix, retrieval_fix`. This implements D-03, D-04, D-05, and D-06; do not mark any case approved, do not include passed cases in the review cases array, and do not drop proposed fix fields from the created artifact or JSON stdout.</action>
  <verify>
    <automated>uv run pytest tests/test_eval_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '@eval_app.command("review")' src/ood/eval_cli.py` succeeds.
    - `grep -q 'data/evaluation/reviews' src/ood/eval_cli.py` succeeds.
    - `grep -q 'Review gespeichert:' src/ood/eval_cli.py` succeeds.
    - `grep -q 'Review-Fälle:' src/ood/eval_cli.py` succeeds.
    - `grep -q 'PROPOSED_FIX_TYPES' src/ood/eval_cli.py` succeeds.
    - `grep -q 'proposed_fix_types' src/ood/eval_cli.py` succeeds.
    - `grep -q 'Review konnte nicht erstellt werden' src/ood/eval_cli.py` succeeds.
    - `uv run pytest tests/test_eval_cli.py -q` exits 0.
    - `uv run python -c "from ood.eval_cli import eval_app; print('ok')"` prints `ok`.
  </acceptance_criteria>
  <done>`ood eval review` creates a machine-readable review artifact for failed/errored cases with deferred decisions and actionable evidence.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_eval_cli.py tests/test_eval_baseline.py -q` and `uv run python -c "from ood.eval_cli import eval_app; print('ok')"`.
</verification>

<success_criteria>
- `ood eval baseline --help` and `ood eval review --help` are available under the existing eval namespace.
- Baseline command writes observational snapshots only when the user explicitly invokes it.
- Review command writes failed/errored evidence artifacts with deferred decisions and preserves machine-readable proposed fix recommendations in files and JSON stdout.
- No top-level commands, CI thresholds, trend dashboards, or LLM-as-judge behavior are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/14-baseline-feedback-loop-and-review-gate/14-02-SUMMARY.md`.
</output>
