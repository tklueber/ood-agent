---
phase: 14-baseline-feedback-loop-and-review-gate
plan: 03
type: execute
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - src/ood/eval_cli.py
  - tests/test_eval_cli.py
  - README.md
autonomous: true
requirements: [EVAL-05, EVAL-11]
must_haves:
  truths:
    - "User can record an approved, rejected, or deferred review decision from the CLI without losing proposed fix recommendations."
    - "User can update the current baseline only when a review artifact has an explicit approved decision requesting or approving a baseline update."
    - "User can follow README commands for the local loop: regenerate mock data, reindex, smoke query, evaluate, review, decide, and update baseline if approved."
  artifacts:
    - path: "src/ood/eval_cli.py"
      provides: "`ood eval decide` and `ood eval update-baseline` commands enforcing the review gate"
      contains: ["@eval_app.command(\"decide\")", "@eval_app.command(\"update-baseline\")"]
    - path: "tests/test_eval_cli.py"
      provides: "CLI tests for approved/rejected/deferred decisions, proposed fix preservation, and gated baseline updates"
    - path: "README.md"
      provides: "Documented local baseline/review loop without hard thresholds"
  key_links:
    - from: "ood eval decide"
      to: "review JSON artifact"
      via: "apply_review_decision then write_json_file"
      pattern: "apply_review_decision"
    - from: "ood eval update-baseline"
      to: "can_update_baseline"
      via: "approval predicate before save_observational_baseline"
      pattern: "can_update_baseline"
    - from: "README.md"
      to: "CLI commands"
      via: "documented workflow snippets"
      pattern: "ood eval baseline"
---

<objective>
Finish the Phase 14 feedback loop by adding explicit review decisions, a gated baseline update command, and documentation for the local workflow.

Purpose: EVAL-11 requires an acceptance gate: metric improvement alone cannot update a baseline; a review decision must approve it first. EVAL-05 requires the first baseline to remain observational.
Output: `ood eval decide`, `ood eval update-baseline`, tests, and README workflow docs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-baseline-feedback-loop-and-review-gate/14-CONTEXT.md
@.planning/phases/14-baseline-feedback-loop-and-review-gate/14-RESEARCH.md
@.planning/phases/14-baseline-feedback-loop-and-review-gate/14-01-SUMMARY.md
@.planning/phases/14-baseline-feedback-loop-and-review-gate/14-02-SUMMARY.md
@src/ood/eval_cli.py
@src/ood/eval_baseline.py
@tests/test_eval_cli.py
@README.md

<interfaces>
Plan 01 exports to use:
```python
from ood.eval_baseline import (
    apply_review_decision,
    can_update_baseline,
    load_json_file,
    save_observational_baseline,
    write_json_file,
)
```

Gate rule from locked decisions D-07/D-08:
```python
can_update_baseline(review) is True only for decision == "approved", non-empty reviewer, and baseline_update_status in {"requested", "approved"}.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `ood eval decide` and gated `update-baseline` commands</name>
  <files>src/ood/eval_cli.py, tests/test_eval_cli.py</files>
  <read_first>
    - src/ood/eval_cli.py
    - tests/test_eval_cli.py
    - src/ood/eval_baseline.py
    - .planning/phases/14-baseline-feedback-loop-and-review-gate/14-CONTEXT.md
    - .planning/phases/14-baseline-feedback-loop-and-review-gate/14-RESEARCH.md
  </read_first>
  <behavior>
    - `ood eval decide --review review.json --decision approved --reviewer Timo --rationale ok --baseline-update requested` updates the review file with `decision == "approved"`, `reviewer == "Timo"`, `rationale == "ok"`, and `baseline_update_status == "requested"`.
    - `ood eval decide` accepts only `approved`, `rejected`, or `deferred` for `--decision`; invalid values exit 1 through Typer validation or service validation.
    - `ood eval decide` preserves existing per-case `proposed_fix_type` and `proposed_fix_notes` values, including manually edited values such as `corpus_fix`, `retrieval_fix`, `query_fix`, `dataset_fix`, or `baseline_update`.
    - `ood eval update-baseline --report report.json --review review.json --out baseline.json` writes a baseline only when `can_update_baseline(review)` is true.
    - `ood eval update-baseline` exits 1 and does not write the baseline when review decision is `rejected`, `deferred`, missing reviewer, or `baseline_update_status == "not_requested"`; output contains `Baseline-Update erfordert eine genehmigte Review-Entscheidung.`.
    - Successful `update-baseline` rewrites the review file so top-level `baseline_update_status == "updated"`.
  </behavior>
  <action>Modify `src/ood/eval_cli.py` to import `apply_review_decision`, `can_update_baseline`, `load_json_file`, `save_observational_baseline`, and `write_json_file` from `ood.eval_baseline`. Add options: `ReviewPathOption = Annotated[Path, typer.Option("--review", help="Pfad zur Review-Datei.")]`, `DecisionOption = Annotated[str, typer.Option("--decision", help="Review-Entscheidung: approved, rejected oder deferred.")]`, `ReviewerOption = Annotated[str | None, typer.Option("--reviewer", help="Name oder Kennung der prüfenden Person.")]`, `RationaleOption = Annotated[str | None, typer.Option("--rationale", help="Begründung oder Notiz zur Entscheidung.")]`, `BaselineUpdateStatusOption = Annotated[str, typer.Option("--baseline-update", help="Baseline-Status: not_requested, requested, approved oder rejected.")]`. Add `@eval_app.command("decide") def decide(review: ReviewPathOption, decision: DecisionOption, reviewer: ReviewerOption = None, rationale: RationaleOption = None, baseline_update: BaselineUpdateStatusOption = "not_requested", out: Path | None = typer.Option(None, "--out", help="Zielpfad; Default überschreibt die Review-Datei."), json_output: JsonOption = False, quiet: QuietOption = False) -> None:`. It must load review JSON, call `apply_review_decision`, write to `out or review`, and print either compact JSON `{"review_path", "decision", "reviewer", "baseline_update_status"}` or German `Review-Entscheidung gespeichert: {target}`. Add `@eval_app.command("update-baseline") def update_baseline(report: ReportOption, review: ReviewPathOption, out: BaselineOutOption = None, json_output: JsonOption = False, quiet: QuietOption = False) -> None:`. Resolve baseline path to `data/evaluation/baselines/current.json` when `out is None`. Load review JSON, call `can_update_baseline`; if false, echo exactly `Baseline-Update erfordert eine genehmigte Review-Entscheidung.` to stderr and exit 1 before writing the baseline. If true, call `save_observational_baseline(report, baseline_path)`, set `review_payload["baseline_update_status"] = "updated"`, set every case with `baseline_update_status` in `{"requested", "approved"}` to `"updated"`, write the review back to its original path, and print JSON or German `Baseline aktualisiert: {baseline_path}`. Preserve D-05 eval namespace and import ordering. Do not compare metric values, add hard thresholds, or update baselines for rejected/deferred reviews.</action>
  <action_amendment>Also require tests and implementation to preserve proposed fix metadata throughout decision and baseline-update operations: seed at least one review case with `proposed_fix_type: "corpus_fix"` and `proposed_fix_notes: "Add missing wiki note"`, run `ood eval decide`, then assert those values remain unchanged in the written review. During `update-baseline`, update only the top-level `baseline_update_status` to `"updated"`; do not overwrite or infer approval from any per-case `proposed_fix_type`, including `baseline_update`. This amendment addresses the EVAL-11 checker gap by preserving corpus/query/retrieval/dataset/baseline-update recommendations after they are created by Plan 01/02.</action_amendment>
  <verify>
    <automated>uv run pytest tests/test_eval_cli.py tests/test_eval_baseline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '@eval_app.command("decide")' src/ood/eval_cli.py` succeeds.
    - `grep -q '@eval_app.command("update-baseline")' src/ood/eval_cli.py` succeeds.
    - `grep -q 'Baseline-Update erfordert eine genehmigte Review-Entscheidung.' src/ood/eval_cli.py` succeeds.
    - `grep -q 'can_update_baseline' src/ood/eval_cli.py` succeeds.
    - `grep -q 'proposed_fix_type' tests/test_eval_cli.py` succeeds.
    - `grep -q 'proposed_fix_notes' tests/test_eval_cli.py` succeeds.
    - `uv run pytest tests/test_eval_cli.py tests/test_eval_baseline.py -q` exits 0.
    - `uv run python -c "from ood.eval_cli import eval_app; print('ok')"` prints `ok`.
  </acceptance_criteria>
  <done>The CLI can record review decisions and update the current baseline only after an explicit approved review decision.</done>
</task>

<task type="auto">
  <name>Task 2: Document the local baseline/review workflow</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/phases/14-baseline-feedback-loop-and-review-gate/14-CONTEXT.md
    - .planning/phases/14-baseline-feedback-loop-and-review-gate/14-RESEARCH.md
    - src/ood/eval_cli.py
  </read_first>
  <action>Update the `## Evaluation Dataset and Metrics` section in `README.md` so it no longer says Phase 13 is future-only. Add a subsection named `### Baseline and review gate` containing exactly these command snippets in this order: `uv run ood mock-init --target-dir knowledge/mock/v1 --dataset mock-v1`, `uv run ood reindex --knowledge-dir knowledge/mock/v1 --storage-dir data/mock-storage --json`, `uv run ood query "TraceId Kafka AKHQ Ersatzgeschäft" --knowledge-dir knowledge/mock/v1 --storage-dir data/mock-storage --verbose`, `uv run ood eval run --knowledge-dir knowledge/mock/v1 --storage-dir data/mock-storage --dataset evaluation/datasets/mock-v1.json --out data/evaluation/reports/mock-v1.json`, `uv run ood eval baseline --report data/evaluation/reports/mock-v1.json --out data/evaluation/baselines/current.json`, `uv run ood eval review --report data/evaluation/reports/mock-v1.json`, `uv run ood eval decide --review data/evaluation/reviews/mock-v1-<hash>.review.json --decision approved --reviewer YOUR_NAME --rationale "reviewed" --baseline-update requested`, and `uv run ood eval update-baseline --report data/evaluation/reports/mock-v1.json --review data/evaluation/reviews/mock-v1-<hash>.review.json --out data/evaluation/baselines/current.json`. Add text stating `The first baseline is observational: thresholds stay null and ordinary failed eval cases do not turn into process-level failures.` Add text stating `Review cases include machine-readable proposed_fix_type and proposed_fix_notes fields; allowed proposed_fix_type values are corpus_fix, retrieval_fix, query_fix, dataset_fix, baseline_update, and investigate.` Add text stating `Use proposed_fix_type to record the recommended corpus/query/retrieval/dataset/baseline-update follow-up, but it does not bypass the explicit review decision gate.` Add text stating `Rejected or deferred reviews do not update the baseline; metric improvement alone is not accepted improvement.` Add text stating `Generated reports, reviews, baselines, indexes, and mock runtime artifacts live under data/ or user-supplied paths and should not be committed by default.` Do not document CI regression thresholds, trend dashboards, LLM-as-judge scoring, real production import, or hosted dashboards.</action>
  <verify>
    <automated>uv run pytest tests/test_eval_cli.py tests/test_eval_baseline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '### Baseline and review gate' README.md` succeeds.
    - `grep -q 'uv run ood eval baseline --report' README.md` succeeds.
    - `grep -q 'uv run ood eval review --report' README.md` succeeds.
    - `grep -q 'uv run ood eval decide --review' README.md` succeeds.
    - `grep -q 'uv run ood eval update-baseline --report' README.md` succeeds.
    - `grep -q 'thresholds stay null' README.md` succeeds.
    - `grep -q 'metric improvement alone is not accepted improvement' README.md` succeeds.
    - `grep -q 'proposed_fix_type' README.md` succeeds.
    - `grep -q 'proposed_fix_notes' README.md` succeeds.
    - `grep -q 'corpus_fix, retrieval_fix, query_fix, dataset_fix, baseline_update, and investigate' README.md` succeeds.
  </acceptance_criteria>
  <done>README documents the complete local feedback loop and explicitly preserves the no-threshold, review-gated scope.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_eval_cli.py tests/test_eval_baseline.py -q`, `uv run python -c "from ood.eval_cli import eval_app; print('ok')"`, and `uv run ood eval --help`.
</verification>

<success_criteria>
- Review decisions support `approved`, `rejected`, and `deferred` outcomes.
- Baseline updates are blocked unless the review is approved and baseline update is requested/approved.
- README contains the local mock-data → index → smoke query → eval → baseline → review → decide → update-baseline loop and documents proposed fix fields/allowed values.
- Deferred ideas remain absent: no hard CI thresholds, no historical trend dashboard, no LLM-as-judge scoring.
</success_criteria>

<output>
After completion, create `.planning/phases/14-baseline-feedback-loop-and-review-gate/14-03-SUMMARY.md`.
</output>
