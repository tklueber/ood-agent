---
phase: 12-evaluation-dataset-and-metric-core
plan: 03
type: tdd
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/ood/evaluation_ticket_metrics.py
  - tests/test_evaluation_ticket_metrics.py
  - README.md
autonomous: true
requirements:
  - EVAL-04
must_haves:
  truths:
    - "User can compute deterministic ticket-intelligence metrics for intent, routing, identifiers, command risks, and uncertainty expectations."
    - "Ticket metrics evaluate public `TicketAnalysis` output without invoking Cloud LLMs or backend internals."
    - "README documents the Phase 12 dataset/metric boundary and points users away from putting golden labels into Markdown."
  artifacts:
    - path: "src/ood/evaluation_ticket_metrics.py"
      provides: "Ticket-intelligence metric dataclasses and pure metric functions"
      exports: ["TicketIntelligenceCaseMetrics", "TicketIntelligenceMetricsSummary", "evaluate_ticket_intelligence_case", "summarize_ticket_intelligence_metrics"]
    - path: "tests/test_evaluation_ticket_metrics.py"
      provides: "TDD coverage for ticket-intelligence metric formulas"
      contains: "identifier_recall"
    - path: "README.md"
      provides: "User-facing Phase 12 dataset and metric core guidance"
      contains: "Evaluation Dataset and Metrics"
  key_links:
    - from: "src/ood/evaluation_ticket_metrics.py"
      to: "src/ood/evaluation.py"
      via: "EvaluationCase expected ticket labels"
      pattern: "expected_intent"
    - from: "src/ood/evaluation_ticket_metrics.py"
      to: "src/ood/models.py"
      via: "TicketAnalysis public output"
      pattern: "TicketAnalysis"
---

<objective>
Implement deterministic ticket-intelligence metrics and document the Phase 12 metric core.

Purpose: Retrieval quality is not sufficient if the ticket-intelligence labels are wrong; Phase 12 needs pure metrics for the existing deterministic analysis contract before Phase 13 runs black-box evaluations.
Output: Ticket metric functions, tests, and README guidance.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ticket-intelligence/04-01-SUMMARY.md
@.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-02-SUMMARY.md
@.planning/phases/12-evaluation-dataset-and-metric-core/12-01-SUMMARY.md
@src/ood/evaluation.py
@src/ood/models.py
@README.md

<interfaces>
Contracts expected from Plan 12-01:

```python
@dataclass(frozen=True)
class ExpectedIdentifier:
    kind: str
    value: str

@dataclass(frozen=True)
class ExpectedCommandRisk:
    command_contains: str
    risk: str
```

Existing ticket analysis contract:

```python
@dataclass(frozen=True)
class TicketAnalysis:
    intent: str
    assessment: str | None
    solution_steps: list[str]
    routing: RoutingDecision
    identifiers: list[TicketIdentifier]
    command_risks: list[CommandRisk]
    uncertainties: list[str]
    mode: str
```
</interfaces>

Discovery: Level 0. Metrics are deterministic comparisons over existing dataclasses and stdlib-only tests.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement per-case ticket-intelligence metrics</name>
  <files>src/ood/evaluation_ticket_metrics.py, tests/test_evaluation_ticket_metrics.py</files>
  <behavior>
    - Intent and routing return booleans comparing expected labels with `TicketAnalysis.intent` and `TicketAnalysis.routing.route`.
    - Identifier recall equals matched expected `(kind, value)` pairs divided by total expected pairs, with case-insensitive identifier values.
    - Command-risk accuracy checks each expected `command_contains` substring against actual command risks and expected risk label.
    - Uncertainty match is true when each expected uncertainty substring appears in at least one actual uncertainty.
    - Empty expected identifiers or command risks produce perfect scores for that dimension if no expectations exist.
  </behavior>
  <action>Write failing tests first, then create `src/ood/evaluation_ticket_metrics.py` with frozen dataclass `TicketIntelligenceCaseMetrics` and function `evaluate_ticket_intelligence_case(case: EvaluationCase, analysis: TicketAnalysis) -> TicketIntelligenceCaseMetrics`. Include detailed fields: `intent_match`, `routing_match`, `identifier_recall`, `command_risk_accuracy`, `uncertainty_match`, `missing_identifiers`, `missing_command_risks`, and `missing_uncertainties`. Add `to_dict()` for future JSON reports. Do not parse raw ticket text and do not duplicate `analyze_ticket()` rules; metrics compare expected labels against public analysis output only.</action>
  <verify>
    <automated>uv run pytest tests/test_evaluation_ticket_metrics.py -q</automated>
  </verify>
  <done>Per-case ticket metrics are deterministic and cover intent, route, identifiers, command risks, and uncertainty expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aggregate ticket metrics and document Phase 12 boundary</name>
  <files>src/ood/evaluation_ticket_metrics.py, tests/test_evaluation_ticket_metrics.py, README.md</files>
  <behavior>
    - Summary metrics expose mean intent accuracy, routing accuracy, identifier recall, command-risk accuracy, uncertainty accuracy, and case count.
    - Empty metric lists return zeroed rates and `case_count=0`.
    - README states that Phase 12 provides dataset and pure metric functions; Phase 13 will wire CLI/evaluation runs through `RagEngine.query()`.
  </behavior>
  <action>Add frozen dataclass `TicketIntelligenceMetricsSummary` and `summarize_ticket_intelligence_metrics(metrics: list[TicketIntelligenceCaseMetrics]) -> TicketIntelligenceMetricsSummary`, rounding floats to four decimals. Export public names via `__all__`. Add a concise README section `Evaluation Dataset and Metrics` documenting `evaluation/datasets/mock-v1.json`, the separation from indexed Markdown, metrics covered (Hit@1/3/5, MRR, source recall, forbidden-source rate, intent/routing/identifier/command-risk/uncertainty), and the local deterministic boundary. Do not add a CLI command in this plan; Phase 13 owns CLI reporting.</action>
  <verify>
    <automated>uv run pytest tests/test_evaluation_ticket_metrics.py tests/test_evaluation.py tests/test_evaluation_metrics.py -q</automated>
  </verify>
  <done>Ticket metric summaries are stable, and README gives operators the correct Phase 12 usage boundary without implying a runnable evaluation CLI exists yet.</done>
</task>

</tasks>

<verification>
Run all Phase 12 focused tests after Wave 2: `uv run pytest tests/test_evaluation.py tests/test_evaluation_metrics.py tests/test_evaluation_ticket_metrics.py -q`.
</verification>

<success_criteria>
- EVAL-04 is covered by deterministic ticket-intelligence metric formulas.
- Metrics compare only public `TicketAnalysis` output to dataset expectations.
- Documentation preserves the Phase 12/13 boundary and repeats that golden labels stay outside indexed Markdown.
</success_criteria>

<output>
After completion, create `.planning/phases/12-evaluation-dataset-and-metric-core/12-03-SUMMARY.md`.
</output>
