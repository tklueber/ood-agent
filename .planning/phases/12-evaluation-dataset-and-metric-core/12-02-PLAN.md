---
phase: 12-evaluation-dataset-and-metric-core
plan: 02
type: tdd
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/ood/evaluation_metrics.py
  - tests/test_evaluation_metrics.py
autonomous: true
requirements:
  - EVAL-03
must_haves:
  truths:
    - "User can compute Hit@1, Hit@3, Hit@5, MRR, source recall, and forbidden-source rate from deterministic retrieved source lists."
    - "Metric calculations use public `SourceHit.path` ordering and do not depend on LightRAG internals."
    - "Aggregate retrieval metrics remain stable for empty results, partial matches, duplicate returned sources, and forbidden-source hits."
  artifacts:
    - path: "src/ood/evaluation_metrics.py"
      provides: "Retrieval metric dataclasses and pure metric functions"
      exports: ["RetrievalCaseMetrics", "RetrievalMetricsSummary", "evaluate_retrieval_case", "summarize_retrieval_metrics"]
    - path: "tests/test_evaluation_metrics.py"
      provides: "TDD coverage for retrieval metric formulas and edge cases"
      contains: "Hit@5"
  key_links:
    - from: "src/ood/evaluation_metrics.py"
      to: "src/ood/evaluation.py"
      via: "EvaluationCase expected_sources/forbidden_sources"
      pattern: "EvaluationCase"
    - from: "src/ood/evaluation_metrics.py"
      to: "src/ood/models.py"
      via: "SourceHit.path ranked order"
      pattern: "SourceHit"
---

<objective>
Implement deterministic retrieval-quality metrics for Phase 12.

Purpose: Hybrid retrieval can only be improved safely when expected-source hits, rank quality, recall, and forbidden-source leakage are measurable without invoking a backend.
Output: Pure retrieval metric functions plus focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-01-SUMMARY.md
@.planning/phases/12-evaluation-dataset-and-metric-core/12-01-SUMMARY.md
@src/ood/evaluation.py
@src/ood/models.py

<interfaces>
Contracts expected from Plan 12-01:

```python
@dataclass(frozen=True)
class EvaluationCase:
    id: str
    query: str
    expected_sources: tuple[str, ...]
    forbidden_sources: tuple[str, ...]
    expected_intent: str
    expected_route: str
    expected_identifiers: tuple[ExpectedIdentifier, ...]
    expected_command_risks: tuple[ExpectedCommandRisk, ...]
    expected_uncertainties: tuple[str, ...]
```

Existing query source contract:

```python
@dataclass(frozen=True)
class SourceHit:
    path: str
    score: float
    excerpt: str
    score_details: dict[str, Any] = field(default_factory=dict)
```
</interfaces>

Discovery: Level 0. Metrics are pure Python formulas over existing dataclasses; no new libraries or external services.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement per-case retrieval metrics</name>
  <files>src/ood/evaluation_metrics.py, tests/test_evaluation_metrics.py</files>
  <behavior>
    - For returned source paths `A,B,C` and expected source `B`, `hit_at_1=False`, `hit_at_3=True`, `hit_at_5=True`, `mrr=0.5`, `source_recall=1.0`.
    - For multiple expected sources, recall equals matched expected sources divided by total expected sources, using unique path matches.
    - Forbidden source hits are reported as a boolean and list of offending paths.
    - Empty retrieved sources produce false hits, `mrr=0.0`, `source_recall=0.0`, and no crash.
  </behavior>
  <action>Write tests first, then create `src/ood/evaluation_metrics.py` with frozen dataclass `RetrievalCaseMetrics` and function `evaluate_retrieval_case(case: EvaluationCase, sources: list[SourceHit]) -> RetrievalCaseMetrics`. Normalize compared paths by stripping leading `./` and using POSIX separators, but do not fuzzy-match names. Deduplicate retrieved paths for recall/forbidden checks while preserving first rank for Hit@k/MRR. Include `to_dict()` for future JSON reports. Do not call `RagEngine.query()` in this plan; Phase 13 owns black-box execution.</action>
  <verify>
    <automated>uv run pytest tests/test_evaluation_metrics.py -q</automated>
  </verify>
  <done>Per-case retrieval metrics return deterministic values for exact ranked source paths, empty results, partial recall, and forbidden hits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aggregate retrieval metrics across cases</name>
  <files>src/ood/evaluation_metrics.py, tests/test_evaluation_metrics.py</files>
  <behavior>
    - Summary Hit@k values are the mean of per-case boolean hits over all cases.
    - Summary MRR and source recall are arithmetic means over cases.
    - Forbidden-source rate is the fraction of cases with at least one forbidden hit.
    - Empty metric lists return zeroed rates and `case_count=0` without division errors.
  </behavior>
  <action>Add frozen dataclass `RetrievalMetricsSummary` and `summarize_retrieval_metrics(metrics: list[RetrievalCaseMetrics]) -> RetrievalMetricsSummary`. Round public float values to four decimals for stable JSON/report comparisons. Export retrieval metric public names via `__all__`.</action>
  <verify>
    <automated>uv run pytest tests/test_evaluation_metrics.py -q</automated>
  </verify>
  <done>Aggregated retrieval metrics expose Hit@1/3/5, MRR, source recall, forbidden-source rate, and case count in stable `to_dict()` output.</done>
</task>

</tasks>

<verification>
Run focused retrieval metric tests: `uv run pytest tests/test_evaluation_metrics.py -q`.
</verification>

<success_criteria>
- EVAL-03 is covered by deterministic retrieval metric formulas.
- Metrics consume only evaluation cases and `SourceHit` lists, keeping retrieval backends black-box for Phase 13.
- Edge cases are tested without network, Cloud LLM, or sentence-transformer model downloads.
</success_criteria>

<output>
After completion, create `.planning/phases/12-evaluation-dataset-and-metric-core/12-02-SUMMARY.md`.
</output>
