---
phase: 12-evaluation-dataset-and-metric-core
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ood/evaluation.py
  - tests/test_evaluation.py
  - evaluation/datasets/mock-v1.json
autonomous: true
requirements:
  - EVAL-01
must_haves:
  truths:
    - "User can author a versioned JSON evaluation dataset with expected and forbidden source paths plus expected ticket-intelligence labels."
    - "User receives deterministic validation errors for malformed cases before any evaluation run is attempted."
    - "User can validate expected/forbidden source references against the mock knowledge directory without indexing or Cloud LLM usage."
  artifacts:
    - path: "src/ood/evaluation.py"
      provides: "EvaluationDataset/EvaluationCase contracts and JSON loader/validator"
      exports: ["EvaluationDataset", "EvaluationCase", "EvaluationDatasetError", "load_evaluation_dataset"]
    - path: "tests/test_evaluation.py"
      provides: "TDD coverage for valid dataset loading and deterministic validation failures"
      contains: "load_evaluation_dataset"
    - path: "evaluation/datasets/mock-v1.json"
      provides: "Initial versioned mock evaluation fixture separated from indexed Markdown"
      contains: "expected_sources"
  key_links:
    - from: "evaluation/datasets/mock-v1.json"
      to: "knowledge/mock/v1"
      via: "relative expected_sources/forbidden_sources paths"
      pattern: "expected_sources"
    - from: "src/ood/evaluation.py"
      to: "evaluation/datasets/mock-v1.json"
      via: "load_evaluation_dataset(Path(...), knowledge_dir=...)"
      pattern: "json\.loads"
---

<objective>
Create the Phase 12 evaluation dataset contract and first deterministic mock fixture.

Purpose: Retrieval quality cannot be measured until expected sources, forbidden sources, routing, intent, identifiers, command risks, and uncertainty expectations live in versioned JSON outside indexed Markdown.
Output: `src/ood/evaluation.py`, loader tests, and `evaluation/datasets/mock-v1.json`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mock-corpus-contract-and-safety-foundation/05-03-SUMMARY.md
@.planning/phases/06-mock-data-import-and-index-validation/06-02-SUMMARY.md
@src/ood/mock_corpus.py
@src/ood/models.py

<interfaces>
Existing contracts to preserve:

```python
# src/ood/mock_corpus.py
def generate_mock_corpus(target_dir: Path, *, dataset: str = "mock-v1") -> MockCorpusResult
# Generated paths are relative under the selected mock knowledge directory, e.g. ticket/mock-pol-1001.md.
```

```python
# src/ood/models.py
@dataclass(frozen=True)
class QueryResult:
    query: str
    answer: str | None
    confidence: ConfidenceScore
    sources: list[SourceHit]
    llm_used: bool
    status: str
    analysis: TicketAnalysis
    retrieval_diagnostics: RetrievalDiagnostics = field(default_factory=RetrievalDiagnostics)
```
</interfaces>

Discovery: Level 0. No external dependency is needed; implement with stdlib `json`, frozen dataclasses, `Path`, and existing pytest patterns.

Locked project constraints: evaluation labels must never be written into indexed Markdown; Cloud LLM usage remains default-deny and irrelevant for this dataset loader.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define evaluation dataset contracts and loader</name>
  <files>src/ood/evaluation.py, tests/test_evaluation.py</files>
  <behavior>
    - Loading a valid JSON file returns an `EvaluationDataset` with `schema_version`, `dataset`, and typed `cases`.
    - A case must include: `id`, `query`, `expected_sources`, `forbidden_sources`, `expected_intent`, `expected_route`, `expected_identifiers`, `expected_command_risks`, and `expected_uncertainties`.
    - Duplicate case IDs, empty queries, empty expected source lists, unknown top-level schema versions, and malformed JSON raise `EvaluationDatasetError` with actionable messages.
    - When `knowledge_dir` is provided, every expected and forbidden source path must exist below that directory; missing references raise `EvaluationDatasetError` listing the missing path.
  </behavior>
  <action>Write failing tests first in `tests/test_evaluation.py`, then create `src/ood/evaluation.py` with frozen dataclasses `ExpectedIdentifier`, `ExpectedCommandRisk`, `EvaluationCase`, `EvaluationDataset`, and exception `EvaluationDatasetError`. Implement `load_evaluation_dataset(path: Path, *, knowledge_dir: Path | None = None) -> EvaluationDataset` using only stdlib JSON parsing. Normalize all source paths to POSIX-style relative strings; reject absolute source paths and `..` traversal because evaluation fixtures must be portable and must not point outside the mock corpus. Export the public names via `__all__`. Do not call `RagEngine`, do not index anything, and do not add dependencies.</action>
  <verify>
    <automated>uv run pytest tests/test_evaluation.py -q</automated>
  </verify>
  <done>Valid datasets load into typed dataclasses; malformed schema and missing mock source references fail deterministically with `EvaluationDatasetError`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the initial mock-v1 evaluation fixture</name>
  <files>evaluation/datasets/mock-v1.json, tests/test_evaluation.py</files>
  <behavior>
    - The committed fixture has `schema_version: 1`, `dataset: mock-v1`, and at least four cases covering Police, Offerten, DMS self-solve, and Rückfrage scenarios.
    - Fixture cases reference only generated mock corpus paths from Phase 5, never golden answers embedded in Markdown.
    - Fixture validation passes when tests generate the Phase 5 mock corpus into a temporary `knowledge_dir` and load `evaluation/datasets/mock-v1.json` against it.
  </behavior>
  <action>Create `evaluation/datasets/mock-v1.json` with versioned metadata and deterministic case objects. Use relative source paths such as `ticket/mock-pol-1001.md`, `wiki/mock-wiki-5001.md`, `runbook/mock-run-6001.md`, and `servicenow_case/mock-snow-3001.md`. Include expected intent/route/identifiers/command-risk/uncertainty labels, but no full answer text and no secrets. Extend `tests/test_evaluation.py` to generate the mock corpus with `generate_mock_corpus(tmp_path / "knowledge", dataset="mock-v1")` and prove the fixture loads successfully with source-reference validation.</action>
  <verify>
    <automated>uv run pytest tests/test_evaluation.py -q</automated>
  </verify>
  <done>`evaluation/datasets/mock-v1.json` is a portable, validated fixture ready for metric computation in later plans.</done>
</task>

</tasks>

<verification>
Run the focused dataset suite: `uv run pytest tests/test_evaluation.py -q`.
</verification>

<success_criteria>
- EVAL-01 is covered by typed dataset contracts, deterministic loader validation, and a committed `mock-v1` fixture.
- Evaluation labels remain outside indexed Markdown.
- No Cloud LLM or external service is involved.
</success_criteria>

<output>
After completion, create `.planning/phases/12-evaluation-dataset-and-metric-core/12-01-SUMMARY.md`.
</output>
