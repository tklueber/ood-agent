---
phase: 11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/ood/rag.py
  - src/ood/cli.py
  - tests/test_cli.py
  - README.md
autonomous: true
requirements:
  - GRAPH-01
  - CLI-01
must_haves:
  truths:
    - "User can see whether graph retrieval is active or deliberately deferred for CLI queries."
    - "Human `ood query --verbose` output shows backend diagnostics for hybrid retrieval, extractive synthesis, LLM usage, and graph status."
    - "JSON `ood query --json` output remains skill-usable with answer, sources, confidence, routing, uncertainties, and diagnostics."
  artifacts:
    - path: "src/ood/rag.py"
      provides: "Graph retrieval decision metadata on query diagnostics"
      contains: "graph_retrieval"
    - path: "src/ood/cli.py"
      provides: "Verbose human diagnostics for hybrid/extractive/graph status"
      contains: "Diagnostics:"
    - path: "tests/test_cli.py"
      provides: "CLI JSON and human-output regression coverage"
      contains: "graph"
    - path: "README.md"
      provides: "Documented graph retrieval defer decision and activation criteria"
      contains: "Graph retrieval"
  key_links:
    - from: "src/ood/cli.py"
      to: "QueryResult.retrieval_diagnostics"
      via: "verbose output renders non-secret diagnostics from the public result contract"
      pattern: "retrieval_diagnostics"
    - from: "README.md"
      to: "GRAPH-01"
      via: "explicit defer rationale, risks, and activation criteria"
      pattern: "Graph retrieval"
---

<objective>
Make graph retrieval status explicit and polish CLI/docs so one query result is usable by operators and AI skills.

Purpose: Phase 11 requires graph retrieval to be either active or consciously deferred. Given the current local fallback path stores vectors and content but no graph artifact, this plan documents a deliberate defer while exposing the decision in query diagnostics.
Output: Verbose CLI diagnostics and README guidance for hybrid retrieval, local extractive answers, and graph retrieval status.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-CONTEXT.md
@.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-01-SUMMARY.md
@.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-02-SUMMARY.md
@src/ood/rag.py
@src/ood/cli.py
@tests/test_cli.py
@README.md

<interfaces>
Existing CLI output contract from `src/ood/cli.py`:
```python
def _emit_query_result(*, result: QueryResult, cloud_llm_allowed: bool, json_output: bool, verbose: bool, quiet: bool) -> None:
    if json_output:
        typer.echo(json.dumps(result.to_dict()))
        return
    ...
    if verbose:
        typer.echo(f"Diagnostics: cloud_llm_allowed={cloud_llm_allowed} llm_used={result.llm_used} status={result.status} source_count={len(result.sources)} analysis_mode={result.analysis.mode}")
```

Keep JSON output as `QueryResult.to_dict()`; add diagnostics to the model/result, not CLI-only JSON fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose graph defer status and hybrid diagnostics in CLI output</name>
  <files>src/ood/rag.py, src/ood/cli.py, tests/test_cli.py</files>
  <behavior>
    - `ood query --json` includes `retrieval_diagnostics.graph_retrieval.status` set to `deferred` (or equivalent) with reason and activation criteria.
    - `ood query --verbose` human output includes non-secret diagnostics for retrieval strategy, graph status, local extractive synthesis mode, llm_used, and source count.
    - JSON output remains exactly `QueryResult.to_dict()` and does not add CLI-only fields such as `cloud_llm_allowed`.
  </behavior>
  <action>Set graph retrieval diagnostics in `src/ood/rag.py` for both local fallback and LightRAG-backed query paths. Because current local artifacts do not persist graph entities/relationships and v1.1 needs stable local evaluation first, mark graph retrieval as deliberately deferred with: reason, risk, replacement behavior (`hybrid semantic+lexical retrieval`), and activation criteria (local graph artifact present, deterministic tests proving graph lift, privacy-safe query path). Update `_emit_query_result` verbose diagnostics in `src/ood/cli.py` to render concise retrieval strategy and graph status from `result.retrieval_diagnostics`; do not include secrets and do not change quiet behavior.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q</automated>
  </verify>
  <done>Human and JSON query outputs expose graph retrieval status plus hybrid/extractive diagnostics without breaking stable CLI contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 11 local query workflow and graph retrieval decision</name>
  <files>README.md, tests/test_cli.py</files>
  <action>Update README query documentation to describe: hybrid semantic+lexical local retrieval, exact identifier/error-code lift, local extractive answer synthesis with source citations, JSON diagnostics for skill use, and the explicit graph retrieval defer decision with rationale/risks/activation criteria. Add or update a lightweight documentation assertion in `tests/test_cli.py` or an existing docs-related test location only if this repository already asserts README content there; otherwise keep verification to the targeted pytest command and README review. Do not document Cloud LLM synthesis as default; preserve `OOD_ALLOW_CLOUD_LLM=false` guidance.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py tests/test_rag.py -q</automated>
  </verify>
  <done>README gives operators the one-command Phase 11 workflow and makes GRAPH-01 visibly satisfied by a documented defer decision.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_cli.py tests/test_rag.py -q` and manually inspect README diff to ensure no secrets, production identifiers, or Cloud-default language were added.
</verification>

<success_criteria>
- `ood query --json` is skill-usable: answer, ranked sources, confidence, routing, uncertainties, and retrieval diagnostics are present.
- `ood query --verbose` lets an operator audit hybrid scoring, local extractive mode, Cloud LLM usage, and graph retrieval status.
- GRAPH-01 is satisfied by an explicit documented defer decision with rationale, risk, replacement behavior, and activation criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-03-SUMMARY.md`
</output>
