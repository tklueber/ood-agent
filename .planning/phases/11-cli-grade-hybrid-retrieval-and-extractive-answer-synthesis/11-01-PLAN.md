---
phase: 11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ood/models.py
  - src/ood/rag.py
  - tests/test_rag.py
autonomous: true
requirements:
  - RAG-06
  - CLI-01
must_haves:
  truths:
    - "User can query locally and see sources ranked by combined semantic and lexical signals."
    - "Exact identifiers, error codes, system/component names, and ticket terms can lift relevant sources above vector-only matches."
    - "JSON query output exposes retrieval diagnostics without requiring Cloud LLM approval."
  artifacts:
    - path: "src/ood/models.py"
      provides: "Public retrieval diagnostics contract on QueryResult/SourceHit serialization"
      contains: "retrieval_diagnostics"
    - path: "src/ood/rag.py"
      provides: "Hybrid local fallback scoring from semantic cosine and lexical/BM25-style signals"
      contains: "_query_local_fallback"
    - path: "tests/test_rag.py"
      provides: "Regression coverage for hybrid rank lift and diagnostics"
      contains: "hybrid"
  key_links:
    - from: "src/ood/rag.py"
      to: "src/ood/models.py"
      via: "QueryResult includes retrieval diagnostics from local fallback scoring"
      pattern: "retrieval_diagnostics"
    - from: "src/ood/rag.py"
      to: "data/storage/ood-local-vector-index.json"
      via: "reads vector index document content and vector fields"
      pattern: "LOCAL_VECTOR_INDEX_FILENAME"
---

<objective>
Create the public diagnostics contract and implement local hybrid retrieval scoring for `ood query`.

Purpose: Phase 10 made local vector retrieval real, but vector-only ranking can miss exact operational tokens. This plan makes local no-Cloud retrieval useful for IDs, error codes, system names, and ticket terms while preserving the existing source contract.
Output: Hybrid-ranked `QueryResult` objects with JSON-serializable retrieval diagnostics.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-CONTEXT.md
@.planning/phases/10-echtes-lokales-embedding-retrieval-ohne-cloud-llm-aktivieren-und-llm-antwortsynthese-optional-privacy-gated-machen/10-01-SUMMARY.md
@.planning/phases/10-echtes-lokales-embedding-retrieval-ohne-cloud-llm-aktivieren-und-llm-antwortsynthese-optional-privacy-gated-machen/10-02-SUMMARY.md
@src/ood/models.py
@src/ood/rag.py
@tests/test_rag.py

<interfaces>
Existing contracts to preserve and extend:

From `src/ood/models.py`:
```python
@dataclass(frozen=True)
class SourceHit:
    path: str
    score: float
    excerpt: str

@dataclass(frozen=True)
class QueryResult:
    query: str
    answer: str | None
    confidence: ConfidenceScore
    sources: list[SourceHit]
    llm_used: bool
    status: str
    analysis: TicketAnalysis
```

From `src/ood/rag.py`:
```python
LOCAL_VECTOR_INDEX_FILENAME = "ood-local-vector-index.json"
QUERY_SOURCE_LIMIT = 5

def _query_local_fallback(self, query_text: str) -> list[SourceHit]: ...
def _tokenize(text: str) -> set[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add retrieval diagnostics contracts to QueryResult</name>
  <files>src/ood/models.py, tests/test_rag.py</files>
  <behavior>
    - QueryResult.to_dict() still contains existing top-level fields: query, answer, confidence, sources, llm_used, status, analysis.
    - QueryResult.to_dict() additionally contains `retrieval_diagnostics` with backend, strategy, score_components, graph_retrieval, and notes fields.
    - SourceHit.to_dict() remains backward-compatible by keeping path, score, and excerpt; any optional score details must be additive only.
  </behavior>
  <action>Create frozen dataclasses in `src/ood/models.py` for retrieval diagnostics, e.g. `RetrievalDiagnostics` and `ScoreComponent`/`SourceScoreBreakdown`, and add a required `retrieval_diagnostics` field to `QueryResult`. Keep every existing serialized field unchanged and add diagnostics as an additive JSON field so CLI automation from earlier phases still sees the old keys. Update every existing test fixture in `tests/test_rag.py` that instantiates `QueryResult` only if needed by production constructor changes; prefer defaults/factory helpers in models if that keeps older call sites simple.</action>
  <verify>
    <automated>uv run pytest tests/test_models.py tests/test_rag.py -q</automated>
  </verify>
  <done>QueryResult serializes stable retrieval diagnostics while existing SourceHit and analysis JSON fields remain present and unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace vector-only local fallback ranking with hybrid scoring</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - Given two local vector-index documents where one has weaker cosine similarity but contains an exact identifier/error-code/system token from the query, the exact-match document ranks first.
    - Hybrid scores are clamped to 0.0-1.0 and sorted descending, still limited to QUERY_SOURCE_LIMIT.
    - Retrieval diagnostics report semantic_score, lexical_score, combined_score, lexical_matches, and weights for each returned source.
    - No Cloud LLM path is invoked; tests monkeypatch `_encode_local_embeddings` as in Phase 10 to avoid model downloads.
  </behavior>
  <action>Update `_query_local_fallback` in `src/ood/rag.py` to compute semantic cosine plus lexical/BM25-style scores from `query_text` and each indexed document's `content`. Use deterministic local logic only: tokenization should preserve short operational tokens and IDs/codes (e.g. `P-12345`, `ERR-502`, system/component names) instead of dropping everything shorter than 3 characters. Combine scores with explicit weights (semantic primary but lexical strong enough to lift exact identifiers; e.g. 0.65 semantic / 0.35 lexical, with exact-code boosts folded into lexical). Return the same top SourceHit list plus diagnostics from Task 1. Do not add external dependencies.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py -q</automated>
  </verify>
  <done>Local fallback queries rank by hybrid semantic+lexical signals, exact operational tokens can lift relevant sources, and diagnostics expose score components.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_models.py tests/test_rag.py -q` and confirm all tests pass with no model download/progress output in assertions.
</verification>

<success_criteria>
- `RagEngine(settings).query(...)` in local fallback mode returns sources ranked by hybrid semantic and lexical signals.
- A document matching exact IDs/codes can outrank a vector-only semantic match when the query contains that exact operational token.
- Query JSON includes additive retrieval diagnostics suitable for later evaluation and AI-skill use.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-01-SUMMARY.md`
</output>
