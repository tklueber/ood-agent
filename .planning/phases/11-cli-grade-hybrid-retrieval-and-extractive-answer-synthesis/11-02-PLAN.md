---
phase: 11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/ood/rag.py
  - tests/test_rag.py
autonomous: true
requirements:
  - ANS-01
  - CLI-01
must_haves:
  truths:
    - "User receives a local answer in default no-Cloud query mode when useful sources are retrieved."
    - "The answer is extractive: it is assembled from top source snippets and cites source ranks or paths."
    - "Ticket intelligence keeps routing, uncertainties, identifiers, and command-risk output alongside the local answer."
  artifacts:
    - path: "src/ood/rag.py"
      provides: "Local extractive answer synthesis from top ranked SourceHit evidence"
      contains: "extractive"
    - path: "tests/test_rag.py"
      provides: "Regression coverage for no-Cloud cited answers and analysis enrichment"
      contains: "extractive"
  key_links:
    - from: "src/ood/rag.py"
      to: "QueryResult.answer"
      via: "local synthesis fills answer when sources exist and llm_used remains False"
      pattern: "answer=.*extract"
    - from: "src/ood/rag.py"
      to: "analyze_ticket"
      via: "analysis is enriched with the local answer without changing deterministic trusted labels"
      pattern: "analyze_ticket"
---

<objective>
Build local extractive answer synthesis for the hybrid-ranked sources from Plan 11-01.

Purpose: Operators and AI skills need an actionable answer, not just ranked excerpts. This plan produces a cited no-Cloud answer by extracting evidence from retrieved sources while keeping trusted ticket intelligence deterministic.
Output: `QueryResult.answer` and analysis assessment/solution steps populated locally when evidence exists, with `llm_used=False`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-CONTEXT.md
@.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-01-SUMMARY.md
@src/ood/rag.py
@tests/test_rag.py

<interfaces>
Plan 11-01 provides hybrid-ranked `SourceHit` results and `QueryResult.retrieval_diagnostics`.

Existing analysis enrichment pattern from `src/ood/rag.py`:
```python
analysis = analyze_ticket(query_text, sources, confidence)
if llm_used and answer is not None:
    analysis = self._enrich_analysis_with_llm_answer(analysis, answer)
```

Implement a local equivalent that never changes trusted deterministic labels (intent, routing, identifiers, command risks) and only fills assessment, solution_steps, and mode.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Synthesize cited extractive answers from top source excerpts</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - Local fallback query with two relevant sources returns `answer` as a non-empty string and `llm_used is False`.
    - The answer contains citations like `[1]` / `[2]` or source paths that correspond to returned source order.
    - The answer is assembled only from query terms and source excerpts; it must not call `openai_complete_if_cache` or any Cloud LLM function.
    - If no sources are retrieved, answer remains `None` and status remains `no_results`.
  </behavior>
  <action>Add a private helper such as `_synthesize_extractive_answer(query_text, sources)` in `src/ood/rag.py`. Select 1-3 concise evidence sentences from the highest-ranked source excerpts by query-token overlap and source rank, preserve original wording, and render an answer with source citations (for example: `Gefundene Hinweise: [1] ... [2] ...`). Keep it deterministic, local, and conservative: no invented fix steps, no Cloud LLM, no answer when there is no evidence.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py -q</automated>
  </verify>
  <done>Default local queries with sources return a cited extractive answer while no-result queries keep answer as None.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enrich deterministic analysis with the local extractive answer</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - When a local extractive answer exists, `analysis.assessment` equals or summarizes the cited local answer and `analysis.mode` is `local_extractive`.
    - `analysis.solution_steps` contains only extracted/cited operational steps when clear imperative or numbered evidence exists; otherwise it stays empty.
    - Existing deterministic labels for intent, routing, identifiers, command risks, and uncertainties remain produced by `analyze_ticket`.
    - Cloud LLM-approved paths still use existing LLM enrichment behavior and are not regressed.
  </behavior>
  <action>Create a local analysis enrichment helper parallel to `_enrich_analysis_with_llm_answer`. Apply it only when `llm_used=False` and the extractive answer is present. Preserve all trusted deterministic fields from `analyze_ticket`; update only `assessment`, conservative `solution_steps`, and `mode="local_extractive"`. Do not make Cloud LLM approval influence local extractive synthesis.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py -q</automated>
  </verify>
  <done>QueryResult carries a local answer and analysis enrichment that remains privacy-safe and deterministic.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_rag.py -q` and inspect the new tests to confirm they fail if answer synthesis invents text without source citations.
</verification>

<success_criteria>
- No-Cloud local fallback queries with evidence return `answer != None`, `llm_used == False`, cited source evidence, and skill-usable analysis fields.
- Queries with no sources still return `status="no_results"` and `answer is None`.
- Cloud LLM privacy gate behavior from Phase 10 remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-02-SUMMARY.md`
</output>
