---
phase: 04-ticket-intelligence
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/ood/cli.py
  - tests/test_cli.py
  - README.md
autonomous: true
requirements: [TIC-01, TIC-02, TIC-03, TIC-04, TIC-05]
must_haves:
  truths:
    - "Human `ood query` output shows operational triage sections."
    - "Human output shows detected identifiers and command-risk warnings without executing commands."
    - "`ood query --json` emits existing query/source fields plus nested analysis."
    - "README documents the Phase 4 ticket-intelligence CLI contract and privacy behavior."
  artifacts:
    - path: "src/ood/cli.py"
      provides: "Human rendering for structured ticket analysis"
      contains: "Einschätzung"
    - path: "tests/test_cli.py"
      provides: "CLI contract tests for human and JSON Phase 4 output"
      contains: "command_risks"
    - path: "README.md"
      provides: "User-facing ticket-intelligence usage docs"
      contains: "## Ticket Intelligence"
  key_links:
    - from: "src/ood/cli.py"
      to: "QueryResult.analysis"
      via: "_emit_query_result renders sections from analysis"
      pattern: "result\.analysis"
    - from: "tests/test_cli.py"
      to: "src/ood/cli.py"
      via: "CliRunner verifies human and JSON query output"
      pattern: "runner\.invoke\(app, \[\"query\""
---

<objective>
Expose Phase 4 ticket intelligence through the existing CLI and documentation.

Purpose: The user should experience `ood query <ticket_text>` as an operational triage assistant, not only as retrieval output. This plan renders the structured answer contract from D-01/D-16 while preserving JSON automation and CLI-first MVP constraints.
Output: Updated `_emit_query_result()`, CLI tests, and README Ticket Intelligence section.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ticket-intelligence/04-CONTEXT.md
@.planning/phases/04-ticket-intelligence/04-02-SUMMARY.md
@src/ood/cli.py
@src/ood/models.py
@tests/test_cli.py
@README.md

<interfaces>
Plan 02 provides:

```python
result.analysis.intent
result.analysis.assessment
result.analysis.solution_steps
result.analysis.routing.route
result.analysis.routing.rationale
result.analysis.identifiers
result.analysis.command_risks
result.analysis.uncertainties
result.analysis.mode
```

Existing CLI renderer shape:

```python
def _emit_query_result(*, result: QueryResult, json_output: bool, verbose: bool, quiet: bool) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render structured ticket analysis in CLI output</name>
  <files>src/ood/cli.py, tests/test_cli.py</files>
  <read_first>src/ood/cli.py; src/ood/models.py; tests/test_cli.py; .planning/phases/04-ticket-intelligence/04-CONTEXT.md</read_first>
  <behavior>
    - Human output contains section headers exactly `Einschätzung:`, `Lösungsweg:`, `Routing:`, `Quellen:`, `Unsicherheiten:`, `Erkannte IDs:`, and `Command Risks:` per D-01.
    - `Lösungsweg` renders concise numbered steps `1. ...`, `2. ...`; if none exist, render `Keine belastbaren Lösungsschritte verfügbar.` per D-03/D-04.
    - Routing line includes route and rationale, e.g. `Routing: weiterleiten Policen — Police context` per D-10.
    - Command risks render command, color, origin, and rationale, e.g. `rot | rm -rf /tmp/cache | ticket | Destructive delete`; nothing is executed per D-16.
    - JSON output remains `json.dumps(result.to_dict())`, so nested `analysis` is present and prior top-level fields remain.
  </behavior>
  <action>Update `_emit_query_result()` in `src/ood/cli.py`. Keep `--json` branch as `typer.echo(json.dumps(result.to_dict()))` and keep `--quiet` returning no output. Replace the old human `Answer:` block with operational sections: `Einschätzung:` should print `result.analysis.assessment` when present, else `Retrieval-only mode; Bewertung basiert auf deterministischer Analyse.` when `result.llm_used` is false, else `Keine Einschätzung verfügbar.`. `Lösungsweg:` should enumerate `result.analysis.solution_steps`; if empty print `Keine belastbaren Lösungsschritte verfügbar.`. `Routing:` should print `{route} — {rationale}`. `Quellen:` should keep ranked source rendering with path, score, and excerpt. `Unsicherheiten:` should print each uncertainty or `Keine expliziten Unsicherheiten.`. `Erkannte IDs:` should print `{kind}: {value} (confidence={confidence}) — {evidence}` or `Keine.`. `Command Risks:` should print `{risk} | {command} | {origin} | {rationale}` or `Keine erkannt.`. Keep verbose diagnostics and add `analysis_mode={result.analysis.mode}` to the existing diagnostics string per D-08.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py::test_query_human_output_includes_ticket_analysis_sections tests/test_cli.py::test_query_json_output_matches_phase_4_contract -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/cli.py` contains `Einschätzung:`.
    - `src/ood/cli.py` contains `Lösungsweg:`.
    - `src/ood/cli.py` contains `Command Risks:`.
    - `src/ood/cli.py` contains `analysis_mode=`.
    - `tests/test_cli.py` contains `test_query_human_output_includes_ticket_analysis_sections`.
    - `tests/test_cli.py` contains `test_query_json_output_matches_phase_4_contract`.
    - Focused CLI tests exit 0.
  </acceptance_criteria>
  <done>`ood query` human output presents the Phase 4 structured analysis contract while JSON output exposes nested `analysis`.</done>
</task>

<task type="auto">
  <name>Task 2: Document Ticket Intelligence usage and verification</name>
  <files>README.md, tests/test_cli.py</files>
  <read_first>README.md; tests/test_cli.py; .planning/phases/04-ticket-intelligence/04-CONTEXT.md</read_first>
  <action>Add README section `## Ticket Intelligence` after Core RAG/Knowledge Management usage. Document exact commands `uv run ood query "Police P-12345 Fehler beim Login"` and `uv run ood query "Police P-12345 Fehler beim Login" --json`. State that human output includes `Einschätzung`, `Lösungsweg`, `Routing`, `Quellen`, `Unsicherheiten`, detected identifiers, and command risks. State privacy behavior: without `OOD_LLM_API_KEY`, analysis remains deterministic/retrieval-only and no ticket content is sent to a Cloud LLM; with credentials, generated wording may be used for assessment/solution steps while deterministic code owns intent, routing, identifiers, and risk labels. State safety behavior: OOD Agent classifies commands by `grün`, `gelb`, `orange`, `rot` but never executes commands. Add/update CLI test assertions only if needed to keep README-independent tests passing; do not introduce docs-only tests.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py tests/test_rag.py tests/test_ticket_intelligence.py tests/test_models.py -q</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## Ticket Intelligence`.
    - `README.md` contains `uv run ood query "Police P-12345 Fehler beim Login"`.
    - `README.md` contains `grün`, `gelb`, `orange`, and `rot`.
    - `README.md` contains `never executes commands` or `führt keine Befehle aus`.
    - `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>README explains the Phase 4 CLI contract, JSON usage, privacy boundaries, and command-risk safety behavior.</done>
</task>

</tasks>

<verification>
Run `uv run pytest -q` and perform a CLI smoke query against a temporary indexed Markdown fixture if tests create one. Confirm output contains `Einschätzung:`, `Routing:`, `Quellen:`, `Unsicherheiten:`, and `Command Risks:`.
</verification>

<success_criteria>
- Human query output includes all D-01 triage sections.
- JSON query output preserves Phase 2 top-level fields and includes nested `analysis` per D-02.
- Verbose query output discloses `analysis_mode` and `llm_used` per D-08.
- README documents deterministic/no-credential behavior, LLM boundaries, and no command execution.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ticket-intelligence/04-03-SUMMARY.md`.
</output>
