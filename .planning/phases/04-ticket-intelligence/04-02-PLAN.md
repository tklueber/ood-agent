---
phase: 04-ticket-intelligence
plan: 02
type: tdd
wave: 2
depends_on: [04-01]
files_modified:
  - src/ood/models.py
  - src/ood/rag.py
  - tests/test_rag.py
autonomous: true
requirements: [TIC-01, TIC-02, TIC-03, TIC-04, TIC-05]
must_haves:
  truths:
    - "Every `RagEngine.query()` result contains a nested analysis object in JSON serialization."
    - "Retrieval-only/no-credential queries still return deterministic analysis without Cloud LLM calls."
    - "Low-confidence or no-result queries route to Rückfrage and list uncertainties."
    - "LLM-backed answers may populate assessment/solution wording but cannot override deterministic intent, routing, identifier, or risk labels."
  artifacts:
    - path: "src/ood/models.py"
      provides: "QueryResult.analysis serialization contract"
      contains: "analysis: TicketAnalysis"
    - path: "src/ood/rag.py"
      provides: "RagEngine query integration with ticket analysis"
      contains: "analyze_ticket"
    - path: "tests/test_rag.py"
      provides: "Service-level tests for analysis in query results"
      contains: "analysis"
  key_links:
    - from: "src/ood/rag.py"
      to: "src/ood/ticket_intelligence.py"
      via: "calls analyze_ticket(query_text, sources, confidence) after retrieval"
      pattern: "analyze_ticket\(query_text, sources, confidence"
    - from: "src/ood/models.py"
      to: "CLI JSON output"
      via: "QueryResult.to_dict() includes analysis.to_dict()"
      pattern: '"analysis": self.analysis.to_dict\(\)'
---

<objective>
Wire deterministic ticket analysis into `RagEngine.query()` and the stable `QueryResult` JSON contract.

Purpose: Users and automation should receive structured ticket analysis with routing, sources, uncertainties, identifiers, and risks from the existing `ood query` path. This plan preserves the Phase 2 source/answer/confidence fields while adding the Phase 4 nested analysis contract per D-02.
Output: `QueryResult.analysis` plus service-level behavior in local fallback, LightRAG retrieval-only, and LLM-backed query paths.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ticket-intelligence/04-CONTEXT.md
@.planning/phases/04-ticket-intelligence/04-01-SUMMARY.md
@.planning/phases/02-core-rag-engine/02-03-SUMMARY.md
@src/ood/models.py
@src/ood/rag.py
@src/ood/ticket_intelligence.py
@tests/test_rag.py

<interfaces>
Use the Plan 01 contract:

```python
def analyze_ticket(ticket_text: str, sources: list[SourceHit], confidence: ConfidenceScore) -> TicketAnalysis: ...

@dataclass(frozen=True)
class QueryResult:
    query: str
    answer: str | None
    confidence: ConfidenceScore
    sources: list[SourceHit]
    llm_used: bool
    status: str
    analysis: TicketAnalysis  # add in this plan
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add analysis to QueryResult contract</name>
  <files>src/ood/models.py, tests/test_rag.py</files>
  <read_first>src/ood/models.py; src/ood/ticket_intelligence.py; tests/test_rag.py; .planning/phases/04-ticket-intelligence/04-CONTEXT.md</read_first>
  <behavior>
    - `QueryResult.to_dict()` preserves existing top-level keys `query`, `answer`, `confidence`, `sources`, `llm_used`, and `status`.
    - `QueryResult.to_dict()` adds top-level key `analysis` whose value contains `intent`, `solution_steps`, `routing`, `identifiers`, `command_risks`, `uncertainties`, and `mode` per D-02.
    - Existing tests constructing `QueryResult` are updated to pass a `TicketAnalysis` instance instead of removing assertions for existing fields.
  </behavior>
  <action>Modify `src/ood/models.py` so `QueryResult` imports/uses the existing `TicketAnalysis` class from the same module and adds required field `analysis: TicketAnalysis`. Update `QueryResult.to_dict()` to include exact entry `"analysis": self.analysis.to_dict()` while leaving all existing keys and values unchanged. Update affected tests in `tests/test_rag.py` by using real analysis values returned from `RagEngine.query()` where possible; do not add a nullable/default fake analysis because D-02 requires every JSON query result to include nested analysis.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/models.py` contains `analysis: TicketAnalysis`.
    - `src/ood/models.py` contains `"analysis": self.analysis.to_dict()`.
    - `tests/test_rag.py` contains `result.analysis`.
    - `uv run pytest tests/test_rag.py -q` exits 0 after Task 2 integration is complete.
  </acceptance_criteria>
  <done>`QueryResult` has a mandatory nested analysis contract and serialization includes it without removing Phase 2 fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute analysis in every query path</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <read_first>src/ood/rag.py; src/ood/ticket_intelligence.py; src/ood/models.py; tests/test_rag.py; .planning/phases/04-ticket-intelligence/04-CONTEXT.md</read_first>
  <behavior>
    - Local fallback path calls `analyze_ticket(query_text, sources, confidence)` before returning `QueryResult`.
    - LightRAG retrieval-only path calls `analyze_ticket(query_text, sources, confidence)` before returning `QueryResult` and does not call Cloud LLM without credentials per D-05.
    - LLM-backed path sets `llm_used=True` only when generated answer exists, sets analysis `mode="llm_grounded"`, may populate `assessment` from the generated answer, and may populate `solution_steps` by splitting short numbered/bulleted LLM lines; deterministic `intent`, `routing`, `identifiers`, and `command_risks` remain from `analyze_ticket()` per D-06/D-07.
    - Low-confidence/no-source result keeps structured analysis, route `Rückfrage`, uncertainties, and no confident solution steps per D-04.
  </behavior>
  <action>Import `analyze_ticket` and `TicketAnalysis` support into `src/ood/rag.py`. In `_aquery()`, compute `confidence = self._score_confidence(sources, llm_used)` exactly once per return path, then call `analysis = analyze_ticket(query_text, sources, confidence)`, then construct `QueryResult(..., confidence=confidence, analysis=analysis)`. For the LLM-backed branch, after `answer` is set and `llm_used=True`, create an enriched `TicketAnalysis` by copying deterministic fields (`intent`, `routing`, `identifiers`, `command_risks`, `uncertainties`) and setting `mode="llm_grounded"`, `assessment=answer`, and `solution_steps` to at most five concise lines parsed from `answer` when lines start with digits, `-`, or `*`; otherwise use `[]`. Do not send additional prompts or source content beyond the existing LightRAG query path in this plan. Add tests for JSON-like `result.analysis.to_dict()`, low-confidence `Rückfrage`, identifier extraction through query, source-origin command risks, and LLM mode enrichment without overriding deterministic labels.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_query_without_llm_returns_ranked_sources_and_null_answer tests/test_rag.py::test_query_without_llm_credentials_makes_no_cloud_llm_calls tests/test_rag.py::test_llm_credentials_select_mix_query_mode -q</automated>
    <automated>uv run pytest tests/test_rag.py tests/test_ticket_intelligence.py tests/test_models.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/rag.py` contains `from ood.ticket_intelligence import analyze_ticket`.
    - `src/ood/rag.py` contains `analyze_ticket(query_text, sources, confidence)`.
    - `src/ood/rag.py` contains `mode="llm_grounded"`.
    - `tests/test_rag.py` contains `Rückfrage`.
    - `tests/test_rag.py` contains `llm_grounded`.
    - `uv run pytest tests/test_rag.py tests/test_ticket_intelligence.py tests/test_models.py -q` exits 0.
  </acceptance_criteria>
  <done>All `RagEngine.query()` return paths produce a nested `TicketAnalysis` while preserving privacy and deterministic ownership of trusted fields.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_rag.py tests/test_ticket_intelligence.py tests/test_models.py -q` and confirm query results include `analysis` in all paths.
</verification>

<success_criteria>
- `QueryResult.to_dict()` includes nested `analysis` plus unchanged Phase 2 fields.
- Local fallback and retrieval-only paths return deterministic analysis with `llm_used=false`.
- LLM-backed path exposes `mode="llm_grounded"` only when `llm_used=true` and keeps deterministic trusted labels.
- Low-confidence/no-source cases route to `Rückfrage` with uncertainties.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ticket-intelligence/04-02-SUMMARY.md`.
</output>
