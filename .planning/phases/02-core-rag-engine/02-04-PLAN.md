---
phase: 02-core-rag-engine
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/ood/cli.py
  - tests/test_cli.py
  - README.md
autonomous: true
requirements:
  - RAG-01
  - RAG-02
  - RAG-03
  - RAG-04
  - RAG-05
  - INF-01
  - INF-03
  - INF-04
must_haves:
  truths:
    - "User can run `ood index` to build the initial knowledge base."
    - "User can run `ood reindex` to rebuild from scratch."
    - "User can run `ood query <text>` and receive answer/confidence/source output."
    - "`ood query --json` emits stable automation fields: query, answer, confidence, sources, llm_used, status."
  artifacts:
    - path: "src/ood/cli.py"
      provides: "Typer command integration for RagEngine index, query, and reindex"
      contains: "RagEngine(settings)"
    - path: "tests/test_cli.py"
      provides: "End-to-end CLI contract tests for Phase 2 commands and output modes"
      contains: "test_query_json_output_matches_phase_2_contract"
    - path: "README.md"
      provides: "Local usage documentation for indexing, querying, reindexing, and no-credential fallback"
      contains: "Core RAG usage"
  key_links:
    - from: "src/ood/cli.py"
      to: "src/ood/rag.py"
      via: "RagEngine service calls"
      pattern: "RagEngine\(settings\)"
    - from: "src/ood/cli.py"
      to: "QueryResult.to_dict"
      via: "JSON output"
      pattern: "json.dumps\(result.to_dict\(\)"
---

<objective>
Wire the Core RAG service into the existing Typer CLI and document Phase 2 usage.

Purpose: Replace Phase 1 stubs with user-visible indexing, reindexing, and query behavior while preserving output flags and path overrides.
Output: Working `ood index`, `ood reindex`, and `ood query` commands with stable human/JSON outputs and tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-rag-engine/02-CONTEXT.md
@.planning/phases/02-core-rag-engine/02-RESEARCH.md
@.planning/phases/01-foundation-cli/01-03-SUMMARY.md
@.planning/phases/02-core-rag-engine/02-03-SUMMARY.md
@src/ood/cli.py
@src/ood/rag.py
@src/ood/models.py

<interfaces>
Existing CLI options from src/ood/cli.py must remain available on index/query/reindex:
```python
JsonOption = Annotated[bool, typer.Option("--json", help="Emit machine-parseable JSON.")]
VerboseOption = Annotated[bool, typer.Option("-v", "--verbose", help="Show diagnostic context and exception details.")]
QuietOption = Annotated[bool, typer.Option("-q", "--quiet", help="Suppress non-essential output.")]
KnowledgeDirOption = Annotated[Path | None, typer.Option("--knowledge-dir", ...)]
DataDirOption = Annotated[Path | None, typer.Option("--data-dir", ...)]
StorageDirOption = Annotated[Path | None, typer.Option("--storage-dir", ...)]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace index and reindex stubs with RagEngine calls</name>
  <read_first>
    - src/ood/cli.py
    - tests/test_cli.py
    - src/ood/rag.py
    - src/ood/models.py
    - .planning/phases/02-core-rag-engine/02-CONTEXT.md
  </read_first>
  <files>src/ood/cli.py, tests/test_cli.py</files>
  <behavior>
    - INF-01/D-01: `ood index --json --knowledge-dir <dir> --storage-dir <dir>` returns JSON with `command="index"`, `status`, `indexed_documents`, `skipped_documents`, `storage_dir`, and `message`.
    - D-02: empty/missing knowledge dir exits 0 and JSON reports `indexed_documents: 0`.
    - INF-04/D-04: `ood reindex --json` calls `RagEngine.reindex_markdown()` and returns `command="reindex"`.
    - D-12: storage diagnostics appear in `--json` and `--verbose`, not normal human output.
  </behavior>
  <action>Import `RagEngine` from `ood.rag`. Replace `_emit_stub` usage for `index` with `result = RagEngine(settings).index_markdown()` and for `reindex` with `result = RagEngine(settings).reindex_markdown()`. Add `_emit_index_result(command: str, result: IndexResult, json_output: bool, verbose: bool, quiet: bool)`: in JSON mode emit `json.dumps({"command": command, **result.to_dict()})` with `typer.echo`; in quiet mode emit nothing; in normal human mode print `result.message`; in verbose mode additionally print exact prefix `Diagnostics: storage_dir=` followed by result storage path and `indexed_documents=<n> skipped_documents=<n>`. Remove assertions in tests that expect `"Index stub"` or `"Reindex stub"`; replace with Phase 2 contract assertions.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py::test_index_json_reports_document_counts tests/test_cli.py::test_reindex_json_reports_rebuild_command tests/test_cli.py::test_empty_knowledge_dir_index_is_successful_noop -q</automated>
  </verify>
  <done>`index` and `reindex` commands execute service methods and return Phase 2 JSON/human output without stub messages.</done>
  <acceptance_criteria>
    - `src/ood/cli.py` contains exact string `from ood.rag import RagEngine`.
    - `src/ood/cli.py` contains exact string `RagEngine(settings).index_markdown()`.
    - `src/ood/cli.py` contains exact string `RagEngine(settings).reindex_markdown()`.
    - `src/ood/cli.py` does not contain exact string `Index stub`.
    - `tests/test_cli.py` contains exact string `test_index_json_reports_document_counts`.
    - The task verification command exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace query stub with JSON and human RAG output</name>
  <read_first>
    - src/ood/cli.py
    - tests/test_cli.py
    - src/ood/rag.py
    - src/ood/models.py
    - .planning/phases/02-core-rag-engine/02-CONTEXT.md
  </read_first>
  <files>src/ood/cli.py, tests/test_cli.py</files>
  <behavior>
    - INF-03/D-14: `ood query "text" --json` emits JSON with keys `query`, `answer`, `confidence`, `sources`, `llm_used`, and `status`.
    - D-13: human `ood query "text"` prints confidence, confidence rationale, ranked sources, relevance scores, and excerpts; if retrieval-only it explains no generated answer is available.
    - D-16: query before index exits non-zero and prints `No index found. Run `ood index` first.`.
    - D-15: JSON source entries contain `path`, `score`, and `excerpt` with relative paths.
  </behavior>
  <action>In `query`, replace stub message with `result = RagEngine(settings).query(ticket_text)`. Catch `IndexMissingError` and raise `typer.Exit(code=1)` after printing its message to stderr; preserve verbose behavior by re-raising only unexpected exceptions through `_handle_error`. Add `_emit_query_result(result: QueryResult, json_output: bool, verbose: bool, quiet: bool)`: JSON emits `json.dumps(result.to_dict())`; quiet emits nothing; human output prints `Answer: <answer>` when `result.answer` is not `None`, otherwise exact line `Answer: unavailable (retrieval-only mode; no generated answer).`; print `Confidence: <score>` and `Confidence rationale: <rationale>`; then print `Sources:` and one line per source as `<rank>. <path> (score=<score>) — <excerpt>`. In verbose mode print exact prefix `Diagnostics: llm_used=` plus `status=<status> source_count=<len>`. Update tests to use monkeypatched `RagEngine.query` for output contract tests and temp storage for missing-index behavior.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py::test_query_json_output_matches_phase_2_contract tests/test_cli.py::test_query_human_output_includes_confidence_and_sources tests/test_cli.py::test_query_before_index_exits_nonzero_with_instruction -q</automated>
  </verify>
  <done>`query` command exposes stable Phase 2 JSON and useful human output, with actionable missing-index failure.</done>
  <acceptance_criteria>
    - `src/ood/cli.py` contains exact string `RagEngine(settings).query(ticket_text)`.
    - `src/ood/cli.py` contains exact string `Answer: unavailable (retrieval-only mode; no generated answer).`.
    - `src/ood/cli.py` contains exact string `Confidence rationale:`.
    - `tests/test_cli.py` contains exact string `test_query_json_output_matches_phase_2_contract`.
    - `tests/test_cli.py` contains exact string `test_query_before_index_exits_nonzero_with_instruction`.
    - The task verification command exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update README with Core RAG usage and verification</name>
  <read_first>
    - README.md
    - src/ood/cli.py
    - .planning/phases/02-core-rag-engine/02-CONTEXT.md
    - .planning/phases/02-core-rag-engine/02-RESEARCH.md
  </read_first>
  <files>README.md</files>
  <action>Replace the `## Phase 1 status` and `## CLI stubs` sections with `## Core RAG usage`. Document these exact commands: `uv run ood index`, `uv run ood reindex`, `uv run ood query "Wie löse ich den Fehler?"`, `uv run ood query "Wie löse ich den Fehler?" --json`, and `uv run ood index --knowledge-dir /path/to/knowledge --storage-dir /path/to/data/storage --json`. State that plain `.md` files under `knowledge/` are valid in Phase 2 per D-03, missing/empty knowledge directories are successful no-ops per D-02, Cloud LLM credentials are optional per D-05/D-07, and real secrets belong only in `.env` or environment variables. Add verification command `uv run pytest tests/test_cli.py tests/test_rag.py tests/test_models.py -q`.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py tests/test_rag.py tests/test_models.py -q</automated>
  </verify>
  <done>README documents the real Phase 2 commands and no longer describes `index`, `query`, or `reindex` as stubs.</done>
  <acceptance_criteria>
    - `README.md` contains exact heading `## Core RAG usage`.
    - `README.md` contains exact string `uv run ood query "Wie löse ich den Fehler?" --json`.
    - `README.md` contains exact string `Cloud LLM credentials are optional`.
    - `README.md` does not contain exact string `CLI stubs`.
    - The task verification command exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `uv run pytest -q`, then smoke-test JSON parsing with `uv run ood index --json` in an empty temp knowledge directory and `uv run ood query "test" --json` after indexing a temp Markdown fixture.
</verification>

<success_criteria>
- Existing flat Typer command names and path overrides are preserved.
- Phase 1 stub output is removed for `index`, `query`, and `reindex`.
- JSON query contract includes `query`, `answer`, `confidence`, `sources`, `llm_used`, and `status`.
- Human query output includes answer or retrieval-only explanation, confidence, rationale, ranked sources, scores, and excerpts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-rag-engine/02-04-SUMMARY.md`.
</output>
