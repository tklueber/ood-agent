---
phase: 02-core-rag-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/ood/rag.py
  - tests/test_rag.py
autonomous: true
requirements:
  - RAG-01
  - RAG-05
  - INF-01
  - INF-04
must_haves:
  truths:
    - "User can run `ood index` service logic over recursive Markdown files."
    - "Missing or empty knowledge directories are successful zero-document no-ops."
    - "Clean rebuild clears only the configured storage directory before indexing."
  artifacts:
    - path: "src/ood/rag.py"
      provides: "Markdown discovery, LightRAG indexing adapter, and clean reindex service methods"
      exports: ["RagEngine"]
    - path: "tests/test_rag.py"
      provides: "Indexing and reindex behavior tests using temporary knowledge/storage directories"
      contains: "test_index_markdown_discovers_recursive_plain_markdown"
  key_links:
    - from: "src/ood/rag.py"
      to: "Settings.knowledge_dir"
      via: "Path.rglob('*.md')"
      pattern: "knowledge_dir.*rglob\(\"\*\.md\"\)"
    - from: "src/ood/rag.py"
      to: "Settings.storage_dir"
      via: "LightRAG working_dir"
      pattern: "working_dir=str\(self.settings.storage_dir\)"
---

<objective>
Implement the service-layer indexing and clean reindex behavior behind Phase 2.

Purpose: Users need the configured Markdown knowledge directory converted into persistent LightRAG storage while preserving friendly no-op and safe rebuild decisions.
Output: `RagEngine.index_markdown()` and `RagEngine.reindex_markdown()` with tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-rag-engine/02-CONTEXT.md
@.planning/phases/02-core-rag-engine/02-RESEARCH.md
@.planning/phases/02-core-rag-engine/02-01-SUMMARY.md
@src/ood/config.py
@src/ood/models.py

<interfaces>
Contracts created by Plan 01 and consumed here:
```python
class IndexResult:
    status: str
    indexed_documents: int
    skipped_documents: int
    storage_dir: Path
    message: str
    def to_dict(self) -> dict[str, object]: ...

class IndexMissingError(RuntimeError): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Markdown discovery and no-op indexing</name>
  <read_first>
    - src/ood/rag.py (create if missing)
    - tests/test_rag.py (create if missing)
    - src/ood/models.py
    - src/ood/config.py
    - .planning/phases/02-core-rag-engine/02-CONTEXT.md
  </read_first>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - D-01/RAG-01: `RagEngine.index_markdown()` indexes all non-empty `*.md` files recursively under `settings.knowledge_dir`, including nested paths such as `apps/runbook.md`.
    - D-02: missing `knowledge_dir` returns `IndexResult(status="no_documents", indexed_documents=0, skipped_documents=0, message=...)` and does not raise.
    - D-03: plain Markdown without YAML frontmatter is accepted.
    - Empty/whitespace Markdown files are skipped and counted in `skipped_documents`.
    - D-05/D-06/RAG-05: when `settings.has_llm_credentials` is true, LightRAG receives a credential-backed non-noop LLM function; when false, it receives the noop fallback and no Cloud LLM function is invoked.
  </behavior>
  <action>Create `src/ood/rag.py` with `class RagEngine:` initialized as `def __init__(self, settings: Settings) -> None`. Add constants `LOCAL_EMBEDDING_MODEL = "paraphrase-multilingual-MiniLM-L12-v2"`, `LOCAL_EMBEDDING_DIM = 384`, and `MAX_EMBEDDING_TOKENS = 8192`. Implement `_discover_markdown_files()` using `self.settings.knowledge_dir.rglob("*.md")`, sorted by `.as_posix()`. Implement `_build_embedding_func()` that imports `EmbeddingFunc` from `lightrag.utils`, lazily creates `SentenceTransformer(LOCAL_EMBEDDING_MODEL)`, defines `async def _embed(texts: list[str]) -> np.ndarray: return model.encode(texts, normalize_embeddings=True)`, and returns `EmbeddingFunc(embedding_dim=LOCAL_EMBEDDING_DIM, max_token_size=MAX_EMBEDDING_TOKENS, func=_embed)`. Implement `async def _noop_llm_model_func(prompt: str, **_: object) -> str: return ""` for no-credential indexing/query fallback. Add `_build_llm_model_func()` that returns `_noop_llm_model_func` when `settings.has_llm_credentials` is false. When credentials exist, read the secret only with `self.settings.llm_api_key.get_secret_value()`, derive `model = self.settings.llm_model or "gpt-4o-mini"`, support `settings.llm_provider` values `None`, `"openai"`, or `"openai-compatible"`, lazily import `openai_complete_if_cache` from `lightrag.llm.openai`, and return an async non-noop wrapper that calls `openai_complete_if_cache(model, prompt, system_prompt=system_prompt, history_messages=history_messages or [], api_key=api_key, **kwargs)`. Never write the API key into files, logs, tests, result models, or exceptions. Implement `index_markdown()` as a synchronous wrapper around `asyncio.run(self._aindex_markdown(clear_storage=False))`. In `_aindex_markdown`, if `knowledge_dir` does not exist or no non-empty Markdown documents remain, create `settings.storage_dir` and return `IndexResult(status="no_documents", indexed_documents=0, skipped_documents=<empty_count>, storage_dir=settings.storage_dir, message="No Markdown documents found to index.")`. For real documents, read with `encoding="utf-8"`, pass document texts as `documents`, relative POSIX paths as both `ids` and `file_paths`, create `LightRAG(working_dir=str(self.settings.storage_dir), embedding_func=self._build_embedding_func(), llm_model_func=self._build_llm_model_func())`, call `await rag.initialize_storages()`, `await rag.ainsert(documents, ids=relative_paths, file_paths=relative_paths)`, and finalize in `finally`. This preserves retrieval-only indexing fallback without credentials while enabling LightRAG graph extraction/dual-level retrieval per RAG-05 when credentials are configured.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_index_markdown_discovers_recursive_plain_markdown tests/test_rag.py::test_index_missing_or_empty_knowledge_dir_is_successful_noop tests/test_rag.py::test_index_without_llm_credentials_uses_noop_and_no_cloud_calls tests/test_rag.py::test_index_with_llm_credentials_passes_non_noop_lightrag_llm_func -q</automated>
  </verify>
  <done>Recursive plain Markdown indexing and successful no-document no-op behavior are tested and implemented through `RagEngine.index_markdown()`.</done>
  <acceptance_criteria>
    - `src/ood/rag.py` contains exact string `class RagEngine`.
    - `src/ood/rag.py` contains exact string `rglob("*.md")`.
    - `src/ood/rag.py` contains exact string `file_paths=relative_paths`.
    - `src/ood/rag.py` contains exact string `working_dir=str(self.settings.storage_dir)`.
    - `src/ood/rag.py` contains exact string `def _build_llm_model_func`.
    - `src/ood/rag.py` contains exact string `get_secret_value()`.
    - `tests/test_rag.py` contains exact string `test_index_markdown_discovers_recursive_plain_markdown`.
    - `tests/test_rag.py` contains exact string `test_index_missing_or_empty_knowledge_dir_is_successful_noop`.
    - `tests/test_rag.py` contains exact string `test_index_without_llm_credentials_uses_noop_and_no_cloud_calls`.
    - `tests/test_rag.py` contains exact string `test_index_with_llm_credentials_passes_non_noop_lightrag_llm_func`.
    - The task verification command exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement clean reindex scoped to storage_dir</name>
  <read_first>
    - src/ood/rag.py
    - tests/test_rag.py
    - src/ood/models.py
    - .planning/phases/02-core-rag-engine/02-CONTEXT.md
  </read_first>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - D-04/INF-04: `RagEngine.reindex_markdown()` deletes existing files below `settings.storage_dir`, recreates that directory, and rebuilds from current Markdown.
    - D-04 safety: files in `settings.data_dir` outside `settings.storage_dir` and files in `settings.knowledge_dir` remain untouched.
    - D-11: `RagEngine.index_markdown()` must not clear storage before indexing.
  </behavior>
  <action>Add `reindex_markdown()` as a synchronous wrapper around `asyncio.run(self._aindex_markdown(clear_storage=True))`. Implement `_clear_storage_dir()` without using shell commands: if `settings.storage_dir.exists()`, iterate children and remove files/symlinks with `Path.unlink()` and directories with `shutil.rmtree(child)`; never remove `settings.data_dir`, `settings.knowledge_dir`, `.env`, or parent directories. Recreate `settings.storage_dir` with `mkdir(parents=True, exist_ok=True)` before indexing. Ensure `index_markdown()` always calls `_aindex_markdown(clear_storage=False)` so it reuses storage per D-11.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_reindex_clears_only_storage_dir_before_rebuild tests/test_rag.py::test_index_does_not_clear_existing_storage -q</automated>
  </verify>
  <done>Clean reindex behavior is scoped to `settings.storage_dir`; normal index does not clear existing storage.</done>
  <acceptance_criteria>
    - `src/ood/rag.py` contains exact string `def reindex_markdown`.
    - `src/ood/rag.py` contains exact string `clear_storage=True`.
    - `src/ood/rag.py` contains exact string `shutil.rmtree(child)`.
    - `tests/test_rag.py` contains exact string `test_reindex_clears_only_storage_dir_before_rebuild`.
    - `tests/test_rag.py` contains exact string `test_index_does_not_clear_existing_storage`.
    - The task verification command exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_rag.py -q`.
</verification>

<success_criteria>
- `RagEngine.index_markdown()` satisfies D-01, D-02, D-03, D-10, and D-11.
- `RagEngine.reindex_markdown()` satisfies D-04 and only clears `settings.storage_dir`.
- Tests prove no-op indexing, recursive discovery, plain Markdown acceptance, and safe rebuild behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-rag-engine/02-02-SUMMARY.md`.
</output>
