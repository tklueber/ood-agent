---
phase: 02-core-rag-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/ood/rag.py
  - tests/test_rag.py
autonomous: true
requirements:
  - RAG-02
  - RAG-03
  - RAG-04
  - RAG-05
  - INF-03
must_haves:
  truths:
    - "User can query an existing index with semantic search."
    - "Query results include relative source paths, scores, and excerpts."
    - "Missing LLM credentials return retrieval-only results with `answer=null` and `llm_used=false`."
    - "Query before index exists fails with an instruction to run `ood index` first."
  artifacts:
    - path: "src/ood/rag.py"
      provides: "Query execution, source normalization, and heuristic confidence scoring"
      exports: ["RagEngine"]
    - path: "tests/test_rag.py"
      provides: "Query contract tests for retrieval-only, missing index, source scoring, and confidence"
      contains: "test_query_without_llm_returns_ranked_sources_and_null_answer"
  key_links:
    - from: "src/ood/rag.py"
      to: "LightRAG.aquery_data"
      via: "QueryParam(mode=...)"
      pattern: "QueryParam\(mode=\"(mix|naive)\""
    - from: "src/ood/rag.py"
      to: "QueryResult.sources"
      via: "normalize chunks/references to SourceHit"
      pattern: "SourceHit\(path=.*score=.*excerpt="
---

<objective>
Implement semantic query behavior, source attribution, and confidence scoring in the RAG service layer.

Purpose: The CLI must be able to query persisted knowledge with a stable automation contract even when Cloud LLM credentials are absent.
Output: `RagEngine.query()` returns `QueryResult` with ranked sources, scores, excerpts, optional generated answer, and heuristic confidence.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-rag-engine/02-CONTEXT.md
@.planning/phases/02-core-rag-engine/02-RESEARCH.md
@.planning/phases/02-core-rag-engine/02-02-SUMMARY.md
@src/ood/config.py
@src/ood/models.py
@src/ood/rag.py

<interfaces>
Query contract from Plan 01:
```python
class SourceHit:
    path: str
    score: float
    excerpt: str

class ConfidenceScore:
    score: float
    rationale: str

class QueryResult:
    query: str
    answer: str | None
    confidence: ConfidenceScore
    sources: list[SourceHit]
    llm_used: bool
    status: str

class IndexMissingError(RuntimeError): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement retrieval-only query and source normalization</name>
  <read_first>
    - src/ood/rag.py
    - tests/test_rag.py
    - src/ood/models.py
    - src/ood/config.py
    - .planning/phases/02-core-rag-engine/02-CONTEXT.md
  </read_first>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - D-05/D-07: without `settings.has_llm_credentials`, `RagEngine.query("...")` returns `QueryResult(answer=None, llm_used=False, status="success")` when sources exist.
    - D-05 privacy: without credentials, query uses the noop LightRAG LLM function, makes no Cloud LLM calls, and still returns retrieval-only sources.
    - D-15/RAG-03: every source has relative `path`, numeric `score` between `0.0` and `1.0`, and non-empty `excerpt`.
    - D-16: if `settings.storage_dir` has no index marker/artifacts, raise `IndexMissingError("No index found. Run `ood index` first.")`.
  </behavior>
  <action>Add `query(self, query_text: str) -> QueryResult` as a synchronous wrapper around `asyncio.run(self._aquery(query_text))`. Add `_ensure_index_exists()` that raises `IndexMissingError("No index found. Run `ood index` first.")` when `settings.storage_dir` does not exist or contains no files. In `_aquery`, initialize LightRAG with `working_dir=str(self.settings.storage_dir)`, `embedding_func=self._build_embedding_func()`, and `llm_model_func=self._build_llm_model_func()`. When `settings.has_llm_credentials` is false, call `await rag.aquery_data(query_text, QueryParam(mode="naive", top_k=10, chunk_top_k=10))`, return `answer=None` and `llm_used=False`, and tests must prove the credential-backed LLM wrapper is not called. Normalize `data["data"]["chunks"]` into `SourceHit` objects. Extract path from chunk keys `file_path`, `full_doc_id`, `source`, or nested metadata when present; convert absolute paths under `settings.knowledge_dir` to relative POSIX paths; never emit absolute paths in `SourceHit.path`. Extract excerpt from `content`, `text`, or `chunk` and truncate to 500 characters. If LightRAG does not provide numeric scores, assign deterministic rank scores: first source `1.0`, then `max(0.1, 1.0 - rank * 0.15)`. Deduplicate by path keeping the highest score and first excerpt.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_query_without_llm_returns_ranked_sources_and_null_answer tests/test_rag.py::test_query_without_llm_credentials_makes_no_cloud_llm_calls tests/test_rag.py::test_query_before_index_raises_instructional_error -q</automated>
  </verify>
  <done>Retrieval-only query returns stable `QueryResult` sources and missing-index behavior raises an actionable exception.</done>
  <acceptance_criteria>
    - `src/ood/rag.py` contains exact string `def query(self, query_text: str) -> QueryResult`.
    - `src/ood/rag.py` contains exact string `QueryParam(mode="naive"`.
    - `src/ood/rag.py` contains exact string `llm_model_func=self._build_llm_model_func()`.
    - `src/ood/rag.py` contains exact string `Run `ood index` first`.
    - `src/ood/rag.py` contains exact string `SourceHit(`.
    - `tests/test_rag.py` contains exact string `test_query_without_llm_returns_ranked_sources_and_null_answer`.
    - `tests/test_rag.py` contains exact string `test_query_without_llm_credentials_makes_no_cloud_llm_calls`.
    - `tests/test_rag.py` contains exact string `test_query_before_index_raises_instructional_error`.
    - The task verification command exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add heuristic confidence and optional LLM-backed answer path</name>
  <read_first>
    - src/ood/rag.py
    - tests/test_rag.py
    - src/ood/models.py
    - src/ood/config.py
    - .planning/phases/02-core-rag-engine/02-RESEARCH.md
  </read_first>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - D-08/RAG-04: confidence is computed heuristically from top source score, source count, score spread, and LLM availability.
    - D-06/RAG-05: when `settings.has_llm_credentials` is true, query passes a credential-backed non-noop LLM function to LightRAG, uses `QueryParam(mode="mix", top_k=10, chunk_top_k=10)`, and may request a generated answer from LightRAG using retrieved source documents.
    - D-07: retrieval-only confidence rationale contains exact phrase `limited because no LLM credentials are configured`.
  </behavior>
  <action>Add `_score_confidence(sources: list[SourceHit], llm_used: bool) -> ConfidenceScore` with exact formula: if no sources return score `0.0` and rationale `No sources retrieved.`; otherwise `top_score = sources[0].score`, `source_factor = min(len(sources), 3) / 3`, `spread_factor = 1.0` when one source else `min(1.0, max(0.0, sources[0].score - sources[-1].score + 0.5))`, `llm_factor = 1.0 if llm_used else 0.7`, and final `round(min(1.0, top_score * 0.5 + source_factor * 0.25 + spread_factor * 0.15 + llm_factor * 0.10), 2)`. For credential-present mode, select `query_param = QueryParam(mode="mix", top_k=10, chunk_top_k=10)`, create LightRAG with `llm_model_func=self._build_llm_model_func()`, call `await rag.aquery_data(query_text, query_param)` for sources, and call LightRAG's LLM-backed query path (`await rag.aquery(query_text, param=query_param)` if available in installed 1.4.15) to produce `answer` after retrieval. Set `llm_used=True` only when `settings.has_llm_credentials` is true and a non-empty generated answer is returned; otherwise return `llm_used=False`, `answer=None`, and include rationale phrase `limited because no LLM credentials are configured or provider is unsupported`. Tests must monkeypatch the LightRAG/OpenAI adapter rather than using real secrets: use fake `Settings` credentials, assert the captured `llm_model_func` is not `_noop_llm_model_func`, assert no fake API key is written to test snapshots/files, and assert `QueryParam(mode="mix")` is used. Do not write secrets to tests or files.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_confidence_score_uses_retrieval_signal_and_llm_availability tests/test_rag.py::test_llm_credentials_select_mix_query_mode tests/test_rag.py::test_llm_credentials_pass_non_noop_lightrag_llm_func_for_query -q</automated>
  </verify>
  <done>Query results include deterministic confidence scoring and select `mix` mode only when LLM credentials are available.</done>
  <acceptance_criteria>
    - `src/ood/rag.py` contains exact string `def _score_confidence`.
    - `src/ood/rag.py` contains exact string `limited because no LLM credentials are configured`.
    - `src/ood/rag.py` contains exact string `QueryParam(mode="mix"`.
    - `src/ood/rag.py` contains exact string `await rag.aquery(query_text, param=query_param)`.
    - `tests/test_rag.py` contains exact string `test_confidence_score_uses_retrieval_signal_and_llm_availability`.
    - `tests/test_rag.py` contains exact string `test_llm_credentials_select_mix_query_mode`.
    - `tests/test_rag.py` contains exact string `test_llm_credentials_pass_non_noop_lightrag_llm_func_for_query`.
    - The task verification command exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_rag.py -q`.
</verification>

<success_criteria>
- Query service satisfies D-05 through D-08 and D-14 through D-16 at the model/service level.
- Sources are ranked, scored, excerpted, and path-normalized relative to `knowledge_dir`.
- Confidence scoring is deterministic and tested without relying on LLM self-rating.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-rag-engine/02-03-SUMMARY.md`.
</output>
