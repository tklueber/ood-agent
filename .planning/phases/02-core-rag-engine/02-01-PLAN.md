---
phase: 02-core-rag-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/ood/models.py
  - tests/test_models.py
autonomous: true
requirements:
  - RAG-03
  - RAG-04
  - RAG-05
must_haves:
  truths:
    - "User-facing index and query behavior is backed by LightRAG dependencies instead of custom retrieval code."
    - "When Cloud LLM credentials are configured, users can run LLM-backed query generation without a missing OpenAI-compatible runtime dependency."
    - "CLI JSON output can expose stable source, score, confidence, and status fields without leaking LightRAG internals."
  artifacts:
    - path: "pyproject.toml"
      provides: "Runtime dependencies for LightRAG and local semantic embeddings"
      contains: "lightrag-hku==1.4.15"
    - path: "pyproject.toml"
      provides: "OpenAI-compatible runtime dependency required by LightRAG's credential-backed LLM helper"
      contains: "openai==2.33.0"
    - path: "src/ood/models.py"
      provides: "Stable IndexResult, SourceHit, ConfidenceScore, QueryResult, and IndexMissingError contracts"
      exports: ["IndexResult", "SourceHit", "ConfidenceScore", "QueryResult", "IndexMissingError"]
  key_links:
    - from: "src/ood/models.py"
      to: "tests/test_models.py"
      via: "typed model serialization tests"
      pattern: "SourceHit\(path=\"runbook.md\""
---

<objective>
Create the dependency and typed-contract foundation for Phase 2 RAG behavior.

Purpose: Downstream indexing, query, and CLI plans need stable OOD-owned contracts so LightRAG internals never leak into user-facing JSON or tests.
Output: LightRAG dependency stack plus result dataclasses and tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-rag-engine/02-CONTEXT.md
@.planning/phases/02-core-rag-engine/02-RESEARCH.md
@.planning/phases/01-foundation-cli/01-01-SUMMARY.md
@pyproject.toml
@src/ood/config.py

<interfaces>
Existing configuration contract from src/ood/config.py:
```python
class Settings(BaseSettings):
    knowledge_dir: Path = Path("knowledge")
    data_dir: Path = Path("data")
    storage_dir: Path | None = None
    llm_provider: str | None = None
    llm_api_key: SecretStr | None = None
    llm_model: str | None = None
    verbose: int = 0
    quiet: bool = False

    @property
    def has_llm_credentials(self) -> bool: ...

def load_settings(**overrides: Any) -> Settings: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 RAG runtime dependencies</name>
  <read_first>
    - pyproject.toml
    - uv.lock
    - .planning/phases/02-core-rag-engine/02-CONTEXT.md
    - .planning/phases/02-core-rag-engine/02-RESEARCH.md
  </read_first>
  <files>pyproject.toml, uv.lock</files>
  <action>Add these exact runtime dependencies using uv so `uv.lock` is updated by the resolver: `lightrag-hku==1.4.15`, `sentence-transformers==5.4.1`, `numpy==2.4.4`, and `openai==2.33.0`. Use `uv add "lightrag-hku==1.4.15" "sentence-transformers==5.4.1" "numpy==2.4.4" "openai==2.33.0"`. Include `openai==2.33.0` because downstream Plans 02-02 and 02-03 use LightRAG's credential-backed OpenAI-compatible helper when D-05/D-06 credentials are present; adding the dependency here verifies the import path without using real secrets. This implements RAG-05 per D-09/D-10 by enabling LightRAG Core with local storage under `Settings.storage_dir` and preserves privacy by only installing the adapter package, not configuring or calling any Cloud LLM.</action>
  <verify>
    <automated>uv lock --check &amp;&amp; uv run python -c "import lightrag, sentence_transformers, numpy, openai; from lightrag.llm.openai import openai_complete_if_cache; print('rag-deps-ok')"</automated>
  </verify>
  <done>Dependency import command prints `rag-deps-ok`, `pyproject.toml` contains `lightrag-hku==1.4.15`, `sentence-transformers==5.4.1`, `numpy==2.4.4`, and `openai==2.33.0`, the no-real-secret LightRAG OpenAI helper import succeeds, and `uv.lock` is updated.</done>
  <acceptance_criteria>
    - `pyproject.toml` contains exact string `lightrag-hku==1.4.15`.
    - `pyproject.toml` contains exact string `sentence-transformers==5.4.1`.
    - `pyproject.toml` contains exact string `numpy==2.4.4`.
    - `pyproject.toml` contains exact string `openai==2.33.0`.
    - `uv run python -c "import lightrag, sentence_transformers, numpy, openai; from lightrag.llm.openai import openai_complete_if_cache; print('rag-deps-ok')"` exits 0 and prints `rag-deps-ok` without requiring any real API key or secret.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create stable RAG result models</name>
  <read_first>
    - src/ood/models.py (create if missing)
    - tests/test_models.py (create if missing)
    - src/ood/config.py
    - .planning/phases/02-core-rag-engine/02-CONTEXT.md
  </read_first>
  <files>src/ood/models.py, tests/test_models.py</files>
  <behavior>
    - `SourceHit(path="runbook.md", score=0.87, excerpt="restart service")` serializes with keys `path`, `score`, and `excerpt` for D-15/RAG-03.
    - `ConfidenceScore(score=0.42, rationale="retrieval-only: 1 source")` serializes with keys `score` and `rationale` for D-08/RAG-04.
    - `QueryResult(..., llm_used=False, status="success")` serializes to a dict containing `query`, `answer`, `confidence`, `sources`, `llm_used`, and `status` for D-14.
  </behavior>
  <action>Create `src/ood/models.py` with `@dataclass(frozen=True)` models: `SourceHit(path: str, score: float, excerpt: str)`, `ConfidenceScore(score: float, rationale: str)`, `IndexResult(status: str, indexed_documents: int, skipped_documents: int, storage_dir: Path, message: str)`, `QueryResult(query: str, answer: str | None, confidence: ConfidenceScore, sources: list[SourceHit], llm_used: bool, status: str)`, and `IndexMissingError(RuntimeError)`. Add `to_dict()` methods on the four dataclasses; `Path` values must serialize with `str(...)`, nested models must serialize as dictionaries, and no method may expose absolute source paths except `IndexResult.storage_dir`. Export all names through `__all__`. Write `tests/test_models.py` covering the three behavior cases above before implementation.</action>
  <verify>
    <automated>uv run pytest tests/test_models.py -q</automated>
  </verify>
  <done>Models exist, tests pass, and downstream plans can import `IndexResult`, `QueryResult`, `SourceHit`, `ConfidenceScore`, and `IndexMissingError` from `ood.models`.</done>
  <acceptance_criteria>
    - `src/ood/models.py` contains exact string `class SourceHit`.
    - `src/ood/models.py` contains exact string `class ConfidenceScore`.
    - `src/ood/models.py` contains exact string `class QueryResult`.
    - `src/ood/models.py` contains exact string `class IndexMissingError(RuntimeError)`.
    - `tests/test_models.py` contains exact string `def test_query_result_to_dict_matches_json_contract`.
    - `uv run pytest tests/test_models.py -q` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_models.py -q` and `uv lock --check`.
</verification>

<success_criteria>
- LightRAG/local embedding/OpenAI-compatible helper dependencies are resolvable through uv.
- OOD-owned result contracts exist before service and CLI implementation.
- JSON contract fields required by D-14/D-15 are represented in tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-rag-engine/02-01-SUMMARY.md`.
</output>
