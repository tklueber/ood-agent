---
phase: 15-local-graph-metadata-retrieval-and-data-quality-review
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - src/ood/cli.py
  - tests/test_cli.py
  - README.md
autonomous: true
requirements:
  - GRAPH-02
must_haves:
  truths:
    - "Operators can see graph/metadata retrieval status and fusion components from CLI output."
    - "Automation can inspect complete component diagnostics in query JSON."
    - "README documents the local graph/metadata retrieval workflow and privacy boundary."
  artifacts:
    - path: "src/ood/cli.py"
      provides: "Verbose query rendering for metadata/graph fusion diagnostics"
      contains: "metadata_score"
    - path: "tests/test_cli.py"
      provides: "CLI assertions for active graph/metadata diagnostics"
    - path: "README.md"
      provides: "User-facing local graph retrieval documentation"
  key_links:
    - from: "src/ood/cli.py"
      to: "QueryResult.retrieval_diagnostics"
      via: "verbose query output reads public diagnostics"
      pattern: "metadata_score.*graph_score"
---

<objective>
Expose graph/metadata retrieval clearly through existing CLI and documentation.

Purpose: Phase 15 success requires diagnostics that show what came from vector, lexical, metadata, graph, and final fusion; this must be visible to users without reading runtime JSON by hand.
Output: verbose CLI diagnostics and README guidance for local graph/metadata retrieval.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-DISCOVERY.md
@.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-02-SUMMARY.md
@src/ood/cli.py
@tests/test_cli.py
@README.md

<interfaces>
`_emit_query_result()` currently keeps JSON as `result.to_dict()` and prints only human diagnostics when `--verbose` is set. Preserve this boundary: do not add CLI-only fields to JSON.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render concise graph/metadata fusion diagnostics in verbose query output</name>
  <files>src/ood/cli.py, tests/test_cli.py</files>
  <behavior>
    - `ood query "..." --verbose` prints retrieval backend, strategy, graph status, and a concise top-source component line.
    - The component line includes vector/semantic, lexical, metadata, graph, and final scores for at least the top result.
    - `ood query "..." --json` remains exactly `QueryResult.to_dict()` and includes no CLI-only wrappers.
  </behavior>
  <action>Update `_emit_query_result()` verbose branch to render a small diagnostics section after the existing summary. Include `retrieval_backend`, `retrieval_strategy`, `graph_status`, graph artifact/document/edge counts when available, and top 3 score components as `path semantic=... lexical=... metadata=... graph=... final=...`. Use fields from `RetrievalDiagnostics` and `SourceScoreBreakdown.to_dict()` only. Add CLI tests using fake `QueryResult` with active graph diagnostics to assert human output and JSON stability.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py::test_query_verbose_output_includes_graph_metadata_components tests/test_cli.py::test_query_json_output_matches_phase_2_contract -q</automated>
  </verify>
  <done>Verbose CLI output explains fusion components while JSON remains the stable public query contract.</done>
</task>

<task type="auto">
  <name>Task 2: Document local graph/metadata retrieval workflow and privacy rules</name>
  <files>README.md</files>
  <action>Update the README Graph retrieval section so it no longer says graph retrieval is deferred once Plan 15-02 is complete. Document: `ood-local-graph-index.json` is generated under `storage_dir`; signals include frontmatter title/type/service/system/component, keywords, aliases, tags, headings, commands, and Wikilinks; query diagnostics show semantic/vector, lexical, metadata, graph, and final fusion; all graph artifacts are derived locally from Markdown and remain outside git; Cloud LLM approval is not required or used for graph/metadata retrieval. Include an example command using the external OOD-KB path: `uv run ood reindex --knowledge-dir /Users/timo/obsidian/secondBrain/Arbeit/06_OOD-KB/articles --storage-dir data/ood-kb-storage --json` followed by `uv run ood query "TraceId Kafka AKHQ Ersatzgeschäft" --storage-dir data/ood-kb-storage --verbose`.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q</automated>
  </verify>
  <done>README gives operators exact local commands and explains graph/metadata retrieval without implying Cloud LLM usage.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_cli.py tests/test_rag.py tests/test_models.py -q` to verify CLI, retrieval, and diagnostics contracts together.
</verification>

<success_criteria>
- Verbose human query output explicitly shows metadata and graph component scores.
- JSON output remains stable and automation-friendly.
- README contains a local-only OOD-KB workflow for the TraceId/Kafka smoke query.
</success_criteria>

<output>
After completion, create `.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-03-SUMMARY.md`.
</output>
