---
phase: 15-local-graph-metadata-retrieval-and-data-quality-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ood/models.py
  - src/ood/rag.py
  - tests/test_models.py
  - tests/test_rag.py
autonomous: true
requirements:
  - GRAPH-02
must_haves:
  truths:
    - "Local indexing creates a privacy-safe graph/metadata artifact from Markdown/frontmatter without Cloud LLM usage."
    - "The artifact contains operational metadata, headings, Wikilinks, aliases/keywords, and commands needed for graph retrieval."
    - "Existing vector indexing, manifest writing, and metadata warning behavior continue to pass existing tests."
  artifacts:
    - path: "src/ood/models.py"
      provides: "Public diagnostics models with vector, lexical, metadata, graph, and final fusion score fields"
      contains: "SourceScoreBreakdown"
    - path: "src/ood/rag.py"
      provides: "Local graph index artifact generation under storage_dir"
      contains: "LOCAL_GRAPH_INDEX_FILENAME"
    - path: "tests/test_rag.py"
      provides: "Regression tests proving graph artifact generation from Markdown/frontmatter/Wikilinks"
  key_links:
    - from: "src/ood/rag.py"
      to: "storage_dir/ood-local-graph-index.json"
      via: "index/reindex/update writes graph artifact beside vector artifact"
      pattern: "LOCAL_GRAPH_INDEX_FILENAME"
    - from: "src/ood/models.py"
      to: "QueryResult.to_dict()"
      via: "retrieval_diagnostics score component serialization"
      pattern: "metadata_score.*graph_score.*final_score"
---

<objective>
Create the local graph/metadata retrieval foundation that Phase 11 explicitly deferred.

Purpose: frontmatter and Obsidian relationship signals must be available to retrieval so relevant articles such as `how-to-find-traceid-in-kafka-message.md` are not limited to body-vector and lexical body matches.
Output: public diagnostics contract extensions plus a deterministic `ood-local-graph-index.json` runtime artifact generated during index/reindex/update.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-DISCOVERY.md
@.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-01-SUMMARY.md
@.planning/phases/11-cli-grade-hybrid-retrieval-and-extractive-answer-synthesis/11-03-SUMMARY.md
@src/ood/models.py
@src/ood/rag.py
@tests/test_models.py
@tests/test_rag.py

<interfaces>
Existing public contracts to preserve:

```python
@dataclass(frozen=True)
class SourceHit:
    path: str
    score: float
    excerpt: str
    score_details: dict[str, Any] = field(default_factory=dict)

@dataclass(frozen=True)
class SourceScoreBreakdown:
    path: str
    semantic_score: float
    lexical_score: float
    combined_score: float
    lexical_matches: list[str]
    weights: dict[str, float]

@dataclass(frozen=True)
class RetrievalDiagnostics:
    backend: str = "unknown"
    strategy: str = "unknown"
    score_components: list[SourceScoreBreakdown] = field(default_factory=list)
    graph_retrieval: dict[str, Any] = field(default_factory=dict)
    notes: list[str] = field(default_factory=list)
```

Plan 15 should extend these additively by adding defaulted `metadata_score`, `graph_score`, `final_score`, `metadata_matches`, and `graph_matches` fields to `SourceScoreBreakdown`. Existing constructors/tests must continue to work because defaults are provided.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend retrieval score diagnostics for graph/metadata fusion</name>
  <files>src/ood/models.py, tests/test_models.py</files>
  <behavior>
    - Existing `SourceScoreBreakdown(path, semantic_score, lexical_score, combined_score, lexical_matches, weights)` construction still works.
    - `SourceScoreBreakdown.to_dict()` includes `semantic_score`, `lexical_score`, `metadata_score`, `graph_score`, `combined_score`, `final_score`, `lexical_matches`, `metadata_matches`, `graph_matches`, and `weights`.
    - `QueryResult.to_dict()["retrieval_diagnostics"]["score_components"]` serializes the new fields without changing top-level query fields.
  </behavior>
  <action>Add defaulted fields to `SourceScoreBreakdown` for metadata and graph diagnostics: `metadata_score: float = 0.0`, `graph_score: float = 0.0`, `final_score: float | None = None`, `metadata_matches: list[str] = field(default_factory=list)`, `graph_matches: list[str] = field(default_factory=list)`. Update `to_dict()` so `final_score` emits `combined_score` when unset. Keep the old constructor signature compatible by placing all new fields after existing fields. Add focused model tests proving backward compatibility and new serialization.</action>
  <verify>
    <automated>uv run pytest tests/test_models.py -q</automated>
  </verify>
  <done>Score diagnostics can represent vector, lexical, metadata, graph, and final fusion components while existing Phase 11 JSON consumers remain compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate local graph/metadata index artifact during knowledge indexing</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - `index_markdown()`, `reindex_markdown()`, and `update_markdown()` write `ood-local-graph-index.json` under `settings.storage_dir` whenever parsed Markdown documents are available.
    - The graph artifact includes one document node per Markdown path with title, type, service/system/component, keywords/aliases/tags, headings, commands, outgoing Wikilinks, incoming-link counts, and normalized search tokens.
    - No Cloud LLM or external service is called while building the artifact; tests monkeypatch embeddings as needed.
  </behavior>
  <action>In `src/ood/rag.py`, add `LOCAL_GRAPH_INDEX_FILENAME = "ood-local-graph-index.json"` and a `_graph_index_path` helper. Implement dependency-free extraction helpers that use `_ParsedMarkdownDocument.raw_content`, `.metadata`, and `.body_text` to collect: scalar metadata, observed YAML list fields (`keywords`, `keywords_de`, `keywords_en`, `aliases`, `tags`, `related`, `symptoms`, `splunk_queries`), Markdown headings, fenced/inline commands under `## Commands`, and Obsidian Wikilinks `[[target]]`. Build a JSON payload with `schema_version`, `generated_at`, `documents`, and `edges`; store only source-derived metadata/body snippets needed for retrieval, under `storage_dir`. Call the writer after manifest/vector write paths in index/reindex and after update manifest write so graph artifact stays synchronized. Do not commit generated artifacts.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py -q</automated>
  </verify>
  <done>Local indexing creates `ood-local-graph-index.json` with document metadata and relationship signals for synthetic fixtures including a TraceId/Kafka-style article and its incoming/outgoing links.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_models.py tests/test_rag.py -q` and confirm existing hybrid retrieval tests still pass while new graph artifact tests pass.
</verification>

<success_criteria>
- `LOCAL_GRAPH_INDEX_FILENAME` is exported from `src/ood/rag.py`.
- A graph artifact is generated under `storage_dir`, not inside source control.
- Retrieval diagnostic models can show metadata and graph components before retrieval scoring consumes them in Plan 15-02.
</success_criteria>

<output>
After completion, create `.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-01-SUMMARY.md`.
</output>
