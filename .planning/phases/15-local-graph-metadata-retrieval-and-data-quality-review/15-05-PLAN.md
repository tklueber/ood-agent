---
phase: 15-local-graph-metadata-retrieval-and-data-quality-review
plan: 05
type: execute
wave: 4
depends_on:
  - 15-03
  - 15-04
files_modified:
  - src/ood/cli.py
  - tests/test_cli.py
  - README.md
  - docs/ood-kb-source-quality.md
  - evaluation/datasets/ood-kb-traceid-smoke.json
autonomous: true
requirements:
  - GRAPH-02
  - KNW-11
  - KNW-12
must_haves:
  truths:
    - "User can run a CLI quality audit against the external 438-document OOD-KB corpus and save a local JSON report."
    - "User has a documented TraceId/Kafka smoke case for the Phase 13/14 evaluation loop."
    - "User receives concrete source-data improvement tips for normalized metadata, synonyms, links, ownership, and verification fields."
  artifacts:
    - path: "src/ood/cli.py"
      provides: "quality-audit CLI command"
      contains: "quality-audit"
    - path: "docs/ood-kb-source-quality.md"
      provides: "Actionable source-data improvement guide"
    - path: "evaluation/datasets/ood-kb-traceid-smoke.json"
      provides: "Portable smoke case for TraceId/Kafka retrieval regression"
  key_links:
    - from: "src/ood/cli.py"
      to: "src/ood/knowledge_quality.py"
      via: "quality-audit command calls audit_knowledge_corpus"
      pattern: "audit_knowledge_corpus"
    - from: "evaluation/datasets/ood-kb-traceid-smoke.json"
      to: "how-to-find-traceid-in-kafka-message.md"
      via: "expected source for future black-box evaluation"
      pattern: "TraceId Kafka AKHQ Ersatzgeschäft"
---

<objective>
Wire the quality review into the CLI and document the concrete OOD-KB improvement workflow.

Purpose: Phase 15 must leave the user with runnable local review commands, a TraceId/Kafka regression artifact, and actionable source-data recommendations/tips.
Output: `ood quality-audit`, source-quality guide, README workflow, and a TraceId/Kafka smoke evaluation fixture.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-DISCOVERY.md
@.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-03-SUMMARY.md
@.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-04-SUMMARY.md
@src/ood/cli.py
@src/ood/knowledge_quality.py
@README.md

<interfaces>
Plan 15-04 exports `audit_knowledge_corpus(corpus_dir: Path, expected_documents: int | None = None)` and report dataclasses with `to_dict()`. The CLI should call this directly and write optional report files under user-selected paths, not commit generated reports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `ood quality-audit` CLI command for local corpus review</name>
  <files>src/ood/cli.py, tests/test_cli.py</files>
  <behavior>
    - `uv run ood quality-audit --corpus-dir <dir> --json` prints `{"command":"quality-audit", ...report}`.
    - `--expected-documents 438` adds a corpus-size expectation to the report and flags mismatch if needed.
    - `--report-path <path>` writes the same JSON report locally without requiring Cloud LLM or network access.
  </behavior>
  <action>Add a flat Typer command named `quality-audit` following existing CLI style. Options: `--corpus-dir` (Path, default `/Users/timo/obsidian/secondBrain/Arbeit/06_OOD-KB/articles`), `--expected-documents` (int | None, default 438), `--report-path` (Path | None), `--json`, `--quiet`, `--verbose`. Human output should summarize document count, overall readiness score if available, top weak dimensions, and first 5 recommendations (10 in verbose). JSON output should be exactly `{"command": "quality-audit", **report.to_dict()}`. Write report files only when requested and create parent directories as needed. Do not write reports into git by default.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py::test_quality_audit_json_reports_retrieval_readiness tests/test_cli.py::test_quality_audit_writes_report_path -q</automated>
  </verify>
  <done>User can audit the external OOD-KB corpus and save a local JSON report through the CLI.</done>
</task>

<task type="auto">
  <name>Task 2: Add OOD-KB source-quality guide and TraceId smoke fixture</name>
  <files>README.md, docs/ood-kb-source-quality.md, evaluation/datasets/ood-kb-traceid-smoke.json</files>
  <action>Create `docs/ood-kb-source-quality.md` with practical source-data improvement guidance: normalized frontmatter examples for `system`, `component`, `problem_type`, `keywords`, `aliases`, `related`, `supersedes`, `last_verified`, `owner`; synonym tips for `trace id`, `traceid`, `TraceId`, `correlation id`, `Kafka message`, `AKHQ`, `Ersatzgeschäft`; Wikilink patterns between troubleshooting articles, tickets, runbooks, and error-code notes; command block/risk marker hygiene; title quality; freshness and source attribution. Update README with commands to run `quality-audit` against `/Users/timo/obsidian/secondBrain/Arbeit/06_OOD-KB/articles`, save a local report under `data/reports/ood-kb-quality.json`, reindex/query the TraceId case, and feed the smoke fixture into Phase 13/14 once those execution/reporting commands exist. Add `evaluation/datasets/ood-kb-traceid-smoke.json` as a lightweight fixture with query text `TraceId Kafka AKHQ Ersatzgeschäft`, expected source `how-to-find-traceid-in-kafka-message.md`, and note that it targets the external OOD-KB corpus rather than the synthetic mock corpus.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py tests/test_knowledge_quality.py -q</automated>
  </verify>
  <done>Documentation and fixture make the TraceId/Kafka miss reproducible and give the user actionable source-data improvement tips.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_cli.py tests/test_knowledge_quality.py tests/test_rag.py tests/test_models.py -q`. If the external OOD-KB path is available, also run `uv run ood quality-audit --corpus-dir /Users/timo/obsidian/secondBrain/Arbeit/06_OOD-KB/articles --expected-documents 438 --report-path data/reports/ood-kb-quality.json --json` and do not commit `data/reports/ood-kb-quality.json`.
</verification>

<success_criteria>
- CLI quality audit can review the 438-document corpus locally and emit actionable recommendations.
- The TraceId/Kafka smoke fixture is available for the Phase 13/14 reporting loop.
- README and docs explain how to improve source data and how to validate retrieval diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-05-SUMMARY.md`.
</output>
