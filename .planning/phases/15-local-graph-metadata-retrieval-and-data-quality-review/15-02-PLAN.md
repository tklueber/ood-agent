---
phase: 15-local-graph-metadata-retrieval-and-data-quality-review
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/ood/rag.py
  - tests/test_rag.py
autonomous: true
requirements:
  - GRAPH-02
must_haves:
  truths:
    - "Local queries combine vector, lexical, metadata, and graph signals without Cloud LLM usage."
    - "TraceId/Kafka query diagnostics explain why `how-to-find-traceid-in-kafka-message.md` is promoted."
    - "Query JSON shows what came from vector, lexical, metadata, graph, and final fusion."
  artifacts:
    - path: "src/ood/rag.py"
      provides: "Metadata/graph fusion scorer in local fallback retrieval"
      contains: "hybrid_semantic_lexical_metadata_graph"
    - path: "tests/test_rag.py"
      provides: "TraceId/Kafka regression coverage for metadata/graph promotion"
      contains: "how-to-find-traceid-in-kafka-message.md"
  key_links:
    - from: "src/ood/rag.py"
      to: "ood-local-vector-index.json"
      via: "semantic vector score"
      pattern: "semantic_score"
    - from: "src/ood/rag.py"
      to: "ood-local-graph-index.json"
      via: "metadata and wikilink graph score"
      pattern: "metadata_score.*graph_score"
    - from: "SourceHit.score_details"
      to: "RetrievalDiagnostics.score_components"
      via: "same component values for ranked sources"
      pattern: "final_score"
---

<objective>
Activate deterministic local metadata/graph retrieval in the query path.

Purpose: operational misses like `TraceId Kafka AKHQ Ersatzgeschäft` need article metadata, keyword, service/type, and relationship signals to affect rank, not just stripped body text and vector similarity.
Output: local fallback query scoring strategy `hybrid_semantic_lexical_metadata_graph` with transparent per-source fusion diagnostics.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-DISCOVERY.md
@.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-01-SUMMARY.md
@src/ood/rag.py
@tests/test_rag.py

<interfaces>
Plan 15-01 creates `LOCAL_GRAPH_INDEX_FILENAME`, graph artifact writer/reader helpers, and extended `SourceScoreBreakdown` fields. Use those contracts directly; do not invent a second artifact or diagnostics shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Score metadata and graph matches for local fallback documents</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - Given query tokens `traceid kafka akhq ersatzgeschäft`, metadata score matches target article title/service/keywords even if body vector similarity is low.
    - Graph score increases when related documents link to the target article or the target article links to query-relevant articles.
    - Missing or malformed graph artifact degrades gracefully to existing semantic+lexical behavior with graph status explaining the fallback.
  </behavior>
  <action>Implement local-only scoring helpers: `_read_local_graph_index()`, `_score_metadata_match(query_tokens, graph_doc)`, and `_score_graph_match(query_tokens, graph_doc, graph_index)`. Metadata scoring should consider normalized title, type, service/system/component, keywords, aliases, tags, headings, and commands. Graph scoring should consider outgoing linked article slugs/titles, incoming-link count, and query overlap in linked document metadata. Keep scores clamped to 0.0-1.0 and return explicit match labels such as `keyword:TraceId`, `service:Kafka`, `link:how-to-splunk`, `incoming_link:uno-fehler-offerte-verarbeitung.md`. If the graph artifact is absent, return 0.0 metadata/graph scores and a diagnostics note rather than crashing.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_local_graph_metadata_scoring_matches_traceid_frontmatter -q</automated>
  </verify>
  <done>Metadata and graph scoring helpers are deterministic, privacy-safe, and independently covered by TraceId/Kafka-style fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fuse vector, lexical, metadata, and graph scores into ranked query results</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - A synthetic regression query `TraceId Kafka AKHQ Ersatzgeschäft` ranks `how-to-find-traceid-in-kafka-message.md` at rank 1 when its vector score is lower than competing body-similar Ersatzgeschäft articles.
    - `SourceHit.score_details` for the target includes semantic/vector, lexical, metadata, graph, weights, match lists, and final fusion score.
    - `retrieval_diagnostics.backend == "local_vector_graph_index"` and `strategy == "hybrid_semantic_lexical_metadata_graph"`.
  </behavior>
  <action>Update `_query_local_fallback()` to load the graph artifact once, score every valid vector document with semantic, lexical, metadata, and graph components, and compute `final_score` with explicit weights. Start with conservative weights that preserve existing exact lexical behavior while enabling metadata/graph lift, for example semantic 0.45, lexical 0.25, metadata 0.20, graph 0.10 plus existing exact-operational-token boost only when relevant. Populate `SourceScoreBreakdown` and `SourceHit.score_details` with all components. Update `_local_retrieval_diagnostics()` to report active graph retrieval status (`active`, artifact path/counts, replacement no longer deferred) and notes describing fusion. Keep result cap at `QUERY_SOURCE_LIMIT`.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_traceid_kafka_query_is_promoted_by_metadata_graph_fusion tests/test_rag.py::test_query_without_llm_uses_hybrid_scoring_to_lift_exact_operational_tokens -q</automated>
  </verify>
  <done>Local query results expose vector, lexical, metadata, graph, and final fusion diagnostics and promote the TraceId/Kafka target via explainable metadata/graph signals.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_rag.py tests/test_models.py -q` and inspect the regression test payload to ensure the target article rank and diagnostics are asserted, not only source count.
</verification>

<success_criteria>
- The Phase 11 graph defer state is replaced by an active local graph status when the graph artifact exists.
- The TraceId/Kafka regression proves `how-to-find-traceid-in-kafka-message.md` is explainably promoted by metadata/graph signals.
- Query diagnostics include vector/semantic, lexical, metadata, graph, and final fusion components.
</success_criteria>

<output>
After completion, create `.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-02-SUMMARY.md`.
</output>
