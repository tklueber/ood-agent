---
phase: 15-local-graph-metadata-retrieval-and-data-quality-review
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ood/knowledge_quality.py
  - tests/test_knowledge_quality.py
autonomous: true
requirements:
  - KNW-11
  - KNW-12
must_haves:
  truths:
    - "OOD can audit a Markdown corpus for retrieval readiness without indexing or Cloud LLM usage."
    - "The audit covers frontmatter, titles, keywords, service/type fields, Wikilinks, commands, freshness, duplicates/conflicts, and source attribution."
    - "The audit returns actionable recommendations rather than only raw counts."
  artifacts:
    - path: "src/ood/knowledge_quality.py"
      provides: "Local corpus quality audit contracts and recommendation engine"
      exports: ["audit_knowledge_corpus", "KnowledgeQualityReport"]
    - path: "tests/test_knowledge_quality.py"
      provides: "Coverage for quality dimensions and recommendations"
  key_links:
    - from: "src/ood/knowledge_quality.py"
      to: "Markdown/frontmatter files"
      via: "stdlib parser extracts metadata and relationship signals"
      pattern: "audit_knowledge_corpus"
---

<objective>
Build a deterministic source-data quality audit for the OOD-KB Markdown corpus.

Purpose: before graph/metadata boosts become trusted defaults, the user needs evidence about whether the 438 Obsidian OOD-KB articles have sufficient metadata, titles, keywords, service/type fields, Wikilinks, commands, and relationship signals.
Output: a pure local `knowledge_quality` module with structured report and recommendations.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-DISCOVERY.md
@src/ood/rag.py
@tests/test_rag.py

<interfaces>
Implement a new module so this can run independently from indexing. Use frozen dataclasses and `to_dict()` patterns consistent with `src/ood/evaluation.py` and `src/ood/evaluation_metrics.py`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define knowledge quality report contracts and corpus audit</name>
  <files>src/ood/knowledge_quality.py, tests/test_knowledge_quality.py</files>
  <behavior>
    - Auditing a temp Markdown corpus returns document count, field completeness, list-field coverage, link counts, command coverage, freshness findings, duplicate title/kb_id findings, and per-document issues.
    - The parser supports observed OOD-KB scalar frontmatter and indented list fields like `keywords_de` and `keywords_en`.
    - Empty/missing corpus returns a valid report with zero documents and a warning-style recommendation, not an exception.
  </behavior>
  <action>Create `src/ood/knowledge_quality.py` with frozen dataclasses such as `QualityIssue`, `QualityRecommendation`, `FieldCoverage`, `KnowledgeQualityReport`, each with `to_dict()`. Implement `audit_knowledge_corpus(corpus_dir: Path, *, expected_documents: int | None = None) -> KnowledgeQualityReport`. Analyze all `*.md` recursively and compute: required metadata coverage (`title`, `type`, `service` or `system`, `keywords`/`keywords_de`/`keywords_en`, `last_verified` or `last_synced`, `owner` when present), title quality, keyword/alias/synonym coverage, Wikilink in/out counts, commands section/risk marker presence, duplicate titles/kb_ids, stale `last_verified` or `last_synced`, source attribution (`confluence_url`, `snow_incidents`, `jira`, `quelle`). Keep the implementation stdlib-only and local.</action>
  <verify>
    <automated>uv run pytest tests/test_knowledge_quality.py::test_audit_reports_retrieval_readiness_dimensions -q</automated>
  </verify>
  <done>The audit module produces a stable structured report for retrieval-readiness dimensions required by KNW-11.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate actionable source-data recommendations</name>
  <files>src/ood/knowledge_quality.py, tests/test_knowledge_quality.py</files>
  <behavior>
    - A TraceId/Kafka-style article missing `aliases`, normalized `system`/`component`, `related`, `last_verified`, or `owner` receives targeted recommendations.
    - Recommendations include examples for synonyms such as `trace id`, `traceid`, `kafka message`, and `correlation id` when TraceId/Kafka signals are present.
    - Corpus-level recommendations identify low coverage dimensions and suggest normalized fields to add.
  </behavior>
  <action>Add recommendation rules to the audit: per-document recommendations for missing normalized operational fields (`system`, `component`, `problem_type`, `keywords`, `aliases`, `related`, `supersedes`, `last_verified`, `owner`), weak titles, missing links, missing source attribution, missing command risk markers, and stale verification. Add special synonym guidance when query terms/metadata include TraceId/Kafka/AKHQ/Ersatzgeschäft. Add corpus-level recommendations when coverage thresholds are low. Each recommendation must include `severity`, `path` or `corpus`, `reason`, and `suggestion` text that the user can apply to source Markdown.</action>
  <verify>
    <automated>uv run pytest tests/test_knowledge_quality.py::test_traceid_article_recommendations_are_actionable tests/test_knowledge_quality.py::test_corpus_level_recommendations_flag_low_metadata_coverage -q</automated>
  </verify>
  <done>The report tells the user exactly what to improve in source data, not just that quality is low.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_knowledge_quality.py -q` and confirm all report dictionaries are JSON-serializable.
</verification>

<success_criteria>
- The quality audit works on synthetic tests without indexing, Cloud LLM, or external services.
- It covers all Phase 15 requested dimensions: frontmatter, titles, keywords, service/type, Wikilinks, commands, and relationship signals.
- It emits actionable source-data improvement recommendations for KNW-12.
</success_criteria>

<output>
After completion, create `.planning/phases/15-local-graph-metadata-retrieval-and-data-quality-review/15-04-SUMMARY.md`.
</output>
