---
phase: 16-rag-backed-ood-skill-llm-synthesis-and-learning-loop
plan: 03
type: execute
wave: 2
depends_on: [16-01]
files_modified: [src/ood/incident_synthesis.py, tests/test_incident_synthesis.py]
autonomous: true
requirements: [LLM-02]
must_haves:
  truths:
    - "Privacy-gated LLM synthesis only runs when `Settings.can_use_cloud_llm` is true."
    - "German solution proposals remain grounded in retrieved source excerpts and citations."
    - "Deterministic routing, identifiers, confidence, command risks, and uncertainties are preserved around synthesis."
  artifacts:
    - path: "src/ood/incident_synthesis.py"
      provides: "Grounded incident proposal builder over QueryResult"
      exports: ["IncidentSolutionProposal", "build_incident_solution_proposal"]
    - path: "tests/test_incident_synthesis.py"
      provides: "Privacy gate and grounding tests"
  key_links:
    - from: "src/ood/incident_synthesis.py"
      to: "src/ood/config.py"
      via: "Settings.can_use_cloud_llm"
      pattern: "can_use_cloud_llm"
    - from: "src/ood/incident_synthesis.py"
      to: "src/ood/models.py"
      via: "QueryResult.to_dict-compatible fields"
      pattern: "QueryResult"
---

<objective>
Add a focused, testable synthesis layer that turns `QueryResult` into an operational German solution proposal while preserving the privacy gate.

Purpose: The skill needs better proposal formatting and optional Cloud LLM synthesis without weakening deterministic RAG/routing contracts.
Output: `incident_synthesis` module with tests for no-LLM, LLM-approved, and citation-preservation paths.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-RESEARCH.md
@.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-01-SUMMARY.md
@src/ood/config.py
@src/ood/models.py
@src/ood/rag.py

<interfaces>
Existing contracts:
```python
class Settings(BaseSettings):
    @property
    def can_use_cloud_llm(self) -> bool: ...

@dataclass(frozen=True)
class QueryResult:
    query: str
    answer: str | None
    confidence: ConfidenceScore
    sources: list[SourceHit]
    llm_used: bool
    status: str
    analysis: TicketAnalysis
    retrieval_diagnostics: RetrievalDiagnostics
```

New contract target:
```python
@dataclass(frozen=True)
class IncidentSolutionProposal:
    suggestion_id: str
    synthesis_mode: str  # "local_extractive" | "llm_grounded" | "no_results"
    proposal: str | None
    citations: list[dict[str, object]]
    confidence: dict[str, object]
    llm_used: bool
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build local grounded proposal formatter</name>
  <files>src/ood/incident_synthesis.py, tests/test_incident_synthesis.py</files>
  <read_first>src/ood/models.py, src/ood/rag.py, src/ood/config.py</read_first>
  <behavior>
    - Test 1: QueryResult with answer and two sources yields `IncidentSolutionProposal.synthesis_mode == "local_extractive"`, `llm_used is False`, and citations with source paths and scores.
    - Test 2: QueryResult with no sources yields `synthesis_mode == "no_results"` and `proposal is None`.
    - Test 3: `suggestion_id` is deterministic for same query plus source paths.
  </behavior>
  <action>Create `src/ood/incident_synthesis.py` with frozen dataclass `IncidentSolutionProposal`, `to_dict()`, and `build_incident_solution_proposal(query_result: QueryResult, settings: Settings) -> IncidentSolutionProposal`. For now, use `query_result.answer` and top sources directly; do not call any Cloud API in Task 1. Include citations as `[{'path': source.path, 'score': source.score, 'excerpt': source.excerpt[:500]}]`. Generate `suggestion_id` as `sha256(query + joined source paths + answer).hexdigest()[:16]`. Set mode `local_extractive` when `query_result.answer` exists and `query_result.llm_used` is false, `llm_grounded` when `query_result.llm_used` is true, and `no_results` when no answer/sources exist.</action>
  <verify>
    <automated>uv run pytest tests/test_incident_synthesis.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/incident_synthesis.py` contains `class IncidentSolutionProposal` and `def build_incident_solution_proposal(`.
    - Test file asserts deterministic `suggestion_id` length 16.
    - Test file asserts citations contain source path and score.
  </acceptance_criteria>
  <done>Local and existing LLM-backed QueryResult answers can be normalized into stable proposal artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce operational privacy gate invariants</name>
  <files>src/ood/incident_synthesis.py, tests/test_incident_synthesis.py</files>
  <read_first>src/ood/incident_synthesis.py, src/ood/config.py, src/ood/rag.py, tests/test_rag.py</read_first>
  <behavior>
    - Test 1: When `settings.can_use_cloud_llm` is false, synthesis output contains `llm_allowed: False` and does not change `query_result.llm_used`.
    - Test 2: When `settings.can_use_cloud_llm` is true and `query_result.llm_used` is true, output mode remains `llm_grounded` and preserves all deterministic fields from `query_result.analysis.to_dict()` under `analysis`.
    - Test 3: Source inspection verifies `incident_synthesis.py` does not reference `has_llm_credentials`.
  </behavior>
  <action>Extend `IncidentSolutionProposal` with `llm_allowed: bool`, `analysis: dict[str, object]`, `retrieval_diagnostics: dict[str, object]`, and `uncertainties: list[str]`. Set `llm_allowed` from `settings.can_use_cloud_llm`. Do not consult `settings.has_llm_credentials` anywhere in this module. Preserve `query_result.analysis.to_dict()`, `query_result.confidence.to_dict()`, and `query_result.retrieval_diagnostics.to_dict()` verbatim in `to_dict()` so downstream skill/CLI output keeps deterministic routing, identifiers, command risks, confidence, and uncertainties.</action>
  <verify>
    <automated>uv run pytest tests/test_incident_synthesis.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/incident_synthesis.py` contains `can_use_cloud_llm` and does not contain `has_llm_credentials`.
    - `IncidentSolutionProposal.to_dict()` includes `analysis`, `retrieval_diagnostics`, `confidence`, `llm_allowed`, and `llm_used`.
  </acceptance_criteria>
  <done>Operational synthesis preserves the existing privacy boundary and all deterministic evidence fields.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_incident_synthesis.py -q` and `uv run pytest tests/test_rag.py tests/test_models.py -q`.
</verification>

<success_criteria>
LLM-02 is covered by a privacy-gated proposal layer that can consume existing RAG answers and expose grounded citations plus preserved deterministic analysis.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-03-SUMMARY.md`.
</output>
