---
phase: 16-rag-backed-ood-skill-llm-synthesis-and-learning-loop
plan: 04
type: execute
wave: 2
depends_on: [16-01]
files_modified: [src/ood/learning.py, tests/test_learning.py]
autonomous: true
requirements: [FB-02, LEARN-02, KNW-13]
must_haves:
  truths:
    - "Every incident suggestion can be rated immediately with usefulness, correctness, routing accuracy, and missing evidence."
    - "A later actual resolution can be linked to the original suggestion ID."
    - "Feedback and resolution notes become review-required knowledge proposals, not automatically indexed truth."
  artifacts:
    - path: "src/ood/learning.py"
      provides: "Feedback, resolution, and knowledge-proposal artifact helpers"
      exports: ["record_feedback", "record_actual_resolution", "create_knowledge_update_proposal"]
    - path: "tests/test_learning.py"
      provides: "Artifact write/read and review-gate tests"
  key_links:
    - from: "src/ood/learning.py"
      to: "data/feedback"
      via: "JSON artifact storage"
      pattern: "feedback"
    - from: "src/ood/learning.py"
      to: "review_status"
      via: "knowledge proposal gate"
      pattern: "pending"
---

<objective>
Create the artifact-based learning loop foundation for immediate feedback, asynchronous actual resolutions, and reviewed knowledge-update proposals.

Purpose: Phase 16 should learn from support outcomes without forcing users to hand-author frontmatter or silently poisoning the indexed KB.
Output: `learning.py` with deterministic JSON/Markdown-like proposal artifacts and tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-RESEARCH.md
@.planning/phases/14-baseline-feedback-loop-and-review-gate/14-03-SUMMARY.md
@src/ood/eval_baseline.py
@src/ood/config.py

<interfaces>
Follow Phase 14 pattern: artifact helpers are pure stdlib, explicit, and review-gated. New target functions:

```python
def record_feedback(*, data_dir: Path, suggestion_id: str, solved: bool, useful: int, correct: int, routing_correct: bool, missing_evidence: str | None) -> Path: ...
def record_actual_resolution(*, data_dir: Path, suggestion_id: str, resolution_text: str, resolver: str | None, source_ticket: str | None) -> Path: ...
def create_knowledge_update_proposal(*, data_dir: Path, suggestion_id: str) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Record immediate feedback artifacts</name>
  <files>src/ood/learning.py, tests/test_learning.py</files>
  <read_first>src/ood/eval_baseline.py, src/ood/config.py, .gitignore</read_first>
  <behavior>
    - Test 1: `record_feedback(data_dir=tmp_path, suggestion_id="abc123", solved=True, useful=5, correct=4, routing_correct=True, missing_evidence="")` writes JSON under `tmp_path/feedback/abc123.feedback.json`.
    - Test 2: Feedback JSON contains `schema_version: 1`, `suggestion_id`, `solved`, `useful`, `correct`, `routing_correct`, `missing_evidence`, and ISO timestamp ending in `Z`.
    - Test 3: useful/correct outside 1..5 raises `ValueError`.
  </behavior>
  <action>Create `src/ood/learning.py` with pure stdlib helpers. Implement `record_feedback` to create `data_dir / "feedback"`, validate `useful` and `correct` as integers 1 through 5, write UTF-8 JSON with `ensure_ascii=False`, `indent=2`, and return the written path. Use schema version `1` and timestamp key `recorded_at`. Do not add dependencies or touch git-tracked `data/` contents.</action>
  <verify>
    <automated>uv run pytest tests/test_learning.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/learning.py` contains `def record_feedback(`.
    - `tests/test_learning.py` asserts path suffix `feedback/abc123.feedback.json`.
    - `tests/test_learning.py` asserts invalid ratings raise `ValueError`.
  </acceptance_criteria>
  <done>Immediate quality feedback can be captured as a deterministic local artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Link actual resolutions and create reviewed knowledge proposals</name>
  <files>src/ood/learning.py, tests/test_learning.py</files>
  <read_first>src/ood/learning.py, tests/test_learning.py, docs/ood-kb-source-quality.md</read_first>
  <behavior>
    - Test 1: `record_actual_resolution` writes `tmp_path/resolutions/abc123.resolution.json` with `suggestion_id`, `resolution_text`, `resolver`, and `source_ticket`.
    - Test 2: `create_knowledge_update_proposal(data_dir=tmp_path, suggestion_id="abc123")` reads feedback/resolution artifacts and writes `tmp_path/knowledge-proposals/abc123.proposal.json`.
    - Test 3: Proposal JSON contains `review_status: "pending"`, `proposed_frontmatter`, `proposed_body`, `source_artifacts`, and never claims `approved` by default.
  </behavior>
  <action>Extend `learning.py` with `record_actual_resolution` and `create_knowledge_update_proposal`. The proposal should include normalized frontmatter keys `title`, `type`, `status`, `system`, `komponente`, `keywords`, `aliases`, `related`, `owner`, `last_verified`, and `source_constraints`, with unknown values represented as empty strings/lists rather than omitted. Build `proposed_body` with headings `## Problem`, `## Tatsächliche Lösung`, `## Evidenz`, and `## Review-Notizen`. Set `review_status` to exactly `pending` and include `source_artifacts` paths for the linked feedback/resolution files. Do not write into `knowledge/` and do not invoke `ood index` or `ood update`.</action>
  <verify>
    <automated>uv run pytest tests/test_learning.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/learning.py` contains `def record_actual_resolution(` and `def create_knowledge_update_proposal(`.
    - Proposal test asserts `review_status == "pending"`.
    - `src/ood/learning.py` does not contain `RagEngine(` or `index_markdown`.
  </acceptance_criteria>
  <done>Actual resolutions can become review-required knowledge proposals without automatic indexing.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_learning.py -q`.
</verification>

<success_criteria>
FB-02, LEARN-02, and KNW-13 have a local artifact foundation that captures ratings, links real resolutions, and creates pending knowledge proposals.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-04-SUMMARY.md`.
</output>
