---
phase: 16-rag-backed-ood-skill-llm-synthesis-and-learning-loop
plan: 05
type: execute
wave: 3
depends_on: [16-02, 16-03, 16-04]
files_modified: [src/ood/cli.py, tests/test_cli.py, README.md, docs/ood-learning-loop.md]
autonomous: true
requirements: [SKILL-01, SKILL-02, LLM-02, ROUTE-01, FB-02, LEARN-02, KNW-13]
must_haves:
  truths:
    - "User can run one `ood incident` command that routes first, then queries/synthesizes only when appropriate."
    - "User can record immediate feedback and later actual resolution from CLI commands."
    - "User can create reviewed knowledge-update proposals from linked feedback/resolution artifacts."
  artifacts:
    - path: "src/ood/cli.py"
      provides: "Incident, feedback, resolution, and proposal CLI commands"
    - path: "tests/test_cli.py"
      provides: "End-to-end CLI coverage for Phase 16 commands"
    - path: "docs/ood-learning-loop.md"
      provides: "Operational learning-loop workflow"
    - path: "README.md"
      provides: "Phase 16 CLI workflow documentation"
  key_links:
    - from: "src/ood/cli.py"
      to: "src/ood/incident.py"
      via: "route before RagEngine.query"
      pattern: "route_operational_incident"
    - from: "src/ood/cli.py"
      to: "src/ood/incident_synthesis.py"
      via: "build proposal from QueryResult"
      pattern: "build_incident_solution_proposal"
    - from: "src/ood/cli.py"
      to: "src/ood/learning.py"
      via: "feedback/resolution/proposal commands"
      pattern: "record_feedback|record_actual_resolution|create_knowledge_update_proposal"
---

<objective>
Wire the Phase 16 operational workflow into the CLI and documentation.

Purpose: The RAG-backed skill needs a single command surface it can call, plus feedback/resolution commands that close the learning loop.
Output: `ood incident`, feedback, resolution, and proposal CLI commands with README/docs coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-02-SUMMARY.md
@.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-03-SUMMARY.md
@.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-04-SUMMARY.md
@src/ood/cli.py
@tests/test_cli.py
@README.md

<interfaces>
CLI patterns to preserve:
```python
@app.command()
def query(ticket_text: str, json_output: JsonOption = False, ...): ...

def _handle_error(error: Exception, *, verbose: bool) -> None: ...
def _load_valid_settings(...): ...
```

New commands target:
```bash
ood incident "<ticket>" --json
ood feedback <suggestion-id> --solved true --useful 5 --correct 4 --routing-correct true --missing-evidence "..."
ood resolution <suggestion-id> --resolution-text "..." --resolver "..." --source-ticket "..."
ood knowledge-proposal <suggestion-id>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `ood incident` route-first CLI command</name>
  <files>src/ood/cli.py, tests/test_cli.py</files>
  <read_first>src/ood/cli.py, tests/test_cli.py, src/ood/incident.py, src/ood/incident_synthesis.py, src/ood/rag.py</read_first>
  <behavior>
    - Test 1: `ood incident "Police 15.456.789 Fehler" --json` exits 0, returns JSON with `routing.should_forward=true`, `routing.target_team="PKV"`, and no `proposal` requiring RAG query.
    - Test 2: non-forwarded incident calls `RagEngine.query`, returns `proposal.suggestion_id`, `proposal.citations`, `feedback.prompt`, and `routing.continue_to_solution=true`.
    - Test 3: `IndexMissingError` maps to exit code 1 with existing query-style error behavior.
  </behavior>
  <action>Add `@app.command("incident")` to `src/ood/cli.py`. It must load settings with the same options as `query`, call `route_operational_incident(ticket_text)` first, and if `continue_to_solution` is false, emit only routing plus a German hand-off message. If `continue_to_solution` is true, call `RagEngine(settings).query(ticket_text)`, pass the result to `build_incident_solution_proposal(result, settings)`, and include a feedback object with prompt text `Bewerte diesen Vorschlag mit: ood feedback {suggestion_id} --solved true|false --useful 1-5 --correct 1-5 --routing-correct true|false`. For JSON output, emit keys `command`, `routing`, `proposal`, and `feedback`. For human output, use German sections `Weiterleitung`, `Lösungsvorschlag`, `Quellen`, `Risiken`, `Unsicherheiten`, and `Feedback`.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/cli.py` contains `@app.command("incident")` and `route_operational_incident`.
    - JSON CLI test asserts forwarded PKV route does not instantiate/query RagEngine.
    - Human output test asserts `Feedback` appears for a non-forwarded successful proposal.
  </acceptance_criteria>
  <done>The skill has one route-first CLI command to call for operational incident responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add feedback, resolution, and knowledge proposal CLI commands</name>
  <files>src/ood/cli.py, tests/test_cli.py</files>
  <read_first>src/ood/cli.py, tests/test_cli.py, src/ood/learning.py</read_first>
  <behavior>
    - Test 1: `ood feedback abc123 --solved true --useful 5 --correct 4 --routing-correct true --missing-evidence "" --data-dir tmp` writes feedback JSON and prints path.
    - Test 2: `ood resolution abc123 --resolution-text "Kafka Offset neu gelesen" --resolver "Timo" --source-ticket "INC001" --data-dir tmp` writes linked resolution JSON.
    - Test 3: `ood knowledge-proposal abc123 --data-dir tmp --json` writes proposal JSON with `review_status="pending"` and prints compact JSON.
  </behavior>
  <action>Add CLI commands `feedback`, `resolution`, and `knowledge-proposal` that call `record_feedback`, `record_actual_resolution`, and `create_knowledge_update_proposal`. Use existing `DataDirOption`, `JsonOption`, `QuietOption`, and `VerboseOption` patterns where applicable. Output German human confirmations: `Feedback gespeichert: {path}`, `Ist-Lösung gespeichert: {path}`, and `Knowledge-Vorschlag erstellt: {path}`. JSON output should include `command`, `path`, and for proposal command the loaded proposal payload.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py tests/test_learning.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/cli.py` contains `@app.command("feedback")`, `@app.command("resolution")`, and `@app.command("knowledge-proposal")`.
    - CLI tests assert artifact files exist under tmp `feedback`, `resolutions`, and `knowledge-proposals` directories.
    - Proposal CLI JSON contains `"review_status":"pending"` or equivalent parsed JSON assertion.
  </acceptance_criteria>
  <done>Support users can rate, attach actual resolutions, and generate reviewed knowledge proposals from the CLI.</done>
</task>

<task type="auto">
  <name>Task 3: Document the complete operational learning loop</name>
  <files>README.md, docs/ood-learning-loop.md, docs/ood-rag-skill.md</files>
  <read_first>README.md, docs/ood-rag-skill.md, src/ood/cli.py</read_first>
  <action>Create `docs/ood-learning-loop.md` with exact commands for: reindexing the OOD-KB, running `ood incident`, recording `ood feedback`, recording `ood resolution`, creating `ood knowledge-proposal`, manually reviewing the proposal, and only then copying approved Markdown/frontmatter into the knowledge corpus before `ood update`. Update README and `docs/ood-rag-skill.md` to link this workflow. Explicitly state that Phase 16 does not write approved knowledge into `knowledge/` automatically and does not execute source commands.</action>
  <verify>
    <automated>grep -q "ood knowledge-proposal" docs/ood-learning-loop.md && grep -q "docs/ood-learning-loop.md" README.md && grep -q "ood feedback" docs/ood-rag-skill.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ood-learning-loop.md` contains command examples for `ood incident`, `ood feedback`, `ood resolution`, and `ood knowledge-proposal`.
    - `docs/ood-learning-loop.md` contains the exact phrase `nicht automatisch indexiert`.
    - README links `docs/ood-learning-loop.md`.
  </acceptance_criteria>
  <done>Operators have an executable documented workflow from incident response through reviewed KB improvement.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_cli.py tests/test_learning.py tests/test_incident.py tests/test_incident_synthesis.py -q` and `uv run ood --help` to confirm the new commands are listed.
</verification>

<success_criteria>
All Phase 16 requirements are connected through one operational CLI/skill workflow with privacy-gated synthesis, route-first hand-off, feedback capture, actual resolution learning, and review-required knowledge proposal generation.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-05-SUMMARY.md`.
</output>
