---
phase: 16-rag-backed-ood-skill-llm-synthesis-and-learning-loop
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: [.claude/skills/ood-agent-rag/SKILL.md, docs/ood-rag-skill.md, README.md]
autonomous: true
requirements: [SKILL-01, SKILL-02, ROUTE-01]
must_haves:
  truths:
    - "User can install or review a project-local OOD RAG skill derived from the existing template."
    - "The skill calls the Python RAG CLI as canonical source instead of reading `_index.md`."
    - "The skill instruction runs routing first and only continues to RAG for OOD-handled incidents."
  artifacts:
    - path: ".claude/skills/ood-agent-rag/SKILL.md"
      provides: "Installable project skill prompt for OOD incident support"
      contains: "uv run ood incident"
    - path: "docs/ood-rag-skill.md"
      provides: "Skill installation and operator guide"
    - path: "README.md"
      provides: "Phase 16 skill usage pointer"
  key_links:
    - from: ".claude/skills/ood-agent-rag/SKILL.md"
      to: "uv run ood incident"
      via: "Bash command instruction"
      pattern: "uv run ood incident"
---

<objective>
Package the RAG-backed OOD skill prompt as a project artifact derived from the existing OOD-KB skill template.

Purpose: Operators need a direct chat/skill entry point that delegates retrieval to the project CLI instead of duplicating manual `_index.md` matching logic.
Output: Project-local skill prompt, installation guide, and README usage pointer.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-RESEARCH.md
@.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-01-SUMMARY.md
@/Users/timo/obsidian/secondBrain/Arbeit/06_OOD-KB/ood-agent/SKILL.md
@/Users/timo/obsidian/secondBrain/Arbeit/06_OOD-KB/ood-agent/README.md
@README.md

<interfaces>
Plan 01 provides:
```python
def route_operational_incident(ticket_text: str) -> IncidentRoutingDecision
```

Planned CLI command consumed by this skill and implemented in Plan 05:
```bash
uv run ood incident "<ticket text>" --storage-dir data/ood-kb-storage --json
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project-local RAG skill prompt</name>
  <files>.claude/skills/ood-agent-rag/SKILL.md</files>
  <read_first>/Users/timo/obsidian/secondBrain/Arbeit/06_OOD-KB/ood-agent/SKILL.md, .planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-01-SUMMARY.md</read_first>
  <action>Create `.claude/skills/ood-agent-rag/SKILL.md` with YAML frontmatter `name: ood-agent-rag`, German description triggers copied/adapted from the existing template, and `allowed-tools: Bash`. The body must instruct the skill to work in `/Users/timo/01_dev/projects/ood-agent`, run `uv run ood incident "<Incident-Text>" --storage-dir data/ood-kb-storage --json`, parse the JSON fields `routing`, `answer`, `confidence`, `sources`, `analysis.command_risks`, `analysis.uncertainties`, `feedback`, and never read `_index.md` as canonical search. Include the existing calendar links for PKV, KMU, Frontend.MF, OAPS, ZEPAS, ProspectZone/KUPO, Conversational/Notification, and SWAD/WAF as fallback information. State explicitly: no source commands are executed, and ticket content is sent to Cloud LLM only when project config has `OOD_ALLOW_CLOUD_LLM=true` and credentials.</action>
  <verify>
    <automated>test -f .claude/skills/ood-agent-rag/SKILL.md && grep -q "uv run ood incident" .claude/skills/ood-agent-rag/SKILL.md && grep -q "_index.md" .claude/skills/ood-agent-rag/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/ood-agent-rag/SKILL.md` contains `allowed-tools: Bash`.
    - `.claude/skills/ood-agent-rag/SKILL.md` contains `uv run ood incident` and `--json`.
    - `.claude/skills/ood-agent-rag/SKILL.md` contains `nie _index.md als kanonische Suche` or equivalent German wording.
  </acceptance_criteria>
  <done>Installable skill prompt exists and delegates canonical search to the CLI.</done>
</task>

<task type="auto">
  <name>Task 2: Document installation and operator workflow</name>
  <files>docs/ood-rag-skill.md, README.md</files>
  <read_first>README.md, /Users/timo/obsidian/secondBrain/Arbeit/06_OOD-KB/ood-agent/README.md, .claude/skills/ood-agent-rag/SKILL.md</read_first>
  <action>Create `docs/ood-rag-skill.md` with sections `## Installation`, `## Einmaliger Index-Aufbau`, `## Incident-Nutzung`, `## Datenschutz`, and `## Feedback`. Include exact setup command `uv run ood reindex --knowledge-dir /Users/timo/obsidian/secondBrain/Arbeit/06_OOD-KB/articles --storage-dir data/ood-kb-storage --json` and exact query command `uv run ood incident "TraceId Kafka AKHQ Ersatzgeschäft" --storage-dir data/ood-kb-storage --json`. Update README with a short `## OOD RAG Skill` section linking to `docs/ood-rag-skill.md` and `.claude/skills/ood-agent-rag/SKILL.md`.</action>
  <verify>
    <automated>grep -q "## OOD RAG Skill" README.md && grep -q "uv run ood reindex --knowledge-dir /Users/timo/obsidian/secondBrain/Arbeit/06_OOD-KB/articles --storage-dir data/ood-kb-storage --json" docs/ood-rag-skill.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ood-rag-skill.md` contains all five required section headers.
    - `README.md` contains `docs/ood-rag-skill.md` and `.claude/skills/ood-agent-rag/SKILL.md`.
  </acceptance_criteria>
  <done>Operator can install and use the skill without reading the old template.</done>
</task>

</tasks>

<verification>
Run `test -f .claude/skills/ood-agent-rag/SKILL.md` and the grep checks above.
</verification>

<success_criteria>
The project contains a ready-to-install skill artifact that explicitly uses the CLI-backed multi-level incident workflow and preserves privacy/routing guardrails.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rag-backed-ood-skill-llm-synthesis-and-learning-loop/16-02-SUMMARY.md`.
</output>
