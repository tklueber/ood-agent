---
phase: 03-knowledge-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ood/models.py
  - src/ood/rag.py
  - tests/test_models.py
  - tests/test_rag.py
autonomous: true
requirements: [KNW-02, KNW-04, KNW-05]
must_haves:
  truths:
    - "Markdown documents can be parsed into body text plus metadata without sending YAML frontmatter to retrieval."
    - "Required metadata warnings are produced without blocking indexing."
    - "Manifest-capable result contracts serialize deterministic diagnostics for later CLI JSON output."
  artifacts:
    - path: "src/ood/models.py"
      provides: "Knowledge metadata, manifest, duplicate, and update result dataclass contracts"
      exports: ["MetadataWarning", "DuplicateGroup", "ManifestEntry", "ManifestDiff", "UpdateResult"]
    - path: "src/ood/rag.py"
      provides: "Frontmatter parsing, body normalization, hash helpers, and manifest path constants"
      contains: "MANIFEST_SCHEMA_VERSION"
    - path: "tests/test_models.py"
      provides: "Serialization tests for Phase 3 result contracts"
    - path: "tests/test_rag.py"
      provides: "Parsing and metadata warning tests"
  key_links:
    - from: "src/ood/rag.py"
      to: "src/ood/models.py"
      via: "imports manifest and warning dataclasses"
      pattern: "from ood\.models import .*ManifestEntry"
    - from: "src/ood/rag.py"
      to: "LightRAG document input"
      via: "frontmatter-stripped document body"
      pattern: "body_text"
---

<objective>
Create the Phase 3 contracts and parsing primitives that all knowledge-management work builds on.

Purpose: Establish stable, testable OOD-owned data shapes for manifest diagnostics and warning-based metadata validation before implementing indexing/update behavior.
Output: Dataclass contracts, frontmatter/body parsing helpers, content hash helpers, and tests proving metadata warnings are non-blocking.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-knowledge-management/03-CONTEXT.md
@.planning/phases/02-core-rag-engine/02-01-SUMMARY.md
@.planning/phases/02-core-rag-engine/02-02-SUMMARY.md
@src/ood/models.py
@src/ood/rag.py
@tests/test_models.py
@tests/test_rag.py

<interfaces>
Existing contracts to preserve:

From `src/ood/models.py`:
```python
@dataclass(frozen=True)
class IndexResult:
    status: str
    indexed_documents: int
    skipped_documents: int
    storage_dir: Path
    message: str
    def to_dict(self) -> dict[str, str | int]: ...
```

From `src/ood/rag.py`:
```python
FALLBACK_INDEX_FILENAME = "ood-fallback-index.json"
class RagEngine:
    def index_markdown(self) -> IndexResult: ...
    def reindex_markdown(self) -> IndexResult: ...
    def _load_markdown_documents(self) -> tuple[list[str], list[str], int]: ...
```

New contracts to create in this plan for downstream plans:
```python
MANIFEST_SCHEMA_VERSION = 1
MANIFEST_FILENAME = "ood-manifest.json"
REQUIRED_METADATA_FIELDS = ("quelle", "datum", "status", "system", "komponente", "title", "type")
VALID_STATUS_VALUES = ("active", "deprecated", "draft")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add manifest and update result contracts</name>
  <files>src/ood/models.py, tests/test_models.py</files>
  <read_first>src/ood/models.py, tests/test_models.py, .planning/phases/03-knowledge-management/03-CONTEXT.md</read_first>
  <behavior>
    - `ManifestEntry.to_dict()` returns only JSON-serializable values: `path`, `content_hash`, `body_hash`, `metadata`, `indexed_at`, `warnings`, `body_excerpt`, and `duplicate_group_ids`.
    - `MetadataWarning.to_dict()` returns `path`, `field`, and `message`.
    - `DuplicateGroup.to_dict()` returns `group_id`, `kind`, `canonical_path`, `paths`, and `score`.
    - `ManifestDiff.to_dict()` returns `new_paths`, `changed_paths`, `unchanged_paths`, `deleted_paths`, and `skipped_paths`.
    - `UpdateResult.to_dict()` returns stable CLI JSON fields including `status`, `indexed_documents`, `skipped_documents`, `storage_dir`, `manifest_path`, `message`, `diff`, `metadata_warnings`, `duplicate_groups`, and `schema_version`.
  </behavior>
  <action>
    Implement Phase 3 frozen dataclasses in `src/ood/models.py` without removing existing `IndexResult`, `QueryResult`, `SourceHit`, `ConfidenceScore`, or `IndexMissingError` exports.

    Add:
    - `MetadataWarning(path: str, field: str, message: str)` with `to_dict() -> dict[str, str]`.
    - `DuplicateGroup(group_id: str, kind: str, canonical_path: str, paths: list[str], score: float)` with `to_dict() -> dict[str, object]`. `kind` values used by later plans are exactly `"exact"` and `"near"`.
    - `ManifestEntry(path: str, content_hash: str, body_hash: str, metadata: dict[str, str], indexed_at: str, warnings: list[MetadataWarning], body_excerpt: str, duplicate_group_ids: list[str])` with `to_dict() -> dict[str, Any]`.
    - `ManifestDiff(new_paths: list[str], changed_paths: list[str], unchanged_paths: list[str], deleted_paths: list[str], skipped_paths: list[str])` with `to_dict() -> dict[str, list[str]]`.
    - `UpdateResult(status: str, indexed_documents: int, skipped_documents: int, storage_dir: Path, manifest_path: Path, message: str, diff: ManifestDiff, metadata_warnings: list[MetadataWarning], duplicate_groups: list[DuplicateGroup], schema_version: int)` with `to_dict() -> dict[str, Any]`.

    Extend `IndexResult` with optional fields needed for indexing diagnostics while preserving existing constructor calls by using dataclass defaults after `message`: `metadata_warnings: list[MetadataWarning] = field(default_factory=list)`, `duplicate_groups: list[DuplicateGroup] = field(default_factory=list)`, and `manifest_path: Path | None = None`. Update `IndexResult.to_dict()` to include `metadata_warnings`, `duplicate_groups`, and `manifest_path` only when non-empty/non-None so existing Phase 2 JSON tests remain valid.

    Export all new classes through `__all__`. This implements D-14 and D-15 by creating a rich manifest/update diagnostics contract, and D-05 by modeling warnings rather than blocking errors.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/models.py` contains `class MetadataWarning`.
    - `src/ood/models.py` contains `class ManifestEntry`.
    - `src/ood/models.py` contains `class ManifestDiff`.
    - `src/ood/models.py` contains `class UpdateResult`.
    - `src/ood/models.py` contains `metadata_warnings: list[MetadataWarning] = field(default_factory=list)`.
    - `tests/test_models.py` contains `test_update_result_serializes_manifest_diagnostics`.
    - `uv run pytest tests/test_models.py -q` exits 0.
  </acceptance_criteria>
  <done>Phase 3 result and manifest contracts serialize deterministically and existing Phase 2 model tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add frontmatter parsing and hash primitives</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <read_first>src/ood/rag.py, src/ood/models.py, tests/test_rag.py, .planning/phases/03-knowledge-management/03-CONTEXT.md</read_first>
  <behavior>
    - Markdown beginning with `---` frontmatter is split into `metadata` and `body_text`; only `body_text` is returned for indexing.
    - Missing required fields `quelle`, `datum`, `status`, `system`, `komponente`, `title`, and `type` create `MetadataWarning` objects but do not skip the document.
    - `status` values other than `active`, `deprecated`, and `draft` create a warning.
    - Plain Markdown without frontmatter remains indexable and receives missing-field warnings.
    - Content hashes are SHA-256 hex strings; `content_hash` uses full raw file content, `body_hash` uses normalized body text.
  </behavior>
  <action>
    Update `src/ood/rag.py` with parsing primitives while preserving `RagEngine.index_markdown()` behavior for existing tests.

    Add constants near the existing filename constants:
    - `MANIFEST_SCHEMA_VERSION = 1`
    - `MANIFEST_FILENAME = "ood-manifest.json"`
    - `REQUIRED_METADATA_FIELDS = ("quelle", "datum", "status", "system", "komponente", "title", "type")`
    - `VALID_STATUS_VALUES = ("active", "deprecated", "draft")`

    Add a small internal parsed-document representation, either a private frozen dataclass `_ParsedMarkdownDocument` or an internal typed dict, with fields: `path: Path`, `relative_path: str`, `raw_content: str`, `body_text: str`, `metadata: dict[str, str]`, `warnings: list[MetadataWarning]`, `content_hash: str`, `body_hash: str`, `body_excerpt: str`.

    Implement helper methods on `RagEngine`:
    - `_manifest_path(self) -> Path` returning `self.settings.storage_dir / MANIFEST_FILENAME`.
    - `_parse_markdown_document(self, path: Path) -> _ParsedMarkdownDocument`.
    - `_split_frontmatter(self, content: str) -> tuple[dict[str, str], str]` using a simple YAML-scalar parser only: if content starts with `---\n`, read lines until the next line equal to `---`; parse `key: value` pairs as lowercase keys with stripped string values and remove optional single/double quotes around values. Do not add a new dependency.
    - `_metadata_warnings(self, relative_path: str, metadata: dict[str, str]) -> list[MetadataWarning]`.
    - `_sha256(text: str) -> str` and `_normalize_body(text: str) -> str`.

    Update `_load_markdown_documents()` to call a new `_load_parsed_markdown_documents()` and append `parsed.body_text` rather than raw `content`. Empty body text still counts as skipped. This implements D-05, D-06, D-07, D-08, D-13, and prepares D-14.
  </action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_frontmatter_is_stripped_before_indexing tests/test_rag.py::test_metadata_warnings_are_non_blocking -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/rag.py` contains `MANIFEST_SCHEMA_VERSION = 1`.
    - `src/ood/rag.py` contains `MANIFEST_FILENAME = "ood-manifest.json"`.
    - `src/ood/rag.py` contains `REQUIRED_METADATA_FIELDS` with all seven required lowercase field names.
    - `src/ood/rag.py` contains `VALID_STATUS_VALUES = ("active", "deprecated", "draft")`.
    - `src/ood/rag.py` does not contain `import yaml`.
    - `tests/test_rag.py` contains `test_frontmatter_is_stripped_before_indexing`.
    - `tests/test_rag.py` contains `test_metadata_warnings_are_non_blocking`.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Markdown parsing strips frontmatter from indexed text and records warning-only metadata diagnostics with deterministic hashes.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_models.py tests/test_rag.py -q` and confirm existing Phase 2 indexing tests still pass with frontmatter-aware loading.
</verification>

<success_criteria>
- New dataclass contracts exist for manifest/update diagnostics.
- Frontmatter metadata validation is warning-based and non-blocking per D-05.
- Required fields and status values exactly match D-06 and D-07.
- Indexed document text uses body text without YAML frontmatter per D-08.
</success_criteria>

<output>
After completion, create `.planning/phases/03-knowledge-management/03-01-SUMMARY.md`.
</output>
