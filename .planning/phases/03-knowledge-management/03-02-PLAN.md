---
phase: 03-knowledge-management
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/ood/models.py
  - src/ood/rag.py
  - tests/test_rag.py
autonomous: true
requirements: [KNW-02, KNW-03, KNW-04, KNW-05]
must_haves:
  truths:
    - "Indexing and reindexing maintain a JSON manifest under storage_dir."
    - "Manifest entries include metadata, hashes, warnings, body excerpts, and duplicate diagnostics."
    - "Exact and near duplicates are reported without blocking indexing or skipping duplicate documents."
  artifacts:
    - path: "src/ood/rag.py"
      provides: "Manifest writing and duplicate detection during index/reindex"
      contains: "_write_manifest"
    - path: "src/ood/models.py"
      provides: "IndexResult duplicate/metadata diagnostics in JSON output"
      exports: ["IndexResult", "DuplicateGroup"]
    - path: "tests/test_rag.py"
      provides: "Manifest and duplicate reporting tests"
  key_links:
    - from: "src/ood/rag.py"
      to: "storage_dir/ood-manifest.json"
      via: "manifest write after successful indexing path"
      pattern: "_write_manifest"
    - from: "src/ood/rag.py"
      to: "IndexResult"
      via: "metadata_warnings and duplicate_groups returned to callers"
      pattern: "IndexResult\(.*metadata_warnings"
---

<objective>
Make normal indexing/reindexing produce manifest-backed diagnostics and duplicate reports.

Purpose: Phase 3 needs rich manifest state before incremental updates can compare changed files. Index and reindex must also surface metadata and duplicate findings so knowledge quality issues are visible immediately.
Output: `storage_dir/ood-manifest.json` is written during index/reindex, and duplicate/metadata diagnostics are returned by `IndexResult`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-knowledge-management/03-CONTEXT.md
@.planning/phases/03-knowledge-management/03-01-SUMMARY.md
@src/ood/models.py
@src/ood/rag.py
@tests/test_rag.py

<interfaces>
Contracts created by Plan 03-01 and used here:
```python
class MetadataWarning: ...
class DuplicateGroup: ...
class ManifestEntry: ...
class ManifestDiff: ...
class UpdateResult: ...
MANIFEST_SCHEMA_VERSION = 1
MANIFEST_FILENAME = "ood-manifest.json"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write rich manifest during index and reindex</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <read_first>src/ood/rag.py, src/ood/models.py, tests/test_rag.py, .planning/phases/03-knowledge-management/03-CONTEXT.md, .planning/phases/03-knowledge-management/03-01-SUMMARY.md</read_first>
  <behavior>
    - `RagEngine.index_markdown()` creates `settings.storage_dir / "ood-manifest.json"` when at least one non-empty Markdown body exists.
    - Manifest JSON contains top-level `schema_version: 1`, `generated_at`, and `entries` keyed or listed with `path`, `content_hash`, `body_hash`, `metadata`, `warnings`, `body_excerpt`, and `duplicate_group_ids`.
    - Frontmatter is stripped from both LightRAG insertions and `ood-fallback-index.json` content.
    - Metadata warnings are included in `IndexResult.metadata_warnings` and, when JSON-serialized, under `metadata_warnings`.
    - `reindex_markdown()` clears old storage children, rebuilds, and writes a fresh manifest under the same filename.
  </behavior>
  <action>
    Update `src/ood/rag.py` so `_aindex_markdown(clear_storage=False|True)` loads parsed documents once and uses their `body_text`, relative paths, metadata, warnings, hashes, and excerpts throughout.

    Implement `_build_manifest_entries(parsed_documents, duplicate_groups) -> list[ManifestEntry]` and `_write_manifest(entries: list[ManifestEntry], duplicate_groups: list[DuplicateGroup]) -> None`. The manifest JSON top-level shape must be exactly:
    ```json
    {
      "schema_version": 1,
      "generated_at": "<UTC ISO timestamp ending with Z>",
      "entries": [ ... ManifestEntry.to_dict() ... ],
      "duplicate_groups": [ ... DuplicateGroup.to_dict() ... ]
    }
    ```

    Ensure the local fallback index writes stripped `body_text` content instead of raw Markdown with frontmatter. Preserve existing no-document behavior: if no non-empty body documents exist, return `IndexResult(status="no_documents", indexed_documents=0, ...)` and do not require a manifest to exist.

    Aggregate metadata warnings from all indexed documents and pass them to `IndexResult(metadata_warnings=..., manifest_path=self._manifest_path())`. This implements D-05, D-08, D-13, D-14, and D-15 for index/reindex flows.
  </action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_index_writes_manifest_with_metadata_and_hashes tests/test_rag.py::test_fallback_index_uses_frontmatter_stripped_body -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/rag.py` contains `def _write_manifest(`.
    - `src/ood/rag.py` contains `"schema_version": MANIFEST_SCHEMA_VERSION`.
    - `src/ood/rag.py` contains `"duplicate_groups"` in manifest writing logic.
    - `tests/test_rag.py` contains `test_index_writes_manifest_with_metadata_and_hashes`.
    - `tests/test_rag.py` contains `test_fallback_index_uses_frontmatter_stripped_body`.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Index/reindex create a schema-versioned manifest with metadata warnings, hashes, excerpts, and stripped retrieval content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add exact and near-duplicate diagnostics</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <read_first>src/ood/rag.py, src/ood/models.py, tests/test_rag.py, .planning/phases/03-knowledge-management/03-CONTEXT.md</read_first>
  <behavior>
    - Documents with identical normalized `body_hash` produce an exact `DuplicateGroup(kind="exact", score=1.0)`.
    - Documents with token Jaccard similarity `>= 0.85` but different body hashes produce a near `DuplicateGroup(kind="near", score=<rounded similarity>)`.
    - Duplicate canonical path is chosen by preferring `metadata["status"] == "active"`, then newest parseable `metadata["datum"]`, then deterministic path order.
    - Duplicate documents are still indexed; duplicate diagnostics are reporting-only.
    - Manifest entries list `duplicate_group_ids` for groups containing that entry.
  </behavior>
  <action>
    Implement duplicate detection in `src/ood/rag.py` behind private helpers:
    - `_detect_duplicates(parsed_documents: list[_ParsedMarkdownDocument]) -> list[DuplicateGroup]`.
    - `_select_canonical_duplicate(paths_or_documents) -> str` using D-11 ordering: active status first, newest `datum` second, POSIX path order third. Parse `datum` with `datetime.date.fromisoformat` when possible; invalid or missing dates sort older than valid dates.
    - `_jaccard_similarity(left: str, right: str) -> float` using existing `_tokenize()` and rounding group scores to two decimals.
    - `_duplicate_group_id(kind: str, paths: list[str]) -> str` returning deterministic IDs such as `exact:<sha256 first 12>` or `near:<sha256 first 12>`.

    Call duplicate detection during index/reindex before manifest writing. Attach `duplicate_group_ids` to each `ManifestEntry`, include all groups in manifest JSON, and pass all groups to `IndexResult(duplicate_groups=...)`. Do not skip duplicate documents and do not fail the command when duplicates exist. This implements D-09, D-10, D-11, and the full-index/reindex side of D-12.
  </action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_index_reports_exact_and_near_duplicates_without_skipping tests/test_rag.py::test_duplicate_canonical_prefers_active_newest_then_path -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/ood/rag.py` contains `def _detect_duplicates(`.
    - `src/ood/rag.py` contains `def _jaccard_similarity(`.
    - `src/ood/rag.py` contains `status") == "active"` or equivalent active-status canonical logic.
    - `tests/test_rag.py` contains `test_index_reports_exact_and_near_duplicates_without_skipping`.
    - `tests/test_rag.py` contains `test_duplicate_canonical_prefers_active_newest_then_path`.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Index/reindex report exact and near duplicate groups with deterministic canonical documents while still indexing all documents.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_rag.py tests/test_models.py -q` and inspect one generated manifest fixture in tests to confirm `schema_version`, `entries`, and `duplicate_groups` fields are present.
</verification>

<success_criteria>
- Manifest exists under configured `storage_dir` after indexing non-empty Markdown.
- Metadata and duplicate diagnostics are visible through returned result contracts.
- Duplicate reporting is non-blocking and does not skip documents per D-10.
- Canonical duplicate selection follows D-11 exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-knowledge-management/03-02-SUMMARY.md`.
</output>
