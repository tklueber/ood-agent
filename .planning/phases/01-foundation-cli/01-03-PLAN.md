---
phase: 01-foundation-cli
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - pyproject.toml
  - src/ood/cli.py
  - src/ood/config.py
  - tests/test_cli.py
  - README.md
autonomous: true
requirements:
  - INF-06
  - INF-07
must_haves:
  truths:
    - "Developer can run `ood index`, `ood update`, `ood query`, and `ood reindex`."
    - "Each command validates configuration and returns a successful stub response."
    - "CLI supports human-friendly output by default and JSON output for automation."
    - "CLI exposes normal, verbose, and quiet output modes."
  artifacts:
    - path: "src/ood/cli.py"
      provides: "Typer CLI app and command stubs"
      exports: ["app"]
    - path: "pyproject.toml"
      provides: "ood console script entry point"
      contains: "ood = \"ood.cli:app\""
    - path: "tests/test_cli.py"
      provides: "automated command and option tests"
  key_links:
    - from: "pyproject.toml"
      to: "src/ood/cli.py"
      via: "project.scripts console entry point"
      pattern: "ood.cli:app"
    - from: "src/ood/cli.py"
      to: "src/ood/config.py"
      via: "load_settings before command execution"
      pattern: "load_settings"
---

<objective>
Create the Typer CLI surface with configuration-aware command stubs.

Purpose: Users need a professional CLI contract now so Phase 2 can replace stub internals without changing command names or basic options.
Output: `ood` console script with `index`, `update`, `query`, and `reindex` commands, tested with Typer's CLI runner.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cli/01-CONTEXT.md
@.planning/phases/01-foundation-cli/01-02-SUMMARY.md

<interfaces>
From `src/ood/config.py` created in Plan 02:
```python
class Settings(BaseSettings): ...
def load_settings(**overrides: object) -> Settings: ...
```

This plan creates:
```python
from typer import Typer
app = Typer(...)
```
`pyproject.toml` must expose `ood = "ood.cli:app"` in `[project.scripts]` per D-07.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Typer app and command contracts</name>
  <files>src/ood/cli.py, tests/test_cli.py, pyproject.toml</files>
  <behavior>
    - `ood --help` lists `index`, `update`, `query`, and `reindex` per D-06.
    - `ood index`, `ood update`, and `ood reindex` exit 0 and print placeholder output per D-16.
    - `ood query "ticket text"` accepts a required query argument and exits 0 with placeholder output.
  </behavior>
  <action>Write Typer CLI tests first using `typer.testing.CliRunner`, then implement `src/ood/cli.py` with a single flat `app` per D-05 and D-06. Add `[project.scripts] ood = "ood.cli:app"` to `pyproject.toml` per D-07 if not already present. Commands must be stubs only per D-16: accept the correct arguments/options, call `load_settings`, validate config shape, print a clear placeholder such as "Index stub: ready to index knowledge_dir=...", and return exit code 0. Do not implement LightRAG, semantic search, indexing, or file manifests because those are out of scope for Phase 1.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q</automated>
  </verify>
  <done>All four flat commands exist, help text is generated by Typer, and stub commands pass CLI runner tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add global output and config options</name>
  <files>src/ood/cli.py, tests/test_cli.py, README.md</files>
  <behavior>
    - Commands support `--json` and return machine-parseable JSON when requested per D-12.
    - Commands support `-v/--verbose` and `-q/--quiet`; verbose shows additional diagnostic context, quiet suppresses non-essential placeholder text per D-14/D-15.
    - Commands accept path overrides for `--knowledge-dir`, `--data-dir`, and `--storage-dir` and pass them to `load_settings` per D-09/D-11.
  </behavior>
  <action>Extend tests before implementation for `--json`, `-v`, `-q`, and path override behavior. Implement shared options on each command or a small reusable helper; keep the command structure flat and simple. Use Rich for human-friendly output/color auto-detection per D-12/D-13, and Python traceback behavior such that user-friendly messages are default while `-v` exposes exception details per D-15. JSON output should include at least `command`, `status`, `message`, `knowledge_dir`, `data_dir`, and `storage_dir`. Update README with example commands for all four stubs, JSON mode, and path overrides.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q && uv run ood --help</automated>
  </verify>
  <done>CLI supports human and JSON outputs, verbosity controls, path overrides, and documented examples; all tests pass.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_cli.py -q`, `uv run ood --help`, `uv run ood index --json`, and `uv run ood query "sample ticket" --json`.
</verification>

<success_criteria>
- `ood` console script is installed through uv.
- All required commands exist as configuration-aware stubs.
- Output modes and verbosity match locked decisions from CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli/01-03-SUMMARY.md`
</output>
