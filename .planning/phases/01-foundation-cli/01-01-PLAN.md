---
phase: 01-foundation-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - .gitignore
  - .env.example
  - src/ood/__init__.py
  - tests/test_project_scaffold.py
autonomous: true
requirements:
  - INF-07
  - INF-05
must_haves:
  truths:
    - "Developer can install project dependencies with uv using Python 3.10+."
    - "Project uses src/ layout with importable ood package."
    - "Persistent runtime data is directed to data/ and excluded from git."
  artifacts:
    - path: "pyproject.toml"
      provides: "uv-compatible Python package metadata, dependencies, scripts, and tool config"
      contains: "requires-python = \">=3.10\""
    - path: ".gitignore"
      provides: "secret and runtime-data exclusion"
      contains: "data/"
    - path: ".env.example"
      provides: "documented non-secret environment variable template"
      contains: "OOD_"
    - path: "src/ood/__init__.py"
      provides: "importable src-layout package"
    - path: "tests/test_project_scaffold.py"
      provides: "automated scaffold verification"
  key_links:
    - from: "pyproject.toml"
      to: "src/ood/__init__.py"
      via: "src layout package discovery"
      pattern: "package-dir.*src|src/ood"
    - from: ".gitignore"
      to: "data/"
      via: "runtime data exclusion"
      pattern: "^data/"
---

<objective>
Create the Python project foundation for OOD Agent.

Purpose: Phase 2 needs a reliable, installable Python package before RAG logic can be implemented.
Output: uv-based package metadata, src/ layout, safe default directories, and scaffold tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cli/01-CONTEXT.md

<interfaces>
Greenfield project: no existing code interfaces. This plan creates the base package contract that downstream plans use: import path `ood` from `src/ood/` and project tooling through `uv`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create uv Python package scaffold</name>
  <files>pyproject.toml, README.md, src/ood/__init__.py, tests/test_project_scaffold.py</files>
  <action>Create a uv-managed Python package per D-01 and D-02. Use `requires-python = ">=3.10"`, project name `ood-agent`, package import name `ood`, and src layout per D-03. Add runtime dependencies needed by later plans: `typer`, `rich`, `pydantic`, `pydantic-settings`, `python-dotenv`; add dev dependencies for `pytest`, `pytest-cov`, `ruff`, and `mypy`. Configure pytest to discover tests under `tests/` and pythonpath `src`. Add README with install commands using `uv sync` and a short note that Phase 1 commands are stubs. Add `tests/test_project_scaffold.py` asserting `import ood` works and package exposes a non-empty `__version__`. Do not add LightRAG or RAG dependencies yet because Phase 1 explicitly excludes real RAG logic.</action>
  <verify>
    <automated>uv sync && uv run pytest tests/test_project_scaffold.py -q</automated>
  </verify>
  <done>uv creates an environment successfully, `ood` imports from `src/ood`, and scaffold test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Protect runtime data and secret templates</name>
  <files>.gitignore, .env.example, README.md</files>
  <action>Create `.gitignore` entries for `.env`, `.env.*` except `.env.example`, `.venv/`, Python caches, pytest/mypy/ruff caches, coverage outputs, and `data/` per INF-05 and D-04/D-10. Create `.env.example` with documented placeholders only: `OOD_LLM_PROVIDER`, `OOD_LLM_API_KEY`, `OOD_LLM_MODEL`, `OOD_KNOWLEDGE_DIR`, `OOD_DATA_DIR`, `OOD_STORAGE_DIR`, and no real secrets. Update README with the default directory contract from D-04 and D-09: `knowledge/` for Markdown input, `data/` for persisted index/runtime storage, `data/storage/` for storage internals. Do not create `.env` and do not commit real credentials.</action>
  <verify>
    <automated>uv run pytest tests/test_project_scaffold.py -q</automated>
  </verify>
  <done>`data/` and `.env` secrets are excluded from git, `.env.example` documents safe placeholders, and README documents uv setup plus directory defaults.</done>
</task>

</tasks>

<verification>
Run `uv sync`, then `uv run pytest tests/test_project_scaffold.py -q`. Confirm `git check-ignore data/ .env` reports both paths as ignored.
</verification>

<success_criteria>
- Developer can install dependencies with uv on Python 3.10+.
- `src/ood` is importable.
- Runtime data and secrets are protected from git.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli/01-01-SUMMARY.md`
</output>
