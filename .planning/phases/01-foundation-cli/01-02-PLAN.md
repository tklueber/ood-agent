---
phase: 01-foundation-cli
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/ood/config.py
  - tests/test_config.py
  - .env.example
  - README.md
autonomous: true
requirements:
  - INF-06
  - INF-05
must_haves:
  truths:
    - "System loads Cloud LLM credentials from environment or .env without committing secrets."
    - "All important paths have sensible defaults and can be overridden."
    - "Configuration precedence is CLI args over environment/.env over defaults."
  artifacts:
    - path: "src/ood/config.py"
      provides: "Settings model and config loader"
      exports: ["Settings", "load_settings"]
    - path: "tests/test_config.py"
      provides: "automated config precedence and default path tests"
    - path: ".env.example"
      provides: "safe configuration template"
  key_links:
    - from: "src/ood/config.py"
      to: ".env"
      via: "pydantic-settings env_file loading"
      pattern: "env_file.*\.env"
    - from: "src/ood/config.py"
      to: "data/storage"
      via: "default storage_dir derived from data_dir"
      pattern: "storage.*data"
---

<objective>
Implement configuration loading for paths and Cloud LLM credentials.

Purpose: The CLI must validate configuration consistently before Phase 2 replaces stubs with real RAG behavior.
Output: Typed settings module with tests for defaults, `.env`, environment variables, and CLI override handling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cli/01-CONTEXT.md
@.planning/phases/01-foundation-cli/01-01-SUMMARY.md

<interfaces>
Created by Plan 01:
```text
Package import path: ood
Default directories: knowledge/, data/, data/storage/
Dependency manager: uv
```

This plan must create:
```python
class Settings(BaseSettings): ...
def load_settings(**overrides: object) -> Settings: ...
```
Downstream CLI code in Plan 03 will import these names directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed settings and defaults</name>
  <files>src/ood/config.py, tests/test_config.py</files>
  <behavior>
    - Default settings use `knowledge/`, `data/`, and `data/storage/` when no environment variables are set per D-09.
    - `OOD_LLM_PROVIDER`, `OOD_LLM_API_KEY`, and `OOD_LLM_MODEL` are accepted from environment or `.env` per D-10.
    - Missing LLM API key is allowed in Phase 1 because CLI stubs must work for local setup, but config exposes whether credentials are configured.
  </behavior>
  <action>Write tests first in `tests/test_config.py`, then implement `src/ood/config.py`. Use `pydantic-settings` with `env_prefix="OOD_"`, `env_file=".env"`, and `extra="ignore"`. Model fields: `knowledge_dir: Path = Path("knowledge")`, `data_dir: Path = Path("data")`, `storage_dir: Path | None = None`, `llm_provider: str | None = None`, `llm_api_key: SecretStr | None = None`, `llm_model: str | None = None`, `verbose: int = 0`, `quiet: bool = False`. Derive effective `storage_dir` as `data_dir / "storage"` when unset. Add a property or method such as `has_llm_credentials` that returns true only when an API key is present. Respect D-11 by making `load_settings(**overrides)` apply explicit overrides after env/default loading.</action>
  <verify>
    <automated>uv run pytest tests/test_config.py -q</automated>
  </verify>
  <done>Settings defaults, env loading, credential detection, and override precedence are covered by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate path configuration behavior</name>
  <files>src/ood/config.py, tests/test_config.py, README.md, .env.example</files>
  <behavior>
    - CLI-style overrides for `knowledge_dir`, `data_dir`, and `storage_dir` win over env/defaults per D-11.
    - `data_dir` and `storage_dir` can point outside the repository per INF-05.
    - `.env.example` documents every supported `OOD_` variable without secrets.
  </behavior>
  <action>Extend config tests to cover path overrides using temporary directories. Ensure `load_settings(knowledge_dir=tmp_path / "kb", data_dir=tmp_path / "idx")` returns those exact paths and derives storage under the overridden data dir unless an explicit storage override is provided. Update README and `.env.example` to document every supported `OOD_` variable and precedence order exactly as D-11 states: CLI args > environment vars > config file/.env > defaults. Do not add a separate config-file parser in Phase 1 unless already trivial through pydantic-settings; a `.env` file satisfies INF-06 for this phase.</action>
  <verify>
    <automated>uv run pytest tests/test_config.py -q</automated>
  </verify>
  <done>Path override behavior is tested, external data locations are supported, and docs/templates match the implemented settings names.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_config.py -q` and inspect that `.env.example` contains only placeholders, not credentials.
</verification>

<success_criteria>
- Settings can be loaded from defaults, env/.env, and explicit CLI-style overrides.
- Cloud LLM credential fields exist but secrets are never required or committed.
- Data and storage paths are configurable outside git.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli/01-02-SUMMARY.md`
</output>
