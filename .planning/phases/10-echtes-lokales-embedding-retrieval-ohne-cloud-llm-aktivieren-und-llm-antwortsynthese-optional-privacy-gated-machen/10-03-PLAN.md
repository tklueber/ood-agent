---
phase: 10-echtes-lokales-embedding-retrieval-ohne-cloud-llm-aktivieren-und-llm-antwortsynthese-optional-privacy-gated-machen
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - src/ood/cli.py
  - tests/test_cli.py
  - README.md
  - .env.example
autonomous: true
requirements: [PRIV-01, PRIV-02, LOCAL-RET-02]
must_haves:
  truths:
    - "Operators can see from verbose CLI output whether Cloud LLM synthesis was allowed and used."
    - "Configuration docs explain that local embedding retrieval is default and Cloud LLM synthesis is opt-in."
    - "The example env file shows the privacy gate without containing real secrets."
  artifacts:
    - path: "src/ood/cli.py"
      provides: "Human diagnostics for local retrieval and privacy-gated LLM usage"
      contains: "cloud_llm_allowed"
    - path: "tests/test_cli.py"
      provides: "CLI coverage for privacy diagnostic output"
    - path: "README.md"
      provides: "User workflow documentation for local embeddings and optional LLM synthesis"
    - path: ".env.example"
      provides: "Safe template for OOD_ALLOW_CLOUD_LLM"
  key_links:
    - from: "src/ood/cli.py"
      to: "src/ood/config.py"
      via: "CLI diagnostics render Settings.can_use_cloud_llm/allow_cloud_llm without exposing secrets"
      pattern: "can_use_cloud_llm|allow_cloud_llm"
---

<objective>
Expose and document the local-first retrieval and privacy-gated answer synthesis workflow.

Purpose: The implementation is only safe if operators understand that embeddings run locally by default and Cloud LLM synthesis requires explicit approval.
Output: CLI diagnostics, tests, README updates, and safe `.env.example` guidance.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-echtes-lokales-embedding-retrieval-ohne-cloud-llm-aktivieren-und-llm-antwortsynthese-optional-privacy-gated-machen/10-01-SUMMARY.md
@.planning/phases/10-echtes-lokales-embedding-retrieval-ohne-cloud-llm-aktivieren-und-llm-antwortsynthese-optional-privacy-gated-machen/10-02-SUMMARY.md
@src/ood/cli.py
@README.md
@.env.example

<interfaces>
Existing CLI output pattern to extend without breaking JSON contracts:

```python
# src/ood/cli.py
if verbose:
    typer.echo(
        f"Diagnostics: llm_used={result.llm_used} "
        f"status={result.status} source_count={len(result.sources)} "
        f"analysis_mode={result.analysis.mode}"
    )
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add privacy/local-retrieval query diagnostics</name>
  <files>src/ood/cli.py, tests/test_cli.py</files>
  <behavior>
    - Verbose human `ood query` output includes whether Cloud LLM synthesis was allowed by settings and whether it was used in the result.
    - Diagnostics do not print `OOD_LLM_API_KEY` or secret values.
    - JSON output remains exactly `QueryResult.to_dict()` and does not gain CLI-only diagnostic fields.
  </behavior>
  <action>Thread non-secret settings diagnostics into query verbose output. The simplest acceptable pattern is to keep the loaded `settings` in `query()` and pass a boolean such as `cloud_llm_allowed=settings.can_use_cloud_llm` into `_emit_query_result()`. Update tests to assert verbose human output contains `cloud_llm_allowed=False` for a mocked retrieval-only result and that JSON output remains unchanged.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py::test_query_human_output_includes_ticket_analysis_sections tests/test_cli.py::test_query_json_output_matches_phase_4_contract -q</automated>
  </verify>
  <done>Verbose query diagnostics expose the privacy gate without leaking secrets or changing JSON automation output.</done>
</task>

<task type="auto">
  <name>Task 2: Document local embeddings and opt-in Cloud LLM synthesis</name>
  <files>README.md, .env.example</files>
  <action>Update README sections for Core RAG usage, Ticket Intelligence privacy behavior, and Configuration. State that local sentence-transformer embeddings are the default retrieval path, Cloud LLM synthesis is disabled unless `OOD_ALLOW_CLOUD_LLM=true` and credentials are configured, and secrets must remain in `.env` or environment variables. Add `OOD_ALLOW_CLOUD_LLM=false` to `.env.example` with a comment explaining it is the explicit privacy approval gate; do not add real provider names, keys, or URLs.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q</automated>
  </verify>
  <done>Docs and env template match the implemented default-local, opt-in-synthesis behavior.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_cli.py tests/test_config.py tests/test_rag.py -q` and confirm README/.env.example contain no real secrets.
</verification>

<success_criteria>
- Users can discover and verify local-first retrieval behavior from CLI/docs.
- Cloud LLM synthesis approval is visible but default false.
- Automation JSON contract remains stable.
</success_criteria>

<output>
After completion, create `.planning/phases/10-echtes-lokales-embedding-retrieval-ohne-cloud-llm-aktivieren-und-llm-antwortsynthese-optional-privacy-gated-machen/10-03-SUMMARY.md`.
</output>
