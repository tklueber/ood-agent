---
phase: 10-echtes-lokales-embedding-retrieval-ohne-cloud-llm-aktivieren-und-llm-antwortsynthese-optional-privacy-gated-machen
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/ood/rag.py
  - tests/test_rag.py
autonomous: true
requirements: [LOCAL-RET-01, LOCAL-RET-02]
must_haves:
  truths:
    - "A no-Cloud local index stores reusable sentence embeddings for Markdown body text."
    - "Local fallback queries rank sources by embedding similarity instead of lexical token overlap."
    - "SourceHit output remains the stable public query contract."
  artifacts:
    - path: "src/ood/rag.py"
      provides: "Local vector fallback index and cosine-similarity retrieval"
      contains: "LOCAL_VECTOR_INDEX_FILENAME"
    - path: "tests/test_rag.py"
      provides: "Regression coverage for embedding-based local retrieval"
  key_links:
    - from: "src/ood/rag.py"
      to: "sentence_transformers.SentenceTransformer"
      via: "local embedding function encodes Markdown documents and query text"
      pattern: "SentenceTransformer"
    - from: "src/ood/rag.py"
      to: "ood.models.SourceHit"
      via: "cosine-ranked local vector results normalize to public SourceHit objects"
      pattern: "SourceHit\(path=.*score=.*excerpt="
---

<objective>
Replace the lexical JSON fallback with true local embedding retrieval for no-Cloud operation.

Purpose: Users should get semantic-ish local retrieval even when no Cloud LLM is approved or available, and tests should prove the fallback is not just keyword overlap.
Output: Local vector index artifact, cosine ranking query path, and tests with mocked embeddings.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-echtes-lokales-embedding-retrieval-ohne-cloud-llm-aktivieren-und-llm-antwortsynthese-optional-privacy-gated-machen/10-01-SUMMARY.md
@src/ood/rag.py
@tests/test_rag.py

<interfaces>
Existing public output must remain unchanged:

```python
# src/ood/models.py
@dataclass(frozen=True)
class SourceHit:
    path: str
    score: float
    excerpt: str

@dataclass(frozen=True)
class QueryResult:
    query: str
    answer: str | None
    confidence: ConfidenceScore
    sources: list[SourceHit]
    llm_used: bool
    status: str
    analysis: TicketAnalysis
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist local embedding vectors during fallback indexing</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - When the local fallback path is used, `index_markdown()` writes an embedding index file containing each document path, frontmatter-stripped content, excerpt, and vector.
    - The legacy `ood-fallback-index.json` lexical artifact is replaced or superseded by a clearly named vector artifact such as `ood-local-vector-index.json`.
    - Tests monkeypatch the embedding model/function so they do not download a real SentenceTransformer model.
  </behavior>
  <action>Add a local vector fallback writer that encodes document bodies using the existing local embedding machinery and serializes JSON-safe vectors (`list[float]`). Keep manifest writing unchanged. Avoid network/model downloads in tests by monkeypatching the embedding function/model boundary. Preserve no-document behavior.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_fallback_index_uses_frontmatter_stripped_body -q</automated>
  </verify>
  <done>Fallback indexing stores local vectors with source paths and stripped body text, and the focused fallback indexing test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rank local fallback queries by cosine similarity</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - `_query_local_fallback()` encodes the query and ranks stored document vectors by cosine similarity.
    - Returned scores are clamped/rounded between 0.0 and 1.0 and sorted descending.
    - A test fixture proves a semantically closer mocked vector wins even when token overlap would have favored another document.
    - Empty or malformed vector index payloads return an empty list rather than crashing.
  </behavior>
  <action>Implement cosine-similarity query over the local vector index and return stable `SourceHit` objects with existing 500-character excerpt behavior. Delete or stop using token-overlap ranking for the fallback path. Keep `answer=None`, `llm_used=False`, deterministic ticket analysis, and confidence scoring unchanged from the caller perspective.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_query_without_llm_returns_ranked_sources_and_null_answer tests/test_rag.py::test_query_without_llm_credentials_makes_no_cloud_llm_calls -q</automated>
  </verify>
  <done>Local fallback queries are vector-ranked, preserve public QueryResult fields, and never call a Cloud LLM.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_rag.py -q` after focused tests pass.
</verification>

<success_criteria>
- Local fallback retrieval is embedding-based, not token-overlap based.
- No Cloud LLM is called in fallback indexing or query.
- Source attribution, confidence, and ticket analysis contracts remain stable.
</success_criteria>

<output>
After completion, create `.planning/phases/10-echtes-lokales-embedding-retrieval-ohne-cloud-llm-aktivieren-und-llm-antwortsynthese-optional-privacy-gated-machen/10-02-SUMMARY.md`.
</output>
