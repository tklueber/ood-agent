---
phase: 10-echtes-lokales-embedding-retrieval-ohne-cloud-llm-aktivieren-und-llm-antwortsynthese-optional-privacy-gated-machen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ood/config.py
  - src/ood/rag.py
  - tests/test_config.py
  - tests/test_rag.py
autonomous: true
requirements: [LOCAL-RET-01, PRIV-01]
must_haves:
  truths:
    - "Configured Cloud LLM credentials alone do not send ticket content to a Cloud LLM."
    - "LLM answer synthesis activates only when credentials and an explicit privacy approval setting are both present."
    - "Retrieval still works with local embeddings and a no-op LLM function when Cloud LLM usage is not approved."
  artifacts:
    - path: "src/ood/config.py"
      provides: "Privacy gate setting and derived cloud-LLM permission contract"
      contains: "allow_cloud_llm"
    - path: "src/ood/rag.py"
      provides: "Query/index LLM-function selection based on explicit privacy gate"
      contains: "can_use_cloud_llm"
    - path: "tests/test_config.py"
      provides: "Settings coverage for default-deny and explicit approval"
    - path: "tests/test_rag.py"
      provides: "Service coverage proving credentials are insufficient without approval"
  key_links:
    - from: "src/ood/rag.py"
      to: "src/ood/config.py"
      via: "RagEngine uses Settings.can_use_cloud_llm instead of Settings.has_llm_credentials for non-noop LLM selection"
      pattern: "can_use_cloud_llm"
---

<objective>
Make Cloud LLM answer synthesis explicitly privacy-gated instead of implicitly enabled by credentials.

Purpose: The project constraint says ticket content may be sent to a Cloud LLM only after privacy approval; this plan turns that approval into code, with default-deny behavior.
Output: Updated settings contract, RagEngine gating, and focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-rag-engine/02-03-SUMMARY.md
@.planning/phases/04-ticket-intelligence/04-02-SUMMARY.md
@src/ood/config.py
@src/ood/rag.py
@tests/test_config.py
@tests/test_rag.py

<interfaces>
Existing contracts to preserve:

```python
# src/ood/config.py
class Settings(BaseSettings):
    knowledge_dir: Path = Path("knowledge")
    data_dir: Path = Path("data")
    storage_dir: Path | None = None
    llm_provider: str | None = None
    llm_api_key: SecretStr | None = None
    llm_model: str | None = None
    verbose: int = 0
    quiet: bool = False

    @property
    def has_llm_credentials(self) -> bool: ...
```

```python
# src/ood/rag.py
def _build_llm_model_func(self) -> Callable[..., Awaitable[str]]: ...
async def _noop_llm_model_func(prompt: str, **_: object) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add default-deny Cloud LLM approval setting</name>
  <files>src/ood/config.py, tests/test_config.py</files>
  <behavior>
    - Settings defaults `allow_cloud_llm` to `False` even when `OOD_LLM_API_KEY` is present.
    - `Settings.has_llm_credentials` remains true when a key exists, for diagnostics and backwards-compatible credential detection.
    - New derived property `can_use_cloud_llm` is true only when `allow_cloud_llm` is true and credentials are present.
    - Environment variable `OOD_ALLOW_CLOUD_LLM=true` enables the gate through the existing pydantic-settings loader.
  </behavior>
  <action>Add `allow_cloud_llm: bool = False` to `Settings` and a `can_use_cloud_llm` property. Do not remove or weaken `has_llm_credentials`; existing callers may still need to distinguish configured credentials from permission to send content. Add tests that prove default-deny with credentials and explicit env approval.</action>
  <verify>
    <automated>uv run pytest tests/test_config.py -q</automated>
  </verify>
  <done>Settings exposes a default-false approval gate, credentials alone are insufficient for `can_use_cloud_llm`, and config tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use privacy approval for every non-noop LLM path</name>
  <files>src/ood/rag.py, tests/test_rag.py</files>
  <behavior>
    - Indexing and querying pass `_noop_llm_model_func` whenever `settings.can_use_cloud_llm` is false.
    - Query mode remains retrieval-only/naive when credentials exist but approval is false.
    - Generated answers and `llm_used=True` occur only when `can_use_cloud_llm` is true and the provider is supported.
  </behavior>
  <action>Replace all LLM-enabling checks in `RagEngine` that currently rely on `has_llm_credentials` with `can_use_cloud_llm` where content could be sent to a Cloud LLM: `_build_llm_model_func()`, query mode selection, answer synthesis, and confidence rationale. Keep provider support checks. Update existing tests that assumed credentials alone selected mix mode, and add a regression test where credentials are configured but approval is false and `openai_complete_if_cache` would fail if called.</action>
  <verify>
    <automated>uv run pytest tests/test_rag.py::test_index_with_llm_credentials_passes_non_noop_lightrag_llm_func tests/test_rag.py::test_llm_credentials_select_mix_query_mode tests/test_rag.py::test_query_without_llm_credentials_makes_no_cloud_llm_calls -q</automated>
  </verify>
  <done>RagEngine never selects a non-noop Cloud LLM function or generated-answer path unless explicit approval and credentials are both present.</done>
</task>

</tasks>

<verification>
Run the focused settings and RAG privacy tests, then run `uv run pytest tests/test_config.py tests/test_rag.py -q` before writing the summary.
</verification>

<success_criteria>
- Cloud LLM usage is default-deny.
- Existing credential detection remains available.
- Query results stay retrieval-only with `llm_used=false` when approval is absent.
</success_criteria>

<output>
After completion, create `.planning/phases/10-echtes-lokales-embedding-retrieval-ohne-cloud-llm-aktivieren-und-llm-antwortsynthese-optional-privacy-gated-machen/10-01-SUMMARY.md`.
</output>
