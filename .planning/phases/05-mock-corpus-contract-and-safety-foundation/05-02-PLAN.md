---
phase: 05-mock-corpus-contract-and-safety-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ood/mock_validation.py
  - tests/test_mock_validation.py
autonomous: true
requirements:
  - MOCK-03
  - MOCK-05
must_haves:
  truths:
    - "User can validate a mock corpus and receive actionable findings for unsafe or incomplete files."
    - "User can see coverage summarized by source type, system, component, routing target, command-risk level, and scenario category."
    - "Validator output distinguishes warnings/findings from coverage so generated or hand-copied corpora can be improved safely."
  artifacts:
    - path: "src/ood/mock_validation.py"
      provides: "Mock corpus safety validator and coverage summarizer"
      exports: ["MockSafetyFinding", "MockCoverageSummary", "MockValidationResult", "validate_mock_corpus"]
    - path: "tests/test_mock_validation.py"
      provides: "Validator tests for mock markers, suspicious patterns, golden leakage, and coverage dimensions"
  key_links:
    - from: "src/ood/mock_validation.py"
      to: "knowledge/mock/v1/*.md"
      via: "recursive Markdown validation over supplied corpus directory"
      pattern: "validate_mock_corpus\(.*Path"
    - from: "src/ood/mock_validation.py"
      to: "src/ood/rag.py"
      via: "reuse Phase 3 frontmatter shape expectations without changing indexing behavior"
      pattern: "quelle.*datum.*status.*system.*komponente.*title.*type"
---

<objective>
Create a local safety validator and coverage summarizer for mock Markdown corpora.

Purpose: Broad mock data is only useful if it is clearly synthetic, free from obvious secrets/PII-like content, and measurable by operational coverage dimensions.
Output: A dependency-free validation module with JSON-serializable findings and coverage summaries.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-knowledge-management/03-01-SUMMARY.md
@src/ood/rag.py
@tests/test_rag.py

<interfaces>
Existing Phase 3 parsing expectations to mirror in validator tests:
```python
# src/ood/rag.py
REQUIRED_METADATA_FIELDS = ("quelle", "datum", "status", "system", "komponente", "title", "type")
VALID_STATUS_VALUES = ("active", "deprecated", "draft")
```

The validator should parse scalar YAML frontmatter itself, like Phase 3, and must not block or modify `RagEngine` indexing paths.
</interfaces>

<discovery>
Level 0 — all work uses existing codebase patterns and Python stdlib regular expressions. No new dependency.
</discovery>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add safety finding contracts and marker validation</name>
  <files>src/ood/mock_validation.py, tests/test_mock_validation.py</files>
  <behavior>
    - Test 1: missing `mock: true`, missing `dataset`, missing `synthetic_id`, or missing visible body warning produces file-specific findings with actionable messages.
    - Test 2: suspicious real-data patterns such as normal emails, API-token-like strings, IBAN-like values, phone-number-like strings, and non-mock ServiceNow/Jira/ticket identifiers produce findings.
    - Test 3: golden-answer leakage markers such as `expected_answer`, `expected_sources`, `forbidden_sources`, or `golden` in frontmatter/body produce findings.
  </behavior>
  <action>Create `src/ood/mock_validation.py` with frozen dataclasses `MockSafetyFinding` and `MockValidationResult` plus `validate_mock_corpus(corpus_dir: Path) -> MockValidationResult`. Parse Markdown frontmatter using dependency-free scalar parsing. Findings must include path, severity (`error` or `warning`), code, message, and evidence. Validate MOCK-02 markers and MOCK-03 safety checks locally; never call a Cloud LLM. Keep all messages actionable (what marker/pattern is missing and how to fix it).</action>
  <verify>
    <automated>uv run pytest tests/test_mock_validation.py::test_validator_flags_missing_mock_markers tests/test_mock_validation.py::test_validator_flags_suspicious_real_data_and_secrets tests/test_mock_validation.py::test_validator_flags_golden_answer_leakage -q</automated>
  </verify>
  <done>Unsafe or incomplete mock documents produce deterministic, file-specific findings without external calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add coverage summary dimensions</name>
  <files>src/ood/mock_validation.py, tests/test_mock_validation.py</files>
  <behavior>
    - Test 1: valid documents aggregate counts by `source_type`, `system`, `komponente`, `routing_target`, `command_risk`, and `scenario_category`.
    - Test 2: validation result serializes `coverage` and `findings` via `to_dict()` with string paths and count dictionaries.
    - Test 3: empty or missing corpus directory returns a warning finding and zero coverage rather than crashing.
  </behavior>
  <action>Add `MockCoverageSummary` with count dictionaries for source type, system, component, routing target, command-risk level, and scenario category. Populate it inside `validate_mock_corpus()` from frontmatter values. Include `document_count`, `is_safe` (true only when there are no `error` findings), and sorted deterministic output in `MockValidationResult.to_dict()`. Do not introduce pass/fail thresholds for coverage breadth yet; Phase 9 owns regression gates after baseline.</action>
  <verify>
    <automated>uv run pytest tests/test_mock_validation.py -q</automated>
  </verify>
  <done>Validator returns both actionable safety findings and MOCK-05 coverage counts for every corpus directory.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_mock_validation.py -q`; confirm no Cloud LLM credentials or external services are required.
</verification>

<success_criteria>
- `src/ood/mock_validation.py` exports safety finding/result/coverage contracts and `validate_mock_corpus`.
- Missing mock metadata, suspicious real-data/secrets/PII-like patterns, and golden-answer leakage are flagged.
- Coverage summary includes counts for source type, system, component, routing target, command-risk level, and scenario category.
- Empty/missing corpora are handled as actionable warnings, not crashes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mock-corpus-contract-and-safety-foundation/05-02-SUMMARY.md`.
</output>
