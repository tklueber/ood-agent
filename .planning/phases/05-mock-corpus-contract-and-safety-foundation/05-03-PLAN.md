---
phase: 05-mock-corpus-contract-and-safety-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/ood/cli.py
  - tests/test_cli.py
  - README.md
autonomous: true
requirements:
  - MOCK-01
  - MOCK-02
  - MOCK-03
  - MOCK-05
must_haves:
  truths:
    - "User can generate/copy the mock corpus through the OOD CLI into a chosen directory."
    - "User can run a CLI safety validator and inspect findings plus coverage in human-readable output."
    - "Automation can consume mock corpus generation and validation results as JSON."
    - "Documentation explains the mock-data privacy boundary and warns not to put golden answers into indexed Markdown."
  artifacts:
    - path: "src/ood/cli.py"
      provides: "Flat CLI commands for mock corpus init and validation"
    - path: "tests/test_cli.py"
      provides: "CLI contract tests for human and JSON mock workflows"
    - path: "README.md"
      provides: "User-facing mock corpus workflow and safety guidance"
  key_links:
    - from: "src/ood/cli.py"
      to: "src/ood/mock_corpus.py"
      via: "`ood mock-init` calls `generate_mock_corpus`"
      pattern: "generate_mock_corpus"
    - from: "src/ood/cli.py"
      to: "src/ood/mock_validation.py"
      via: "`ood mock-validate` calls `validate_mock_corpus`"
      pattern: "validate_mock_corpus"
    - from: "README.md"
      to: "knowledge/mock/v1"
      via: "Documented default path and path override examples"
      pattern: "mock-init.*mock-validate"
---

<objective>
Expose the mock-corpus generator and safety validator through the CLI and documentation.

Purpose: Phase 5 must end with a user-operable workflow, not only internal services: generate mock data, validate safety/coverage, then hand the corpus to Phase 6 indexing flows.
Output: Flat CLI commands, tests, and README guidance for the privacy-safe mock corpus workflow.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ticket-intelligence/04-03-SUMMARY.md
@src/ood/cli.py
@tests/test_cli.py
@README.md

<interfaces>
Existing CLI pattern:
```python
app = typer.Typer(name="ood", help="CLI for the OOD Agent knowledge workflow.", no_args_is_help=True)
JsonOption = Annotated[bool, typer.Option("--json", help="Emit machine-parseable JSON.")]
VerboseOption = Annotated[bool, typer.Option("-v", "--verbose", help="Show diagnostic context and exception details.")]
QuietOption = Annotated[bool, typer.Option("-q", "--quiet", help="Suppress non-essential output.")]
```

Phase 1 decision: keep the CLI flat. Add commands named `mock-init` and `mock-validate`; do not add a nested Typer sub-app unless tests force it.

Contracts created by prior Phase 5 plans:
```python
from ood.mock_corpus import generate_mock_corpus
from ood.mock_validation import validate_mock_corpus
```
</interfaces>

<discovery>
Level 0 — CLI wiring and docs follow existing Typer/README patterns. No new external dependency.
</discovery>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add flat mock corpus CLI commands</name>
  <files>src/ood/cli.py, tests/test_cli.py</files>
  <behavior>
    - Test 1: `ood --help` lists `mock-init` and `mock-validate` alongside existing flat commands.
    - Test 2: `ood mock-init --target-dir <tmp>/knowledge/mock/v1 --json` returns command, dataset, document_count, generated_paths, and source_types.
    - Test 3: `ood mock-validate --corpus-dir <tmp>/knowledge/mock/v1 --json` returns command plus validation result with findings, coverage, document_count, and is_safe.
    - Test 4: human `mock-validate` output includes safety status, finding counts, and coverage dimensions unless `--quiet` is set.
  </behavior>
  <action>Modify `src/ood/cli.py` to import `generate_mock_corpus` and `validate_mock_corpus`. Add flat Typer commands `mock-init` and `mock-validate`. `mock-init` accepts `--target-dir` defaulting to `knowledge/mock/v1`, `--dataset` defaulting to `mock-v1`, `--json`, and `--quiet`; it writes files then emits a concise human summary or `{"command": "mock-init", ...result.to_dict()}`. `mock-validate` accepts `--corpus-dir` defaulting to `knowledge/mock/v1`, `--json`, `--verbose`, and `--quiet`; it emits `{"command": "mock-validate", ...result.to_dict()}` for JSON and human output with safety status, total findings, first actionable findings, and coverage counts. Keep existing command behavior unchanged.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py::test_help_lists_flat_commands tests/test_cli.py::test_mock_init_json_generates_corpus tests/test_cli.py::test_mock_validate_json_reports_findings_and_coverage tests/test_cli.py::test_mock_validate_human_output_summarizes_safety_and_coverage -q</automated>
  </verify>
  <done>Users and automation can generate and validate the mock corpus via CLI without breaking existing index/update/query/reindex commands.</done>
</task>

<task type="auto">
  <name>Task 2: Document mock corpus workflow and safety boundaries</name>
  <files>README.md, tests/test_cli.py</files>
  <action>Update README with a `Mock Corpus` section after Knowledge Management or before Ticket Intelligence. Include commands for `uv run ood mock-init`, `uv run ood mock-validate`, JSON variants, path overrides, and handoff to Phase 6 indexing (`uv run ood index --knowledge-dir knowledge/mock/v1`). Explicitly document that v1.1 starts with synthetic mock data only, every mock file must contain `mock: true`, dataset metadata, synthetic IDs, and visible body warnings, and golden answers/expected sources belong in future evaluation JSON — never indexed Markdown. Add a lightweight test assertion in `tests/test_cli.py` only if an existing README smoke pattern exists; otherwise rely on CLI tests and keep docs simple.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q</automated>
  </verify>
  <done>README gives the user exact local commands and privacy/safety boundaries for Phase 5 mock data.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_cli.py tests/test_mock_corpus.py tests/test_mock_validation.py -q` and then `uv run pytest -q` to ensure Phase 5 CLI wiring did not regress existing workflows.
</verification>

<success_criteria>
- `ood --help` lists `mock-init` and `mock-validate`.
- `ood mock-init --json` creates importable Markdown files with mock metadata and synthetic warnings.
- `ood mock-validate --json` reports safety findings, coverage counts, document_count, and is_safe.
- README documents mock-data-only privacy scope and no golden-answer leakage into indexed Markdown.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mock-corpus-contract-and-safety-foundation/05-03-SUMMARY.md`.
</output>
