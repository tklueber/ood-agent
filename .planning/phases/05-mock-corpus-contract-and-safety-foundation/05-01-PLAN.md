---
phase: 05-mock-corpus-contract-and-safety-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ood/mock_corpus.py
  - tests/test_mock_corpus.py
autonomous: true
requirements:
  - MOCK-01
  - MOCK-02
must_haves:
  truths:
    - "User can create an importable Markdown mock corpus without using real or anonymized production data."
    - "Every generated mock document visibly declares itself as mock data in YAML frontmatter and body text."
    - "Generated documents cover tickets, Wiki articles, Jira bugs, ServiceNow cases, runbooks, and notes."
  artifacts:
    - path: "src/ood/mock_corpus.py"
      provides: "Mock corpus generation contracts and deterministic Markdown writer"
      exports: ["MockDocument", "MockCorpusResult", "generate_mock_corpus"]
    - path: "tests/test_mock_corpus.py"
      provides: "Contract tests for generated document metadata, warnings, source-type breadth, and importability"
  key_links:
    - from: "src/ood/mock_corpus.py"
      to: "knowledge/mock/v1/*.md"
      via: "generate_mock_corpus writes canonical Markdown files below target directory"
      pattern: "mock: true"
    - from: "src/ood/mock_corpus.py"
      to: "src/ood/rag.py"
      via: "Generated files use existing required metadata fields from Phase 3"
      pattern: "quelle.*datum.*status.*system.*komponente.*title.*type"
---

<objective>
Create the deterministic mock-corpus contract and writer for Phase 5.

Purpose: Phase 5 starts v1.1 with privacy-safe, clearly synthetic data before any real or anonymized production exports are introduced.
Output: A reusable Python module that writes an importable Markdown mock corpus with mandatory mock metadata, synthetic identifiers, source-type coverage, and visible body warnings.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-knowledge-management/03-01-SUMMARY.md
@.planning/phases/03-knowledge-management/03-02-SUMMARY.md
@src/ood/rag.py
@tests/test_rag.py

<interfaces>
Existing Phase 3 Markdown import contract:
```python
# src/ood/rag.py
REQUIRED_METADATA_FIELDS = ("quelle", "datum", "status", "system", "komponente", "title", "type")
VALID_STATUS_VALUES = ("active", "deprecated", "draft")

class RagEngine:
    def _discover_markdown_files(self) -> list[Path]: ...
    def _parse_markdown_document(self, path: Path) -> _ParsedMarkdownDocument: ...
    def index_markdown(self) -> IndexResult: ...
```

Existing pattern from Phase 3: Markdown frontmatter is warning-only but should be complete for mock files. Body text, not frontmatter, is inserted into retrieval.
</interfaces>

<discovery>
Level 0 — no new external dependency or API. Use existing Python stdlib, dataclasses, pathlib, and pytest patterns.
</discovery>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define mock corpus result contracts</name>
  <files>src/ood/mock_corpus.py, tests/test_mock_corpus.py</files>
  <behavior>
    - Test 1: `MockDocument.to_markdown()` emits YAML frontmatter containing `mock: true`, `dataset`, `synthetic_id`, `source_type`, `scenario_category`, `routing_target`, and `command_risk` plus existing Phase 3 metadata fields.
    - Test 2: rendered body starts with a visible warning line containing `MOCK DATA` / `SYNTHETIC` wording before scenario content.
    - Test 3: `MockCorpusResult.to_dict()` serializes generated file paths, document count, dataset name, and covered source types without Path objects.
  </behavior>
  <action>Create `src/ood/mock_corpus.py` with frozen dataclasses `MockDocument` and `MockCorpusResult`. `MockDocument` must hold path, title, source_type, system, komponente, scenario_category, routing_target, command_risk, synthetic_id, tags, and body. Implement `to_markdown()` using simple deterministic YAML frontmatter followed by a visible warning line and body text. Do not add PyYAML or other dependencies; this project intentionally uses dependency-free scalar frontmatter parsing. Export symbols via `__all__`. Add focused tests first, then implementation.</action>
  <verify>
    <automated>uv run pytest tests/test_mock_corpus.py::test_mock_document_renders_required_metadata_and_visible_warning tests/test_mock_corpus.py::test_mock_corpus_result_serializes_paths_and_coverage -q</automated>
  </verify>
  <done>`MockDocument` and `MockCorpusResult` exist, serialize deterministically, include all MOCK-02 markers, and tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic broad mock corpus generation</name>
  <files>src/ood/mock_corpus.py, tests/test_mock_corpus.py</files>
  <behavior>
    - Test 1: `generate_mock_corpus(target_dir)` creates importable `.md` files under the provided target directory and returns their paths.
    - Test 2: generated corpus covers exactly these source types at least once: `ticket`, `wiki`, `jira_bug`, `servicenow_case`, `runbook`, `note`.
    - Test 3: generated metadata includes synthetic IDs only, not production-looking customer names, real emails, real API keys, or golden-answer fields.
  </behavior>
  <action>Implement `generate_mock_corpus(target_dir: Path, *, dataset: str = "mock-v1") -> MockCorpusResult`. Generate a deterministic corpus of at least 12 Markdown files split across source-type folders, with German/operational ticket-like content, synthetic identifiers such as `MOCK-POL-1001`, `MOCK-OFF-2001`, `MOCK-SNOW-3001`, and `MOCK-JIRA-4001`, realistic systems/components, routing targets (`selbst lösen`, `weiterleiten Policen`, `weiterleiten Offerten`, `Rückfrage`), command-risk levels (`grün`, `gelb`, `orange`, `rot`), and scenario categories. Ensure generated docs remain normal Markdown knowledge files for Phase 6 indexing; do not write evaluation expected answers or hidden golden labels into document bodies.</action>
  <verify>
    <automated>uv run pytest tests/test_mock_corpus.py -q</automated>
  </verify>
  <done>Calling `generate_mock_corpus()` creates a broad importable mock corpus with mandatory metadata and visible body warnings for every file.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_mock_corpus.py -q` and ensure generated files are `.md` documents with Phase 3 metadata plus Phase 5 mock markers.
</verification>

<success_criteria>
- `src/ood/mock_corpus.py` exports `MockDocument`, `MockCorpusResult`, and `generate_mock_corpus`.
- Generated corpus contains at least 12 importable Markdown files.
- Source-type coverage includes ticket, wiki, jira_bug, servicenow_case, runbook, and note.
- Every generated document contains `mock: true`, dataset metadata, a synthetic identifier, and a visible body warning.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mock-corpus-contract-and-safety-foundation/05-01-SUMMARY.md`.
</output>
